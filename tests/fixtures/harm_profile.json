{
  "_note": "損害の輪郭（T52／T75）＝自席ターン番号 j（0 始まり）ごとに相手へ与えた損害（λ×ライフ + μ×札 + ν_meas×体）の平均。real = w41（実デッキ 300 局・2026-09-17）・syn = w39+w42（合成 900 局）。crossing_bridge.harm_profile の出力の写し（実測の表・新定数ゼロ）。測る記録と別のセットの輪郭を使う（crossing_bridge.profile_for・cross）。",
  "real": [
    0.0007,
    0.0783,
    0.1059,
    0.1798,
    0.2544,
    0.2559,
    0.2119,
    0.1802,
    0.1802,
    0.1802,
    0.1802,
    0.1802,
    0.1802
  ],
  "syn": [
    0.0004,
    0.0682,
    0.1121,
    0.1997,
    0.2453,
    0.2129,
    0.1849,
    0.151,
    0.1427,
    0.1001,
    0.1001,
    0.1001,
    0.1001
  ],
  "_note_sigma_t": "σ_T（T97）＝交点の橋の `curve` の読みの終局時刻の残差 σ（実測）。`σ_D = √2 × σ_T`（2 席の τ の差なので独立なら √2）。T75 以来 σ_T は「時間軸ヘッド r10 の決着ターン誤差 1.0」の借り物だったが、**同じ器（curve の τ）が自分の残差を測れる**のでそれを使う＝新定数ゼロ。**耐久の体の集合（THETA_BODY_MODE）ごとに違う**ので分けて持つ。測る記録と別のセットの値を使う（cross）。2026-09-18 実測（grow／flow／attackable・blockers の既定構成）。",
  "sigma_t": {
    "attackable": {
      "real": 1.546,
      "syn": 1.724
    },
    "blockers": {
      "real": 1.14,
      "syn": 1.294
    },
    "all": {
      "real": 1.546,
      "syn": 1.724
    }
  },
  "_note_w_bar": "w̄（T98）＝**`w(D)` の実測の平均**。`κ = w(D)/w̄` は「この局面の傾き ÷ 平均の傾き」なので、**分母は定義上 `E[w(D)]`**。従来の `0.5/R`（R = 平均の自席ターン数 4.128）は**閉じた形の代用**で、`D` の分布が「R ターンで勝率が 0.5 動く」形に一致するときだけ等しい。**σ_T を実測にし耐久の形を変えたら一致しなくなった**——`blockers` では `κ` の平均が 1.505／1.737（1 であるべき）。そこで**同じ器の実測**を使う＝新定数ゼロ。**耐久の体の集合ごと**に持ち、**測る記録と別のセット**の値を使う（σ_T・輪郭と同じ規約）。2026-09-18 実測（`theory_bridge` の `stats.w_mean`・σ_T は実測値を入れた条件）。",
  "w_bar": {
    "attackable": {
      "real": 0.127,
      "syn": 0.1519
    },
    "blockers": {
      "real": 0.1776,
      "syn": 0.2043
    },
    "all": {
      "real": 0.127,
      "syn": 0.1519
    }
  }
}
