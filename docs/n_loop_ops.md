# N系 学習ループの分散運用（正本・2026-09-01 ユーザ決定）

複数の Claude セッションで「教材収集 → 学習 → 検証」を回すための取り決め。
**コーディネータ**（本セッション）は指示書を書き、成果物を回収し、判定する。実行は全て
**独立した作業セッション**が行う。理由: コーディネータのコンテナは数十分ごとに巻き戻り
（2026-09-01 に1日で 19 回・35 分の訓練が 4 回連続で消失）、長時間ジョブを完走できない。

## 1. 原則

1. **やり取りは全て origin のブランチ経由**。チャット本文・ローカルファイル・/tmp は
   受け渡しに使わない（消える）。ブランチが唯一の正本。
2. **作業セッションはコードを変更しない**。指定ブランチを checkout して実行し、成果物を
   自分の出力ブランチに push するだけ。PR も作らない。
3. **成果物には必ず `RESULT.json` を添える**（機械可読・§5）。コーディネータはこれを読んで
   回収する＝チャットの報告文を解析しない。
4. **判断基準は実行前に決めて指示書に書く**（結果を見てから解釈を作らない）。
5. **seed 帯はコーディネータが台帳で払い出す**（§6）。作業セッションが勝手に選ばない。
6. 成果物のシャードを取捨選択しない。全部揃って初めて判定になる。

## 2. 役割と受け渡し

| 役割 | 入力（コーディネータが指示書に書く） | 出力（作業セッションが push する） |
|---|---|---|
| **生成** | 生成役ネットの取得元／波番号 W／シャード N／seed 帯／局数 | `claude/n{W}-w{NN}`: `n{W}_records/`・`n{W}_gen.log`・`RESULT.json` |
| **訓練** | 起点ネット／π 波の一覧／z 波の一覧／エポック・lr／出力名 | `claude/train-c{N}`: `n1_results/neff_net_c{N}.npz`・訓練ログ・`RESULT.json` |
| **検証** | 候補ネット／基準ネット／シャード N／seed 帯／ペア数／条件 | `claude/arena-{tag}-w{NN}`: `n1_results/arena_{tag}_dist/*.jsonl`・`RESULT.json` |
| **コーディネータ** | — | `claude/n1-results`（ネット・台帳の正本）／`docs/reports/`（判定の記録）／本書 §6 の seed 台帳 |

コーディネータの回収手順は役割ごとに固定（§4）。回収したネット・台帳は `claude/n1-results`
に取り込み、以後はそこが正本になる（作業セッションの出力ブランチは保全のため消さない）。

## 2.5 コマンドの所在（2026-09-07・Rust 化の第 2 段で移動）

**エンジンは Rust**（`rust/opcg_engine`）。生成・アリーナ・ゲート判定は `opcg_sim/loop/`、
訓練・評価帯は `opcg_sim/learned/train/` に移った（計画 `docs/rust_engine_plan.md` §17）。
どれも `python -m` で動く（`PYTHONPATH=tests` は要らない）。**wheel を先に入れること**
（`make rust-develop`）。

| 役割 | 旧 | 新 |
|---|---|---|
| 生成（棋譜ダンプ） | `tests/scripts/n_record_gen.py` | `python -m opcg_sim.loop.record_gen` |
| アリーナ（シャード） | `tests/scripts/arena_resume.py` | `python -m opcg_sim.loop.arena_shard` |
| 台帳の合算・判定 | `tests/scripts/arena_merge.py` | `python -m opcg_sim.loop.arena_merge` |
| 昇格ゲート・煙試験 | `tests/scripts/promotion_gate.py`／`arena_gate.py`／`n1_gate.py smoke` | `python -m opcg_sim.loop.gate {promote,band,smoke}` |
| 訓練（NRel／NEff） | `tests/scripts/n_rel_train.py`／`n_eff_train.py` | `python -m opcg_sim.learned.train.n_rel_train`／`.n_eff_train` |
| 評価帯 | `tests/scripts/n_rel_band.py` | `python -m opcg_sim.learned.train.n_rel_band` |

**生成の速さ**（2026-09-07 実測・同 seed 帯 6 局・sims 64・単プロセス）: Python 57.06 秒/局 →
**Rust 4.96 秒/局（11.5 倍）**。1 波 960 局は 1 セッションでも 1.3 時間ぶん（4 並列なら 20 分）。
シャード数・seed 帯の規約（§6 の台帳）は変えていない。

**記録形式は不変**＝`n_rel_train` は無変更で読める。ただし**乱数系統が変わった**（対局も探索も
Rust の決定的な生成器）ので、**同じ seed でも旧版と同じ局にはならない**。波をまたいだ seed の
一意性（台帳の役目）は従来どおり守ること。

## 3. 1 サイクルの流れ

```
[生成] 波 W を 8 シャード並列（各 960 局・4〜5 時間）
   ↓ claude/n{W}-w01..08
[コーディネータ] 回収・meta 確認（games/rows/dropped・seed 衝突なし）・データ窓を決める
   ↓ 指示書（訓練）
[訓練] warm-start で 2 エポック（約 70 分・RSS ~11GB）
   ↓ claude/train-c{N}
[コーディネータ] 回収・n1-results へ取り込み・評価帯で参考値・指示書（検証）
   ↓ 指示書（検証）
[検証] 8 シャード並列（各条件 24 ペア・25〜35 分）
   ↓ claude/arena-c{N}-w01..08
[コーディネータ] arena_merge で合算・判定・レポート・昇格なら生成役を切り替え
```

データ窓・昇格条件・評価帯の規約は `CLAUDE.md`「学習データ窓の運用」「アリーナ昇格」に従う。
必要局数の根拠と分散判定の規約は `docs/reports/2026-09-01_distributed_arena.md`。

## 4. コーディネータの回収チェックリスト

**生成**: 8 ブランチ揃う → 各 `meta_n_record.json` の `games`=960・`seed_base` が台帳どおり →
**`net` が指示した生成役と一致**（下記）→ `dropped` が数件以内 → `RESULT.json` の status=done。
1 本でも欠けたら訓練に進まない。

> **`net` の照合は必須**（2026-09-11 に追加）。`record_gen` は `--net` を**ローカルのパスとしてしか
> 解釈しない**（`engine.resolve_net` は `neff:`／`n1:`／`nrel:` の接頭辞を剥がすだけで、git の
> `branch:path` は解決しない）。**`--net` を落とすと既定の `DEFAULT_NET` で静かに回り、完走して
> 見えるのに前 era の教材ができる**——波 31 の初回指示でこの誤りを出した（訂正済み）。
> `meta_n_record.json` の `net` は `resolve_net` の結果を記録するので、**ここを見れば事後に判定できる**
> （生成役がツリー外の npz なら `~/nrel_r5.npz` のような絶対パスが入る。`opcg_sim/data/learned/` 配下の
> パスが入っていたら既定で回った証拠）。**`net` は生成の最後にしか書かれない**ので、走行中の
> 中間 push では確認できない＝回収時に必ず見る。指示書を書く側は、ツリーに無いネットを生成役に
> するとき `git show <branch>:<path> > ~/<name>.npz` で取り出してからローカルパスを渡す形にする。

> **走行中の進捗の見方**（2026-09-11 に判明）。**`get_session` の `post_turn_summary.status_detail`
> に作業セッションの現況が出る**（実測例「n31 gen shard 2: 210/480 games, ~53min elapsed」
> 「Wave 31 shard 8: 250/480 games in 63m; process PID 2956 live」）＝ブランチが立つ前でも
> 残り局数を掴める。**`updated_at` は停滞の指標にならない**——`run_in_background` で生成を投げた
> セッションは idle に戻り、40〜50 分更新されないのが正常な姿（16:13 の実測で 32 本中 20 本が
> その状態だったが、問い合わせた 4 本は全て健全に進行中だった）。
> **`list_events` は使えない**ので、作業セッションに何か報告させたいときは**ブランチに書かせる**
> （§1 の原則どおり）か post_turn_summary を見る。チャットで「報告してください」と頼んでも
> コーディネータには届かない。

**訓練**: `RESULT.json` の inputs（起点・π・z）が指示書と一致 → `epochs`/`best_ep`/val 指標を
記録 → ネットを `claude/n1-results` の `n1_results/` へ取り込む → 評価帯で参考値を測る。

**検証**: 8 ブランチ揃う → 各台帳 24 行 → `opcg_sim.loop.arena_merge` で条件ごとに合算 →
`dup_seeds`=0・`void` 率 ≤2% を確認 → 判定 → 台帳を `claude/n1-results` へ取り込む →
`docs/reports/` に記録。昇格なら次の指示書（生成）の生成役を新ネットにする。

## 5. RESULT.json の形式

作業セッションは出力ブランチの直下に置く。最低限これだけ（追加は自由）:

```json
{"job": "train-c10", "role": "train", "status": "done",
 "inputs": {"warm_start": "claude/n1-results:n1_results/neff_net_c8_e2.npz",
            "pi_waves": [16, 17, 18, 19], "z_waves": [14, 15, 16, 17, 18, 19],
            "epochs": 2, "lr": 2e-4},
 "outputs": {"net": "n1_results/neff_net_c10.npz"},
 "metrics": {"val_v_mse": 0.5185, "val_v_sign": 0.814, "pi_top1": 0.634, "best_ep": 0},
 "command": "python -m opcg_sim.learned.train.n_eff_train train ...",
 "notes": "ep1 はやや悪化"}
```

`status` は `done` / `partial`（局数・ペア数が指示に満たない）/ `failed`。`partial` と
`failed` は理由を `notes` に書く。コーディネータは `done` 以外を回収しない（やり直しを指示）。

## 6. seed 帯の台帳（コーディネータが払い出す・追記のみ）

規約: 生成 = `2000000 + W*10000 + N*1000`（W=波番号・N=シャード 1..8）。
検証 = 100000 番台〜（対戦ごとに 8 シャード分・条件ごとに別帯）。**既出の帯は再利用しない**。

| 用途 | 帯 | 状態 |
|---|---|---|
| 生成 波1〜18 | 2010000〜2188000 | 使用済み |
| 生成 波19 | 2191000〜2198000 | 使用済み（w05 は打ち切り＝75シャード約750局・meta無し・そのまま使う） |
| 生成 波20 | 2201000〜2208000 | 使用済み（2026-09-03 回収・7,680局・棄却5・生成役 c10＝era5。方策一致で c10 0.688 > c8 0.649 を確認） |
| 生成 波21 | 2211000〜2218000 | 使用済み（2026-09-03 回収・**w02 打ち切り（ユーザ決定 2026-09-03・そのまま使う）＝7シャード 6,720局**・棄却5・生成役 c10＝era5。方策一致 w01 で c10 0.678 > c8 0.635） |
| 生成 波22 | 2221000〜2228000 | 使用済み（2026-09-03 回収・**w02 打ち切り（ユーザ決定 2026-09-03・そのまま使う）＝7シャード 6,720局**・棄却5・生成役 c10＝era5。方策一致 w01 で c10 0.672 > c8 0.621。波20〜22 は同一 era＝π 窓は3波まとめて使う） |
| 生成 波23（**dump v2**・NRel P4） | 2231000〜2238000 | 使用済み（2026-09-05 回収・8 シャード 7,680 局・1,079,753 行・棄却 7・生成役 c10＝era5・sims 128・dump_version 2／enc_version 13・コード 0102748 以降） |
| 生成 波24（**dump v2**・NRel P4） | 2241000〜2248000 | 使用済み（2026-09-05 回収・8 シャード 7,680 局・1,084,300 行・棄却 6・生成役 c10＝era5・sims 128・dump_version 2／enc_version 13。波23 と同時に 16 セッションで生成〔ユーザ決定 2026-09-04・初物のためコーディネータが指示書をフルで書いた例外〕） |
| 生成 波25・26（**dump v2**・**era6＝生成役 a1**） | 2251000〜2258000／2261000〜2268000 | 使用済み（2026-09-06 回収・**14,400 局／15,360 局＝93.8%**〔ユーザ判断で途中打ち切り＋コンテナ再起動: n25 w02 830・w04 580＋w04b 150・n26 w03 770・w04 810・w05 840・w06 820・他 960〕・partial シャードは meta 無し・dump v2／enc 13。指示内容: 2026-09-05・ユーザ決定「R なし・相手デッキ知識あり」で a1 を出荷既定と生成役に。生成役 `claude/n1-results:n1_results/nrel_a1.npz`〔=同梱 `nrel_a1.npz`・0e0d649〕・`opcg_sim.loop.record_gen --dump-v2`・**16 セッション並列＝各波 8 シャード×960 局**（波23/24 と同じ。ユーザ決定 2026-09-05「トークン量の都合でセッションは 16 が上限」＝§7.3 の 16 シャード細分化は使わない）・コード f4fabcc 以降。r2 の訓練: π＝波25〜26（era6）・z＝波23〜26・warm-start a1・`--ablate rel`） |
| 生成 波27・28（**dump v2**・era6＝生成役 a1・r2b 用の追加教材） | 2271000〜2278000／2281000〜2288000 | 指示済み（2026-09-06・ユーザ決定「候補 1＝教材を足して再訓練」。生成役 a1〔同上〕・16 セッション×960 局・コード a21aa9d 以降〔serve 軽量化 1 段目込み＝葉評価 −23%〕・打ち切りはしない。1 サイクル指示書 §7.4 の第 2 回） |
| 訓練 r2b（era6 第 2 回・生成ワーカー 1 本のセッションで実施） | —（seed 帯不要） | 指示済み（2026-09-06。warm-start a1・`--ablate rel`・π＝波25〜28・z＝新しい波から載るだけ〔波24〜28 を試し OOM なら 23/24 の順に落とす〕・**epochs 2**（r2 の 1 epoch 疑問を同時に潰す）・lr 5e-4。出力 `claude/train-r2b` の `n1_results/nrel_r2b.npz`＋log＋RESULT.json。訓練はオーケストレータではなく生成ワーカー n28-w08 のセッション〔再起動耐性〕） |
| 検証 r2b vs a1 分散（生成に使った 16 セッションで実施） | ランダム 381000〜388000／ミラー 391000〜398000 | 払い出し済み（2026-09-06・候補 `neff:n1_results/nrel_r2b.npz`〔claude/train-r2b〕・基準 a1=出荷既定・16 セッション＝条件×シャード・各 24 ペア・出力 `claude/arena-r2b-{random|mirror}-wNN`・判断基準 2 条件とも wr≥0.55 かつ CI 下限>0.50） |
| 教材の棚卸し（Rust 化後のループ再開・2026-09-08） | — | **波27＝7,370 局（w05 が 650 で打ち切り）・波28＝6,370 局（w01 570・w04 510・w06 480 で打ち切り・w08 970）**＝計 13,740 局。波25〜28 は全て Python エンジン時代の生成（生成役 a1・dump v2・enc 13）だが、**新しい読み口（`dump_io.load_dump`・v2 をそのまま pack）と torch 経路で読めて学習できることは `dump-f16`／`train-torch2` の WP が n28-w01/w02 で実測済み**＝そのまま教材に使う。Rust の a1 は Python の a1 と決定が 99.97% 一致するので era は変えない（era6 のまま）。r2b（numpy・旧経路）は `claude/train-r2b-wip` に ep0 途中（45,000/48,026 ステップ・2026-09-07 22:09）で止まっており完走の見込みが無いため**打ち切り**＝同じ教材を新経路で r3 として訓練し直す |
| 訓練 r3（era6 第 2 回・新経路＝torch＋memmap・r2b の置き換え） | —（seed 帯不要） | 回収済み（2026-09-08・`claude/train-r3` cb11d28e・**val v_mse 0.5288／v_sign 0.810／pi_top1 0.602**〔r2: 0.5399／0.803／0.605〕・best ep0（ep1 は 0.5296 でやや過学習＝r2 系譜と同じ）・value 621.9 万行／policy 230.1 万点・読み込み 290 s・1 エポック 2,010 s・**全体 73 分**〔r2 は 424.8 万行で 114 分〕・OOM なし。VmHWM 13.6 GB は memmap の常駐ページ（ファイル裏付け・回収可能）を含む値で、匿名メモリは P/C＋torch ぶんだけ＝指示の「8GB」は匿名メモリの目安として妥当、VmHWM で読むと超えて見える。指示内容: warm-start a1・`--ablate rel`・`--backend torch`・π＝波25〜28・z＝波23〜24・epochs 2・lr 5e-4） |
| 検証 r3 vs a1（2 条件・各 8 帯×24 ペア・**Rust なので条件ごとに 1 セッション**） | ランダム 421000〜428000／ミラー 431000〜438000 | 使用済み（2026-09-08 判定: **規約上は非昇格**・主 **0.615 [0.560,0.669]・Elo +81**〔N 系で最大〕／副 0.510 [0.465,0.556]＝互角・void 0/0・`docs/reports/2026-09-08_r3_judgment.md`・台帳 n1-results 84a162ae。r2 と逆の構図（教材 2 波追加＋2 epoch で主条件が動きミラーは動かず）。**採用（ユーザ決定 2026-09-08・副条件は退行なしで可＝CLAUDE.md の規約に追記）＝出荷既定と生成役を r3 に・era7 開始**（`docs/reports/r3_adoption_20260908.md`）。各条件 1 セッション約 30 分＝1 サイクル約 2 時間になった） |
| 検証 探索設定 R2 vs 既定（同じ r3・候補席だけ **sims 160・worlds 4**・q_min_n 1/8・root_prior_temp 2） | ランダム 441000〜448000／ミラー 451000〜458000 | 使用済み（2026-09-09・WP `rs-search-arena`・`docs/rust_engine_plan.md` §20.5.3。ネットは両席 r3、違うのは探索設定だけ。判定基準は規約どおり〔主 wr≥0.55・CI 下限>0.50／副 退行なし〕。**候補設定は分析 #6 の `C_w4_r2` へ差し替えた**〔2026-09-09・`docs/reports/2026-09-09_scenario_analysis_06.md` の提案 1。旧: sims 640・worlds 1＝実効 sims は同じ 640 だが壁時計は 1.6 s → 0.39 s〕。準備箱は入れない〔訪問が分裂して worlds＋R2 の効きを消す＝§20.7.6 の後〕。候補席は worlds 4＝4 スレッドなので `--workers` は 4 コアで 2 まで。**2026-09-09 実測: 主 191 ペア 0.5314 [0.4757,0.5871]＝規約未達／副 192 ペア 0.5443 [0.5022,0.5863]＝退行なし・void 1/0・dup 0**。台帳 `n1_results/arena_search_r2/`・`docs/rust_engine_plan.md` §20.5.3-1。判定はユーザ） |
| WP `rs-removal-decks` の受け入れ自己対戦（synth_roles・sims 32・60 局 × 2 帯 ＋ 対照 synth 60 局） | 930000〜930059／940000〜940059 | 使用済み（2026-09-10・作業セッションが自前で取った帯を事後に登録。void 1/120＝PRB02-006 の置換効果ループ・§20.8.4） |
| 生成 波31（**dump v14**・era8＝**生成役 r5**・探索設定は波 30 と同じ sims 160・worlds 4・q_min_n 1/8・t=2・synth_roles・両方向 ε 3%） | 2311000〜2318500（16 シャード × 480 局＝§7.3 の細分化: シャード k の base は 2310000 + ceil(k/2)*1000 + (k 偶数なら 500)） | **2026-09-11 18:35 UTC 回収（ユーザ決定で打ち切り）: 14 シャード＝6,720 局・943,873 行・dropped 4・net 照合 14/14 とも `nrel_r5.npz`**（w16 は part1 30 局〔meta 無し・BrokenPipe で中断〕＋ p2 450 局＝480。w14 は RESULT.json 欠落・meta は健全）。**打ち切った 2 本も partial を push 済み**（20:46 UTC・保全の規約どおり）: w03（2312000・npz 15 個＝約 150 局・RESULT.json 無し）・w06（2313500・**120 局・17,434 行**・1 局 14.5 秒・コンテナが 8 回再起動〔15:11／16:02／16:27／16:50／17:55／18:27／18:58／19:40 UTC〕）。どちらも meta_n_record.json は打ち切りのため未生成。**r6 の教材には含めていない**（訓練を配った時点で 28 シャードに固定したため）が、**次世代（r7）の z 窓では使える**。帯は再走に使わず消費済みとして扱う。**波 31＋32 で 13,440 局・約 188 万行＝era7（波 29＋30 の 13,440 局・195 万行）と同等**なので、π の窓としては r5 が受け取った量に並んだ＝ここで締めて r6 の訓練へ進む（ユーザ決定 2026-09-11「打ち切りましょうか」）。指示済み（2026-09-11・**Proposal A 採用**・r5 を生成役へ昇格。判定根拠: 高分解能アリーナ 2,798 ペア wr 0.5050 [0.4908,0.5192]・手合意 58.9% >> noise 17.5%。同じ強さで異なる打ち筋＝新教材。16 セッションはコーディネータが create_session で起こす〔sonnet・tag `n31-gen`・出力ブランチ `claude/n31-w01`〜`w16`〕。指示内容: 生成役 r5・`--eps-target` は既定 0（波 30 の 0.3 は対象選択の対照を採るための一時措置で、その役目は終わった）。**初回の指示に誤りがあり 16 本へ訂正を送った（2026-09-11 15:11 UTC）**＝`--net` に `claude/train-r5:n1_results/nrel_r5.npz` と git 参照を書いたが、`engine.resolve_net` が受けるのは**ローカルのパスだけ**（`neff:`／`n1:`／`nrel:` 接頭辞のみ剥がす）で、`nrel_r5.npz` はツリーに無く `origin/claude/train-r5` にしかない。訂正の手順は「`git fetch origin claude/train-r5:r5net` → `git show r5net:n1_results/nrel_r5.npz > ~/nrel_r5.npz` → `--net ~/nrel_r5.npz`」。**`--net` を省くと既定の r3 で回り、完走して見えるのに era7 の教材になる**ので省略を禁じた。初回指示は RESULT.json の形式も書き漏らしており、訂正で補った） |
| 計測 方策の同一行比較（`n_rel_band --policy`・**a1／r3／r5／r6 × 波 28／30／32**・各波 w01＋w02 の holdout・方策点は全部＝`--policy-limit 0`） | —（seed 帯不要） | **2026-09-12 02:15 UTC 回収**（`claude/band-policy-r6` 71161600・所要 8 分）。**方策は現在の分布では劣化していない**（波 32 で a1→r3→r5→r6 が top1・v_mse とも単調改善）。**代わりに忘却が世代ごとに深い**: 波 28 の top1_visits が r3 0.631 → r5 0.577 → **r6 0.498（a1 の 0.585 以下）**。3 波の平均で r6 は r5 より良くない（v_mse +0.002・top1 −0.035）。**候補が基準に勝った波の数がアリーナを当てる**: r3 vs a1 3/3 → +81 Elo・r5 vs r3 2/3 → +3.5・r6 vs r5 1/3 → 0 前後。＝**最新波だけを見る評価帯は候補を地元で測っていて改善を過大に見せていた**。機構の仮説は π の窓（現 era のみ・era7/8 は 2 波）による特化＋忘却。`docs/reports/2026-09-12_policy_band.md`。次の一手（ユーザ判定）: π の窓を直前 era まで広げた r7・評価帯は 3 波の行列で読む。指示済み（2026-09-11 23:50 UTC・r6 判定の次の一手 (1)・ユーザ決定「1をやってください」。**目的**: 「V は伸びるが打ち手は動かない」乖離の原因が方策の劣化かを**同じ行で**決着させる。訓練 val の pi_top1 は r3 0.602 → r5 0.619 → r6 0.5855 だが holdout が世代ごとに違うので今は語れない。**設計**: 各波の `visits` はその時代の生成役の木が作った分布＝r3 は波 28・r5 は波 30・r6 は波 32 が「地元」。3 波を並べて地元有利と忘却を分けて読む。波 28 は dump v2 で `pol_p` が無いので q_improved の π' は `--pi-prior-net`＝a1（当時の生成役）・波 30／32 は v14 の `pol_p` をそのまま使う。「門」の指標（除去候補への P 質量・argmax が除去の割合・教材が除去を選んだ割合）も同時に出る＝r5 の手合意調査（除去の選択率 0.145→0.166）と突き合わせる。出力 `claude/band-policy-r6` の `band_policy_w{28,30,32}.json`＋log＋RESULT.json。実行は r6 セットと同じセッション（教材 6 波・r5・r6・torch が手元にある）） |
| 訓練 r6（era8 初回・warm-start r5・生成ワーカー n31-w09 のセッションで実施） | —（seed 帯不要） | **2026-09-11 22:43 UTC 回収**（`claude/train-r6` 202984ed）: **val v_mse 0.4672／v_sign 0.8297／pi_top1 0.5855・best ep0**（ep1 は過学習＝r2〜r4 と同じ形・r5 の ep1 が例外だった）・value 5,966,525 行／policy 784,205 点・訓練 3,111 s（52 分）・**OOM なし**（z は波 31・32〔`--in` 由来〕＋30・29・28・27 の 6 波が全部載った）。**評価帯（波 32 holdout 19,375 行・r5 と同じ行）で v_mse 0.5263 → 0.5115〔−2.8%〕・v_sign 0.8121 → 0.8149＝また改善した**。申告 2 点: (a) `download.pytorch.org` が組織の egress ポリシーで 403 のため pypi 経由で torch 2.14.0 を導入（CPU 実行）・(b) ワーカー初期化時に `opcg_effects.json` の再生成が複数プロセスで競合して一過性の FileNotFoundError が出たが自己回復し成果物に影響なし＝**次の指示書では生成前に 1 回 `export_effects_json` を単独実行させる**とよい。指示済み（2026-09-11 18:40 UTC。π＝波 31＋32〔era8・28 シャード〕・z＝新しい波から順に載るだけ〔32→31→30→29→28→27 を試し OOM なら古い順に落とす〕・warm-start `claude/train-r5:n1_results/nrel_r5.npz`・`--ablate rel`・`--aux-weight 0.1`・**lr 2e-4**〔§7 の既定・5e-4 は r3 からの再訓練で V を崩した〕・`--epochs 2` で best ep・`--backend torch`・`--pi-teacher visits`。出力 `claude/train-r6` の `n1_results/nrel_r6.npz`＋`train_r6.log`＋RESULT.json。**訓練→評価帯→アリーナを 1 セッションのセットで回す**〔§7〕。z の窓を狭める例外条件は不発＝r5 vs r3 のアリーナが 0.5050 で「極端に高い」に当たらないため通常の窓でよい） |
| 検証 r6 vs r5（主 random × synth_roles／副 固定ミラー・各 8 帯×24 ペア・両席 serve 既定・訓練と同じセッションが続けて回す） | ランダム 551000〜558000／ミラー 561000〜568000 | **2026-09-11 22:45 UTC 判定: 非昇格**・主 190 ペア **0.5132 [0.4582, 0.5681]・+9.1 Elo**＝未達／副 192 ペア **0.474 [0.4283, 0.5196]・−18.1 Elo＝CI 下限が関門 0.45 を割った（NRel で初）**・void 2/0（1.0%）・dup 0。合算はコーディネータが `arena_merge` を独立に回して作業セッションの報告値と一致を確認。`docs/reports/r6_judgment_20260911.md`。**出荷既定は r3 のまま・生成役も r5 のまま据え置き**（r6 を生成役に上げる根拠は無い＝r5 を上げた理屈〔同じ強さで異なる打ち筋なら新教材になる〕を era8 が 1 回試して伸びなかったため）。払い出し済み（2026-09-11 18:40 UTC。候補＝`claude/train-r6` の `n1_results/nrel_r6.npz`・基準＝r5〔生成役・`--baseline` に r5 のローカルパスを明示。**出荷既定はまだ r3 なので `--baseline` 空にしてはいけない**〕。判定は規約どおり: 主 wr≥0.55 かつ CI 下限>0.50／副は退行なし〔CI 下限 ≥0.45〕。void 率 2% 超なら判定を出さない） |
| 生成 波32（era8＝生成役 r5・**波 31 と同一条件**） | 2321000〜2328500（16 シャード × 480 局・シャード k の base は 2320000 + ceil(k/2)*1000 + (k 偶数なら 500)） | **2026-09-11 18:35 UTC 回収（ユーザ決定で打ち切り）: 14 シャード × 480 局＝6,720 局・943,351 行・dropped 0・net 照合 14/14 とも `nrel_r5.npz`**。**打ち切った 2 本も partial を push 済み**（20:46 UTC・保全の規約どおり）: **w06（2323500・450/480 局・63,715 行・main 29,569 行・void 0・forced play 329／hold 45／target 116・1 局 10.2 秒）**＝17:58 の再実行が再起動なしで 450 局まで走り切っていた〔打ち切りが 30 局分だけ惜しかった〕・w12（2326500・130 局・17,454 行・main 8,136 行・p1 120 局＋p2 10 局・プロセスが 2 回無言で停止）。どちらも meta_n_record.json は打ち切りのため未生成。**r6 の教材には含めていない**（訓練を配った時点で 28 シャードに固定したため）が、**次世代（r7）の z 窓では使える**——とくに w06 の 450 局は 1 シャードぶんに近い。帯は再走に使わず消費済みとして扱う。**era8 の教材は合計 14,290 局（13,440 ＋ partial 約 850）＝目標 15,360 の 93%**（波 25/26 の 93.8% と同水準）。指示済み（2026-09-11 15:22 UTC・**波 31 と並列**。根拠: π 窓は現 era のみ＝波 31 だけでは era8 の π が 1 波しかない。era6 は波 25+26 の r2 が非昇格、波 27+28 を足した r3 が採用〔主 0.615・+81 Elo〕＝同じ era に教材を足して伸びた実例があり、era7 も 2 波〔29+30〕で回した。**実行は波 30 の生成セッション 16 本を使い回す**〔tag `n30-gen`・全て IDLE で生存・ユーザ提案 2026-09-11〕＝波 31 の 16 本と合わせて**同時 32 セッション**。ユーザ決定 2026-09-05 の「セッション上限 16」を今回だけ超えるのはユーザの明示指示による。直列なら計 4 時間の壁時計が 2 時間になる。出力ブランチ `claude/n32-w01`〜`w16`。指示は最初から `git show r5net:n1_results/nrel_r5.npz > ~/nrel_r5.npz` → `--net ~/nrel_r5.npz` の形で書いた〔波 31 の訂正を反映〕。r6 の訓練は π＝波 31＋32・z＝新しい波から載るだけ・warm-start r5・lr 2e-4） |
| WP `rs-aux-heads` の測定用生成（200 局・sims 32・既定 synth・ツリー外）＋ 台帳コスト計測 20 局 | 2999000〜2999199／2999500〜2999519 | 使用済み（2026-09-10・作業セッションが自前で取った帯を事後に登録。生成規約の波番号とは衝突しない） |
| WP `rs-replace-rest-fix`＋`rs-eps-explore` の受け入れ自己対戦（synth_roles・sims 32・120 局 ＋ ε 60 局） | 930000〜930059／940000〜940059（再利用・npz は保存せず） | 使用済み（2026-09-10・エンジン修正の確認だけ・教材にはしない） |
| 検証 高分解能 r5 vs r3（主 random × synth_roles のみ・16 シャード × 175 ペア＝2,800 ペア・`--bands 1`・両席 serve 既定・波 30 の生成セッション 16 本を使い回し） | 1001000〜1016000（シャード k の base は 1001000 + (k−1)×1000・各 175 seed） | 払い出し済み（2026-09-11・`docs/rust_engine_plan.md` §20.9.7。候補 `claude/train-r5` ea9f031d の nrel_r5.npz・基準 r3。目的は ±10 Elo の分解能で era7 の改善が実在するかを確定すること・出力 `claude/arena-hires-w01`〜`w16`。**2026-09-11 実測: 2,798 ペア・wr 0.5050 [0.4908, 0.5192]・+3.5 Elo [−6.4, +13.4]・void 2・dup 0＝改善 0**・壁時計 1 時間（1 ペア 4.5〜10.3 秒・4 ワーカー）・`docs/reports/2026-09-11_hires_arena.md`） |
| 検証 r5 vs r3（主 random × synth_roles／副 固定ミラー・各 8 帯×24 ペア・両席 serve 既定・訓練セッションが続けて回す） | ランダム 531000〜538000／ミラー 541000〜548000 | 払い出し済み（2026-09-11・`docs/rust_engine_plan.md` §20.9.5。候補 `claude/train-r5` の `n1_results/nrel_r5.npz`〔warm-start r3→v14・π 波 29+30・z 30→27・aux 0.1・lr 2e-4〕・基準 r3＝出荷既定。**2026-09-11 実測: 主 192 ペア 0.5026 [0.4492, 0.5560]・+1.8 Elo＝未達／副 192 ペア 0.5365 [0.4952, 0.5777]＝退行なし・void 0/0・dup 0**・`docs/reports/r5_judgment_20260911.md`・成果物 `claude/train-r5` ea9f031d〔訓練 2,312 s・評価帯 1,494 s・アリーナ 3,296 s＝1 セッション 1 セット 2 時間〕） |
| 生成 波30（**dump v4・符号化 v14**・era7＝生成役 r3・探索設定は波 29 と同じ sims 160・worlds 4・q_min_n 1/8・t=2・synth_roles・両方向 ε 3%・**`--eps-target 0.3`**） | 2301000〜2308500（16 シャード × 480 局＝§7.3 の細分化: シャード k の base は 2300000 + ceil(k/2)*1000 + (k 偶数なら 500)） | 払い出し済み（2026-09-11・`docs/rust_engine_plan.md` §20.9.4。r5 の訓練: warm-start r3〔v14 へ pad〕・π＝波 29＋30・z＝波 30→29→28→27・`--aux-weight 0.1`・lr 2e-4・visits 教師。16 セッションはコーディネータが create_session で起こした〔sonnet・tag `n30-gen`・出力ブランチ `claude/n30-w01`〜`w16`〕。**2026-09-11 05:50 UTC 回収: 14 シャード × 480 局＝6,720 局・974,671 行（main 442,922）・forced play 4,585／hold 880／target 3,926・void 2（w09 seed 2305269＝DON_BOX が apply に届く→`rs-void-2305269`／w10 seed 2305912＝症状未特定）・dropped 3・1 局 10.8〜21.5 秒〔コンテナ差〕。w06（2303500）・w11（2306000）は再起動を挟んで進行中＝揃い次第 r5 以降の教材に足す。w15 は part1 200 局の sidecar 無し（npz は健全・§20.9.4-1）**） |
| 生成 波29（**dump v4**・era7＝生成役 r3・探索設定 sims 160・worlds 4・q_min_n 1/8・t=2・synth_roles・両方向 ε 3%） | 2291000〜2298000（16 シャード × 480 局＝§7.3 の細分化: シャード k の base は 2290000 + ceil(k/2)*1000 + (k 偶数なら 500)） | 払い出し済み（2026-09-10・`docs/rust_engine_plan.md` §20.8.6。r4 の訓練: π＝波 29（era7）・z＝新しい波から載るだけ・warm-start r3・`--ablate rel`・`--aux-weight 0.1`。**16 セッションはコーディネータが create_session で起こした**〔2026-09-10・ユーザ指示・モデル sonnet・tag `n29-gen`・出力ブランチ `claude/n29-w01`〜`w16`〕。**2026-09-10 回収: 14 シャード × 480 局＝6,720 局・974,297 行（main 442,217）・dropped 1（void 1: seed 2296720・ST29-001 vs OP04-040・max_steps＝要調査 §20.8.6-1）・forced play 4,505／hold 783・除去が合法な main 行 178,924・1 局 12.8〜14.8 秒。k=4（2292500）と k=13（2297000）はユーザ判断で打ち切り＝帯は未使用（再走可）**） |
| 検証 r4 vs r3（2 条件・各 8 帯×24 ペア・条件ごとに 1 セッション・両席 serve 既定の探索設定） | ランダム 461000〜468000／ミラー 471000〜478000 | 払い出し済み（2026-09-10・`docs/rust_engine_plan.md` §20.8.8。候補＝`claude/train-r4-lr2` の r4a／r4b のうち評価帯で良い方・基準 r3＝出荷既定。**2026-09-10 実測: 主 191 ペア 0.4555 [0.4017,0.5093]＝未達／副 192 ペア 0.5469 [0.4982,0.5955]＝退行なし・void 1/0・dup 0**・`docs/reports/r4_judgment_20260910.md`） |
| 検証 配備案「r4 ＋ worlds 4 ＋ R2」vs 既定「r3 ＋ 既定設定」（2 条件・各 8 帯×24 ペア・候補席だけ探索設定を上書き） | ランダム 481000〜488000／ミラー 491000〜498000 | 払い出し済み（2026-09-10・§20.8.8-1・教師どおりの物差しで r4 を測る。候補 `claude/train-r4-lr2` の nrel_r4b.npz。**2026-09-10 実測: 主 192 ペア 0.4818 [0.4242,0.5394]・−13 Elo＝未達／副 192 ペア 0.4609 [0.4170,0.5049]＝退行シグナル・void 0/0**・`docs/reports/r4w4_judgment_20260910.md`） |
| 検証 r4 vs r3・random × **synth_roles**（教師と同じデッキ分布・両席既定・8 帯×24 ペア） | 501000〜508000 | 払い出し済み（2026-09-10・§20.8.8-2 の妥当性検討 (b)。同じセッションで (a) 波 28 holdout の評価帯も測る。**2026-09-10 実測: 192 ペア 0.5182 [0.4654,0.5711]・+13 Elo・void 0＝有意差なし／波 28 holdout は r4 が全バケットで悪化（忘却）**・`docs/reports/r4_validity_20260910.md`） |
| 検証 r4q（π を Q で補正した教師・WP `rs-q-pi`）vs r3（主 random × synth_roles／副 固定ミラー・各 8 帯×24 ペア） | ランダム 511000〜518000／ミラー 521000〜528000 | 払い出し済み（2026-09-10・§20.6.2。**2026-09-10 実測: 主 192 ペア 0.5182 [0.4649,0.5716]・+13 Elo＝未達／副 192 ペア 0.5104 [0.4630,0.5579]＝退行なし・void 0/0**・`docs/reports/2026-09-10_q_pi_judgment.md`） |
| 訓練 切り分け a3（登場時スキャン v7 の 3 列を遮断・ユーザ決定 2026-09-06「外してみましょうか」） | —（seed 帯不要） | 指示済み（2026-09-06。a1 と同じ教材（波23〜24 dump v2）・同じ手順で `n_rel_train.py --ablate rel,onplay`・warm-start なし・epochs 2・lr 5e-4・hidden 192。出力 `claude/train-a3` の `n1_results/nrel_a3.npz`＋log＋RESULT.json。動機: 登場時スキャンは葉ごとの make/unmake 実測（新規ノードの 30%）で、2026-08-02 の導入時（gen10）から強さへの寄与は未実証。serve は `ablate` に onplay があるとスキャン自体を省く（`E.encode(skip_onplay)`）。判定: 2 条件アリーナ a3 vs a1〔次行〕で互角（CI が 0.5 を跨ぐ・下限 ≥0.45）なら採用＝出荷既定と生成役を a3 に） |
| 検証 a3 vs a1 分散（2 条件・各 8 シャード×24 ペア） | ランダム 401000〜408000／ミラー 411000〜418000 | 払い出し済み（2026-09-06・候補 `neff:n1_results/nrel_a3.npz`〔claude/train-a3〕・基準 a1=出荷既定〔`--baseline` 空〕・16 セッション＝条件×シャード・出力 `claude/arena-a3-{random|mirror}-wNN`。読み方: 候補の勝率 CI が 0.5 を跨げば同等＝スキャンは強さに寄与せず） |
| 訓練 r1（NRel Stage A・初回・`n_rel_train.py`） | —（seed 帯不要・holdout は seed%7==0） | 指示済み（2026-09-05。入力: π＋z＝波23〜24 の dump v2（旧形式の波は使えない＝z の追加窓なし）・warm-start なし・epochs 2・lr 5e-4・hidden 192・bs 256/64。出力: `claude/train-r1` の `n1_results/nrel_r1.npz`＋log＋RESULT.json。判断は評価帯（新帯・後述）→h1〜h6 一致率→レイテンシ→アリーナ r1 vs c10（帯 281000〜/291000〜）の順） |
| 訓練 切り分け a1／a2／c12（r1 の要因分解・ユーザ要望 2026-09-05） | —（seed 帯不要） | 回収済み（2026-09-05・n1-results 0e0d649・3 本とも best ep0。訓練 val〔同じ 2 万行〕v_mse: r1 0.5218／a1 0.5352／a2 0.5280／c12 0.5474〔c10 は評価帯 0.556〕・pi_top1: r1 0.622／a1 0.632／a2 0.620／c12 0.655。所要 a1 7417s・a2 8072s・c12 1207s。指示内容: 同じ教材（波23〜24 dump v2）・同じ訓練で 3 本: **a1**=r1 の構造から関係 R を遮断（`opcg_sim.learned.train.n_rel_train --ablate rel`）／**a2**=相手デッキ知識を遮断（`--ablate opp_pool`）／**c12**=c10 を波23〜24 で warm-start 再訓練（`n_eff_train.py`・π＋z＝波23〜24・epochs 2・lr 2e-4＝教材だけで c 系譜が伸びるかの対照）。出力: `claude/train-a1`/`train-a2`/`train-c12` の `n1_results/nrel_a1.npz`/`nrel_a2.npz`/`neff_net_c12.npz`＋log＋RESULT.json。判定: 評価帯（同じ行）→ 主条件のみの分散アリーナ **各ネット vs r1**（帯は次行で払い出し）。先行の serve 時遮断（訓練なし・r1 の依存）: R 遮断で v_mse 0.531→0.574・opp_pool 遮断で →0.637・両方で →0.751＝r1 は両方を使っている。運用: **r1 と同じ逐次手順を 3 セッション並列**（ユーザ要望 2026-09-05「16 セッション可・並列化」に対し、1 本の訓練を複数セッションに分けると更新順序・同期回数が変わり切り分けが成立しないため見送り。4 スレッド化も 1 シャード 276s→272s で効果なし）・**途中経過は各セッションが 15 分ごとにログと暫定ネットを出力ブランチへ push**（`progress.json`＋`train_{name}.log`＋`n1_results/{out}`・完了時に RESULT.json） |
| 検証 切り分け a1／a2／c12 vs r1（主条件のみ・各 8 シャード×24 ペア） | a1 331000〜338000／a2 341000〜348000／c12 351000〜358000 | 使用済み（2026-09-05 判定・候補=遮断側の勝率: **a1（R なし）0.544 [0.487,0.601]**＝R は強さに寄与せず／**a2（相手デッキ知識なし）0.503 [0.448,0.557]**＝寄与せず（void 1）／**c12（c10＋同じ教材）0.430 [0.378,0.482]**＝r1 に有意に負ける＝構造（枠トークン＋状態 S）の寄与。一致率 h1〜h6（317 点・seeds 4・sims 160・作業セッション 3 本で計測・ログ n1-results 467ebf0）: c10 0.322／r1 0.355／**a1 0.345／a2 0.364／c12 0.311**〔PLAY: 0.196／0.237／0.224／0.244／0.212・ACTIVATE_MAIN: 0.125／0.229／0.198／0.229／0.083〕＝一致率でも R と相手デッキ知識の有無は誤差の範囲・構造の差（c12→r1）が主。レイテンシ sims160・3 盤面×2: c10 2.79s／r1 1.83 倍／a1 1.67 倍／a2 1.77 倍。台帳 n1-results 63861b4・`docs/reports/2026-09-05_r1_ablation.md`。指示内容: 候補=各ネット `neff:n1_results/{nrel_a1|nrel_a2|neff_net_c12}.npz`・**基準=r1** `--baseline neff:n1_results/nrel_r1.npz`〔n1-results 0e0d649〕・主条件のみ `--leaders random --decks synth`・24 セッション＝ネット×シャード・各 4 CPU・出力ブランチ `claude/arena-abl-{a1|a2|c12}-wNN`・台帳 `n1_results/arena_abl_dist/{a1|a2|c12}_wNN.jsonl`。読み方: 候補（遮断側）の勝率が 0.5 を有意に下回れば遮断した要因が効いている〔CI 上限<0.50〕・0.5 と区別できなければ効いていない。c12 が r1 に有意に負ければ構造の寄与、c12 が c10 相当かは別途 c12 vs c10 は測らず訓練 val で読む） |
| 訓練 r2（era6 初回・オーケストレータ自身が実施） | —（seed 帯不要） | 回収済み（2026-09-06・n1-results b5c4246・val v_mse 0.5399／v_sign 0.803／pi_top1 0.605・value 424.8 万行／policy 118.3 万点・6837s・OOM なし。オーケストレータのコンテナが 15〜25 分ごとに再起動して完走できず生成ワーカー n26-w08 のセッションで実行〔運用上の注意: §7.4 の「訓練はオーケストレータ自身」は再起動耐性が無い場合は成り立たない〕。指示内容: 2026-09-05・**1 サイクル指示書**〔§7.4〕の一部。warm-start a1・`--ablate rel`・π＝波25〜26・z＝波23〜26〔OOM なら波23 を落とす〕・epochs 1〔4 本連続で best ep0 だった実測に基づく短縮・ユーザ未確認なら 2 に戻す〕・lr 5e-4。出力 `claude/train-r2` の `n1_results/nrel_r2.npz`＋log＋RESULT.json） |
| 検証 r2 vs a1 分散（生成に使った 16 セッションで実施） | ランダム 361000〜368000／ミラー 371000〜378000 | 使用済み（2026-09-06 判定: **非昇格**・主 0.495 [0.437,0.553]〔190 ペア・void 2〕／副 0.596 [0.554,0.639]・`docs/reports/2026-09-06_r2_judgment.md`・台帳 n1-results b5c4246。生成役は a1 のまま。指示内容: 2026-09-05・候補 `neff:n1_results/nrel_r2.npz`〔claude/train-r2〕・基準 a1=出荷既定〔`--baseline` 空〕・16 セッション＝条件×シャード・各 24 ペア・出力 `claude/arena-r2-{random|mirror}-wNN`・判断基準 2 条件とも wr≥0.55 かつ CI 下限>0.50） |
| 検証 c5a〜c9（単一セッション期） | 101000〜120000 | 使用済み |
| 検証 c9 vs c8 分散 | ランダム 201000〜208000／ミラー 211000〜218000 | 使用済み |
| 参考 c10 vs G15（出荷既定・単一セッション24ペア×2条件） | ランダム 121000／ミラー 122000 | 使用済み（2026-09-02） |
| 検証 c10 vs c8 分散 | ランダム 221000〜228000／ミラー 231000〜238000 | 使用済み（2026-09-02・主0.589/副0.568＝昇格・era5開始） |
| 検証 c11 vs c10 分散 | ランダム 241000〜248000／ミラー 251000〜258000 | 使用済み（2026-09-03 判定: **非昇格**・主 0.539 [0.485,0.593]／副 0.503 [0.459,0.546]・void 0・`docs/reports/2026-09-03_c11_judgment.md`。生成役は c10 のまま・N系ループはここで反復停止＝設計フェーズへ。指示内容: 訓練 train-c11: warm c10・π=波20〜22・z=波16〜19→OOM で 17〜19 に縮小（実績・val v_mse 0.6052/v_sign 0.768/pi_top1 0.681・best ep0・n1-results 5f882b0）。評価帯参考値 v_mse（w01+w03 holdout・c8_e2/c10/c11）: 旧帯 波16〜18 0.5615/0.5423/0.5399・新帯 波20〜22 0.6318/0.6214/0.6130＝世代順に単調改善・判断基準 2条件とも wr≥0.55 かつ CI下限>0.50。リーダー入替は従来どおり席のみ＝`cand_leaders` 記録あり） |
| 検証 c10 vs G15 正式（分散・出荷既定切替の確認） | ランダム 261000〜268000／ミラー 271000〜278000 | 使用済み（2026-09-03 判定: **2条件とも達成**・主 0.633 [0.580,0.686]／副 0.589 [0.541,0.637]・void 0・`docs/reports/2026-09-03_c10_vs_g15_formal.md`。指示内容: 候補 c10=`neff:`・基準 G15=明示パスの value,policy ペア〔既定が c10 に変わっても基準が動かない〕・8シャード×24ペア×2条件・判断基準 2条件とも wr≥0.55 かつ CI下限>0.50） |
| 検証 r1 vs c10 分散 | ランダム 281000〜288000／ミラー 291000〜298000 | 使用済み（2026-09-05 判定: **2条件とも達成**・主 0.557 [0.503,0.611]／副 0.591 [0.547,0.635]・void 0・dup 0・`docs/reports/2026-09-05_r1_judgment.md`・台帳 n1-results 3a14a3c。出荷既定への切替と生成役交代（era6）はレイテンシ 2.0 倍の受容と合わせてユーザ判断待ち。指示内容: 候補 r1=`neff:n1_results/nrel_r1.npz`〔n1-results 1cd37ce・`LearnedEngine` が NRel を自動判別〕・基準=出荷既定 c10（`--baseline` 空）・コード 4f95955 以降・8 シャード×24 ペア×2 条件〔主=`--leaders random --decks synth`／副=既定の固定ミラー〕・**16 セッション＝条件×シャードを 1 セッションずつ**（ユーザ決定 2026-09-05・各 4 CPU＝`--workers 4`・出力ブランチ `claude/arena-r1-{random|mirror}-wNN`・台帳ファイルは `n1_results/arena_r1_dist/{random|mirror}_wNN.jsonl`）・判断基準 2条件とも wr≥0.55 かつ CI下限>0.50・void>2% なら判定しない。先行シグナル: 評価帯（波23 w01+w02 holdout 38,164 行・同じ行）v_mse c10 0.5559→r1 0.5313・v_sign 0.793→0.806（全ターン帯で改善・終盤 0.464→0.422 が最大）／レイテンシ sims160・m2 5 盤面で c10 比 2.0 倍〔非自明 3 盤面 1.77s→3.53s〕／人間リプレイ h1〜h6 一致率（seeds 4・sims 160・317 点）c10 0.322→r1 0.355〔PLAY 0.196→0.237・ACTIVATE_MAIN 0.125→0.229・ATTACK 0.398→0.378〕。`docs/reports/2026-09-05_r1_first_signal.md`） |
| 対照実験 残り起動（腕A2=c10+residual_activate low vs 腕B=c10・紫リーダー×掘り差し込み合成デッキ `--leaders purple --decks synth_dig`・8シャード×24ペア） | 311000〜318000 | 使用済み（2026-09-03・全体 0.523 [0.471,0.576]・発火 37/384 局のみで判定不能。`high` 方針の 321000〜328000 は予約のまま） |
| 対照実験 残ドン掘り（腕A=c10+residual_dig vs 腕B=c10・紫リーダー×掘り差し込み合成デッキ `--leaders purple --decks synth_dig`・8シャード×24ペア） | 301000〜308000 | 使用済み（2026-09-02・全体 0.464 [0.411,0.516]＝基準未達・発火ペア 0.438・掘りは報われず＝教材に混ぜない。`docs/reports/2026-09-02_residual_dig_cf.md`） |

## 7. 指示書テンプレート

オーケストレータ用（セッションを生成して配る側）と、作業セッション用（配られる側）の
2 層。`{ }` はコーディネータが埋める。

**セッションは使い回す（ユーザ決定 2026-09-10）**: 環境構築（pip・wheel・教材の取り出しと pack）に 15〜25 分かかるので、訓練系の作業セッションは 1 本を常設にし、次の仕事はコーディネータが poke（`create_trigger` の `persistent_session_id` ＋ `fire_trigger`）で渡す。セッション側は `~/n29_wave` 等の教材と wheel を残したまま待機し、コンテナが巻き戻っていたら準備スクリプトを自分で再実行する（冪等）。コンテキストが 60 万トークンを超えたら新しいセッションへ交代する。

**訓練 → 評価帯 → アリーナは 1 セッションのセットで回す（ユーザ決定 2026-09-10）**: 候補ネットの訓練指示書には評価帯（旧分布と新分布の 2 波）と判定アリーナ（主 synth_roles・副ミラー）まで含める。分割して回収を挟むより手戻りが少ない。壁時計を縮めたければアリーナのシャードを別セッションに並列で配る（セットの中身は変えない）。

**訓練は 1 コンテナ 1 ジョブ・順次実行（2026-09-10）**: コンテナは物理コア 4 個で、torch は 1 プロセスで 4 コアを使い切る。2 本を同時に回すとスレッド競合で 1 エポック 10 分 → 65 分（r4 再訓練で実測）。複数の案を比べるときも同じセッション内で順に回す。

**lr の既定（2026-09-10・§20.8.7-2）**: warm-start の再訓練は **lr 2e-4**。5e-4 は r3 からの再訓練で V を崩した（波 29 の holdout で r3 より悪化・aux の有無に依らず）。2 エポック目は過学習しやすいので best epoch を採る。

### 7.1 訓練（1 セッション）

```
学習CPUのネットを1本訓練してください。コードは変更せず、下記を実行して結果ブランチを
push するだけです。PR は作らないでください。メモリは 12GB 以上必要です。

bash で以下を順に実行:

pip install numpy
cd ~ && git clone https://github.com/gx5gyqe2-art/opcg-sim-backend.git
cd opcg-sim-backend
git fetch origin {code_branch}:work && git checkout work
git fetch origin claude/n1-results:res
git show res:{warm_start_path} > ~/start.npz
for N in {all_waves}; do
  for w in 01 02 03 04 05 06 07 08; do
    git fetch -q origin claude/n${N}-w$w:tmpw
    mkdir -p ~/n${N}_wave/w$w
    git archive tmpw n${N}_records | tar -x -C ~/n${N}_wave/w$w
    git branch -qD tmpw
  done
done
OPCG_LOG_SILENT=1 python -m opcg_sim.learned.train.n_eff_train train \
  --in {pi_dirs} \
  --z-in {z_dirs} \
  --epochs {epochs} --lr {lr} --warm-start ~/start.npz --out ~/neff_net_{name}.npz \
  2>&1 | tee ~/train_{name}.log

git checkout -B claude/train-{name} res
mkdir -p n1_results && cp ~/neff_net_{name}.npz n1_results/ && cp ~/train_{name}.log .
（RESULT.json を §5 の形式で書く。metrics はログ末尾の val 行から転記）
git add -f n1_results/neff_net_{name}.npz train_{name}.log RESULT.json
git -c user.email=g.x5gyqe2@gmail.com -c user.name=worker commit -m "train {name}"
git push -u origin claude/train-{name}

補足: 所要は約 70 分。OOM で落ちたら z の波を古い方から1つ減らして再実行し、
RESULT.json の notes に減らした旨を書いてください。
```

`{pi_dirs}` は `~/n16_wave/w*/n16_records ~/n17_wave/w*/n17_records ...` の形、
`{z_dirs}` も同様（π に含まれない波だけ）。`{all_waves}` は両方の和。

### 7.2 検証（8 セッション・オーケストレータ用）

`docs/reports/2026-09-01_distributed_arena.md` の実行ブロックを正とする。可変部は
候補・基準のネット取得元、`{tag}`、seed 帯（§6 から払い出し）。各セッションは
`claude/arena-{tag}-w{NN}` に台帳と RESULT.json を push する。

### 7.3 生成（8 セッション・オーケストレータ用）

**生成の指示書もコーディネータが書く**（ユーザ決定 2026-09-05・「生成側の運用に任せるのはやめる」＝
2026-09-02 の決定を置き換え）。指示書には生成役ネットの所在・波番号・シャード・seed 帯・局数を
全て書く（**`--dump-v2` はもう要らない**＝下記 dump v3）。完了は生成側から共有され、コーディネータは §4 の手順で回収する。

**生成の出力ブランチの基点（ユーザ決定 2026-09-12）**: `claude/n{W}-w{NN}` は**生成に使ったコードの
コミット（作業セッションが checkout していた本線）から分岐する**。`claude/n1-results` を基点に
**しない**——n1-results はネットと台帳の正本で npz を大量に抱えており、波 31／32 では基点にした
ために push 段階の fetch だけで 10 分以上かかった（この基点は規約ではなく波 31 の指示書の書き方が
波 32 に写っただけだった）。本線を基点にすると (a) 手元にあるので fetch が要らない (b) push は
棋譜の blob だけ (c) ブランチがどのエンジンのコミットで生成したかを指す＝出所の記録になる。
回収は `git fetch origin claude/n{W}-w{NN}:tmp && git archive tmp n_records | tar -x` で基点に依らない。
**訓練の出力（§7.1 の `claude/train-{name}`）は従来どおり n1-results を基点にする**（ネットを
`n1_results/` に積み上げる場所だから）。作業セッションへの指示は次の形:
```
git checkout -B claude/n{W}-w{NN}          # 今 checkout している本線のコミットから分岐（fetch 不要）
git add -f n_records/n{W}_w{NN} n{W}_gen.log RESULT.json
git -c user.email=g.x5gyqe2@gmail.com -c user.name=worker commit -m "gen wave {W} shard {NN}"
git push -u origin claude/n{W}-w{NN}
```

**シャードの細分化（2026-09-05〜）**: 壁時計を縮めるため 1 波を **16 シャード×480 局**（32 セッションで
2 波）にできる。seed は従来の帯を半分に割る＝シャード k（1..16）の base は
`2000000 + W*10000 + ceil(k/2)*1000 + (k が偶数なら 500)`・各 480 局。帯の払い出しは波単位のまま。
生成役 a1 は c10 より約 1.7 倍遅い（960 局 ≒ 7 時間）ので 480 局で約 3.5 時間。**ただしセッション数の上限は 16**（ユーザ決定 2026-09-05・トークン量）＝2 波を同時に作るなら 8 シャード×960 局のまま。壁時計を縮める余地は生成役のレイテンシ（状態 S の軽量化）側にある。

**NRel の訓練構成（ユーザ決定 2026-09-05）**: `opcg_sim.learned.train.n_rel_train --ablate rel`（関係 R なし・相手デッキ知識あり）が既定。系譜は r2, r3, … を続ける。

**dump v3（既定・2026-09-07〜）**: 生成コマンドに旗は要らない（`opcg_sim.loop.record_gen` が
常に v3 で書く。`--dump-v2` を付けても no-op＝古い指示書のコピーは壊れない）。符号化は v2 と同じ
v13（トークン状態＋候補の枠 index）で、**保存 dtype だけ半分**（tokens/scalars が float16・
card_idx が int16・meta の `dump_version`=3）。学習側は波ごとに memmap の pack を作って読むので
**RAM に載らない**（2 波 218,502 行の常駐が 0.524GB → 0.035GB・`docs/reports/2026-09-07_dump_f16.md`）。
**波 23〜26 までの v2 の npz もそのまま読める**（`dump_io.load_dump` が cast して pack する）。
旧形式（v1）の波は NRel の教材にならない。

### 7.4 1 サイクル指示書（オーケストレータ 1 本・ユーザ要望 2026-09-05）

生成→訓練→アリーナを **1 本のオーケストレータ指示書**で回す（セッション上限 16）:
- **生成**: オーケストレータが 16 セッションを起こし、各シャードの指示（§7.3 の内容をフルで書く）を渡す。
- **訓練**: 生成ブランチが揃い次第、**オーケストレータ自身**が回収して訓練する（3 時間級のジョブは
  オーケストレータのコンテナで完走できる）。五月雨（シャードが届いた順に学習）は**やらない**——
  全シャードは同じ局数で同時に走るので到着時刻がほぼ同じで得が無く、シャード順の学習は
  データの混ざり方（全体シャッフル）を変えて訓練手順の同一性を崩す。
- **アリーナ**: 生成に使った 16 セッションへ追加指示（poke）で 1 本ずつ配る（条件×シャード）。
- **訓練の置き場所（2026-09-06 訂正）**: オーケストレータのコンテナは 15〜25 分ごとに再起動し 3 時間の
  訓練を完走できなかった（r2 で実測）。訓練は生成ワーカーの 1 本（最後のシャードを担当したセッション）に
  poke で渡す＝生成→訓練→アリーナを同じ 16 セッションで回す。
- 判定・台帳・`n1-results` への取り込みはコーディネータ（本セッション）が行う＝オーケストレータは
  `opcg_sim.loop.arena_merge` の出力を報告するだけ。

## 8. コーディネータが守ること

- 指示書を出したら、**その指示書の内容（入力・帯・判断基準）を本書 §6 と
  `docs/reports/` に先に記録する**（巻き戻りで指示内容を失わないため）。
- 回収は必ず origin から fetch して行う（ローカルの残骸を信用しない）。
- 判定は `opcg_sim.loop.arena_merge` の出力をそのまま使う（手計算で置き換えない）。
- 訓練・検証の結果がどうであれ、ネットと台帳は `claude/n1-results` に残す（不採用でも消さない）。

## 9. 分岐点シナリオ（人間のプレイと比べる・`docs/rust_engine_plan.md` §20.1）

§19 の一致率とは別に、**定性的に**「除去やコンボを CPU が考えられているか」を見る道具。
旧計器（`legacy/` の `human_replay_divergence.py`／`coach_gate.py`）は使わない。**Rust の
`Game`（`from_hidden`＋`decide` の trace）だけで新規に作った**（API は無い・CLI のみ）。
判定は人・**分析（盤面の要約・人間の方針との比較）はコーディネータ（Claude）の役割**。

### シナリオの書き方

1. `tests/scripts/rs_scenario_play.py list <replay.json.gz>` でリプレイを下見する（ターンごとの
   `action_index` 範囲・手番・その間の手の記述子が出る）。除去やコンボが見どころのターンを選ぶ。
2. `tests/scripts/rs_scenario_play.py add --replay <file> --seat p1 --start-turn N --end-turn M
   --title "…" --note-file note.md [--tags …] [--name …]` で
   `tests/fixtures/scenarios/<name>.json` を書く。`note` は「人間は何を優先したか」の自由記述
   （後から編集してよい）。名前省略時は `<リプレイのディレクトリ名>_t<start>-<end>`。
3. `source` はリプレイ JSON（ビューアで取得した `/replay/frames` の形・`.json.gz` 可）への
   `tests/fixtures/` からの相対パス。**開始は必ずターンの始め**（`turn_start_index` が探す
   `MAIN_ACTION` 待ちの最初のフレーム）・終了はそれ以降のターンの `TURN_END`。

### 回し方

```bash
python tests/scripts/rs_scenario_play.py play --scenario <name|all> \
  --net opcg_sim/data/learned/nrel_r3.npz  \  # 候補（省略時は出荷既定）
  --opp-net opcg_sim/data/learned/nrel_a1.npz \  # 相手席（省略時は --net と同じ）
  --seeds 3 --sims 160 --out scenario_out
```

分岐点を復元し、両席を指定ネットで（候補側・相手側は別々に選べる）`end_turn` の `TURN_END`
（か決着）まで実際に打つ。`--seeds` の違いは、山札の未見部分の並び（`hidden_build` が seed で
shuffle する）と探索乱数（`search_seed`）の両方に効く。出力は seed ごとに 2 つ:

- `out/<name>/<net>_s<seed>.frames.json` … `services/replay.py` と同じ形（`success`／
  `replay`／`decisions`／`frames`）。**リプレイビューアの「ファイルを開く」でそのまま読める**。
- `out/<name>/<net>_s<seed>.md` … **Claude が分析するための完全な事実の記録**（要約や合否は
  書かない）。冒頭にシナリオの `note` と「同じ範囲で人間が打った手」。続いて CPU の各決定について
  省略なく: (1) 盤面の全情報（両席） (2) pending (3) 合法手の一覧 (4) 探索の候補上位5と選んだ手
  (5) 適用後のイベント列。末尾に登場した全カードの効果本文（付録）。

### 出力の読み方

`.md` は 1 シナリオ 2 ターンで数百〜2,000 行になる。読む側（Claude）は「同じ方針か・除去や
コンボを見つけているか・何が違うか」を `docs/reports/` に分析として書き、ユーザが判定する。
`decisions` には人間の手との一致（同じ `action_type`＋card）を将来の数値化に備えて 1 列だけ
持たせる想定だが、集計はしない。

### RESULT.json

作業セッションが `play` を回して出力を作った場合は `docs/n_loop_ops.md` §5 と同じ要領で
出力ブランチ直下に置く: `{"job": "rs-scenario", "status": "done",
"cli": "tests/scripts/rs_scenario_play.py", "scenarios": ["<name>", ...],
"outputs": "scenario_out/", "unrestorable_rules": "…（`frame_to_hidden` が復元できない条件の要約）"}`。

確認は `make test`（`tests/test_scenario_play.py` が復元の往復と `play` の出力契約を見る）。
