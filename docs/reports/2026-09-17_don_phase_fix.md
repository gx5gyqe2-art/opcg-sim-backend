# T71——計画のドンの枠を「在庫 + 1/ターン」から規則どおり「在庫 + 2/ターン」に直した（ユーザ指摘）。引いた札 1 枚の価値は 0.0581 → 0.0605〔実・1.10 μ〕・0.0516 → 0.0530〔合成〕、次のターンに出せる札が無い手札は 8.2% → 5.1%〔実〕・13.1% → 9.6%〔合成〕。価格・橋は不動

2026-09-17 実施（ユーザ指摘「ドンの数は自ターンが来るたびに +2」）。T66〜T70 の計画 `H` はドン!!フェイズを 1 枚で計算していた（規則は 2 枚）。
**回帰しない・当てはめない**（規則の値に直しただけ・新定数ゼロ）。本報告は特定時点のスナップショット。器は `hand_plan.DON_PER_TURN = 2`／`caps_of`。
他の切替は T70 のまま（`PLAY_NOW_MODE=hand`・`INFLOW_MODE=on`・`SEARCH_PRICE_MODE=plan`・`HAND_MEAS_MODE=quality`）。

## 結論を 3 行で

1. `cap_t = min(10, 在庫 + 2t)`（t = 1, 2）。今のターンの枠（`cap_0`）と「それより後」の枠（`10 × max(1, R − 3)`）は変わらない。
2. **計画に入る札が増えた**: 引いた札 1 枚の `max(ΔH, ΔG)` は実 0.0581 → **0.0605**（μ の 1.10 倍）・合成 0.0516 → **0.0530**（0.96）。
   `ΔH = 0` の引きは 21.4% → 18.7%／32.8% → 31.3%・次のターンに出せる札が無い手札は 8.2% → **5.1%**／13.1% → **9.6%**。
   探した札と引きの差は −0.007 → −0.006（実）・−0.003 → **−0.001**（合成）。
3. **価格と橋はほぼ不動**: 登場の `LOOK` 価格／実現 0.95 → 0.95〔実〕・0.98 → 0.99〔合成〕・`PLAY_CARD` 0.56／0.87 不動・
   橋 `ΔG` 0.918 → 0.917／0.884 → 0.885・`ΔS` 0.309／0.375 不動。ゲート green。

## 数字（`+1` → `+2`・実／合成）

| | 引いた札 `ΔH_play` | 引いた札 `max(ΔH, ΔG)` | 探した札 | 差 | 穴の手札 | `LOOK` 比 | `DRAW` 比 |
|---|---|---|---|---|---|---|---|
| 実 | 0.0450 → 0.0474 | 0.0581 → **0.0605** | 0.051 → 0.054 | −0.007 → −0.006 | 0.082 → 0.051 | 0.954 → 0.948 | 1.08 → 1.10 |
| 合成 | 0.0320 → 0.0334 | 0.0516 → **0.0530** | 0.048 → 0.052 | −0.003 → −0.001 | 0.131 → 0.096 | 0.981 → 0.987 | 1.07 → 1.08 |

## 読み方

- ドンが 2 枚ずつ増えるので次のターン・その次のターンに出せる札が増え、計画に入る札（`ΔH > 0`）が増えた。μ との検算は実で 1.10 倍
  ＝T66 以降の「引いた札 ≈ μ」の整合は保たれる（合わせ込みは無い）。
- 価格は今のターンの枠（`cap_0`）で決まる部分が大きいので動かない。橋も動かない。

## 限界

- 先攻 1 ターン目の 1 枚（規則の例外）は見ていない（記録の窓は 2 ターン目以降が大半）。
- **観察であって因果ではない**。
