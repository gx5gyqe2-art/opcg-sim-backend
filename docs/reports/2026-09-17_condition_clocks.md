# T73／T74——条件の時計（t ターン後の状態で条件付きの札を読む）と、【ドン!!×N】を「付ける選択」として価格に。探した札の価値 0.047 → 0.050〔実〕・引きとの差 −0.012 → −0.009、価格・橋は不動。【ドン!!×N】は登場時能力に無く、今の計器では価格が動かない

2026-09-17 実施（ユーザ決定「両方やりましょうか」）。**回帰しない・当てはめない**（時計は規則と理論の量だけ・**新定数ゼロ**）。本報告は特定時点の
スナップショット。器は `hand_plan.COND_CLOCK_MODE`／`project_state`／`counters_cut`／`opp_life_loss_per_turn`／`has_on_play_condition`・
`condition_value.has_don_requirement`・`effect_value._don_attach_cost`。他の切替は T72 のまま。

## 結論を 5 行で

1. **条件の時計（T73）**: 登場時能力に条件が在る札の `v` を、t ターン後へ進めた状態で読んだ並び `v_t` にした（T70 の相方の到着と同じ形・積）。
   時計はドン +2/ターン（上限 10・ターン開始は全部アクティブ）・自分のライフ −受ける本数・相手のライフ −受ける規則が受けろと言う本数・
   トラッシュ +（カウンターで切る枚数 ＋ KO される体 ＋ 使うイベント）・ターン +2t。場の数・手札枚数はそのまま（限界）。
2. **効いた先は探した札**: 探した札の `max(ΔH, ΔG)` 0.047 → **0.050**〔実〕（引きとの差 −0.012 → −0.009）・引いた札は 0.0585 → 0.0580〔1.05 μ〕・
   合成は 0.051 → 0.050／0.0525 → 0.0511。**価格・橋は不動**（`PLAY_CARD` 0.67／0.88・`LOOK` 0.96／1.02・`ΔG` 0.920／0.884）。
3. **【ドン!!×N】（T74）**: 付ける選択として `max(0, 効果 − N × δ/R)`（`δ_A = δ/R`・§0.1・T57 の実測と同じ形）。付けられなければ（アクティブ < N）0・
   候補に出た起動メイン（付いている前提）は費用なし。**ただし【ドン!!×N】の能力は登場時に 1 つも無く**（アタック時 98・常時 53・自分のターン中 23…）、
   今の計器（登場時・起動メインの価格）では数字が動かない＝規則として置いた。
4. 時計を進めた状態でも判定できない条件は実 0・合成 574／33 万（0.2%）。
5. ゲート green（Rust 429・pytest 1,153）。

## 1. 器

```
project_state(st, t): ドン total/active/(active+rest) = min(10, total + 2t)（自分・相手）
                      my_life  = max(0, life − expected_taken × t)        opp_life = max(0, life − #{自分の攻撃 x: c(x) > Θ} × t)
                      my_trash += (counters_cut + ko_p × 場の数 + 使えるイベント ÷ 計画の長さ) × t     turn += 2t
v_t(条件付きの札)   = use_value(札, 状態 = project_state(判断点の状態, t))        相方待ちの札は base_t／gain_t も同じ状態で（T70 との積）
【ドン!!×N】        = max(0, 効果 − N × δ / R) × [アクティブなドン ≥ N]         offered なら費用なし・状態なしなら費用なし（上限）
```

## 2. 実測（T72 → T73・実 300 局／合成 900 局）

| | 引いた札 `max(ΔH, ΔG)` | 探した札 | 差 | `LOOK` 価格／実現 | `PLAY_CARD` | 橋 `ΔG`／`ΔS` |
|---|---|---|---|---|---|---|
| 実 | 0.0585 → 0.0580 | 0.047 → **0.050** | −0.012 → **−0.009** | 0.96 → 0.96 | 0.67（不動） | 0.920／0.316 → 0.920／0.315 |
| 合成 | 0.0525 → 0.0511 | 0.051 → 0.050 | −0.001 → −0.001 | 0.99 → 1.02 | 0.88（不動） | 0.885／0.371 → 0.884／0.374 |

条件の判定の数（登場の行・計画の側を含む）: 実 15,630 → 102,218（時計を進めた状態でも 4 倍読む・判らない 0）・合成 39,710 → 334,552（判らない 574）。

## 3. 読み方

- **探した札が上がった**のは、条件付きの札（今は条件が偽でも数ターン後に真になる札）を探して取った行の価値が 0 でなくなったため。引いた札は
  微減（計画の側で条件が偽に読まれる札が増えた分・T72 と同じ向き）。μ との整合（1.05）は保たれる。
- **価格が動かない**のは正しい: 今出す価格は今の状態で決まる（効果はその場で解決する）。時計は「手札に残す価値」（計画 `H`）にだけ入る。
- 【ドン!!×N】は登場時能力に無いので、価格に効くのはアタック時・常時の能力を価格に載せたとき（今は載せていない＝限界）。

## 4. 次（ユーザ判定）

1. アタック時・常時の能力（【ドン!!×N】の 174 本を含む）を攻撃の価格に載せる（`ν` の攻撃項の拡張）。
2. 時計の残り: 場の数（計画から）・手札枚数（入る枚数 − 出す枚数）。
3. X2（CPU が任意効果を使わない）の切り分け。

## 限界

- 時計は決定的（期待値をそのまま引く）＝分布は見ない。相手のライフの時計は自分の攻撃手が毎ターン同じと置く。
- 合成で 574 件（0.2%）が判らないまま（進めた状態で欠ける鍵が在る条件・未調査）。
- **観察であって因果ではない**。
