# T154: σ の表を新しい既定で作り直す——較正が両記録で初めてコインに勝つ（2026-09-24）

T151-6（相手の時計の鮮度・手番の半ターン）と T152（歩きの第 1 段）で τ の偏りは 1.6 → 0.3／0.5 ターンまで縮んだが、
勝率の読み `W(D)` の幅（`σ_rel`）は **2026-09-20 の既定で作った固定表のまま**だったため、較正（対数損失）は
0.649 → 0.806（実）・0.745 → 0.945（合成）と**悪化して見えていた**（T152 §0）。本 T はその表（`tests/fixtures/
harm_profile.json` の τ に依る 4 表）を新しい既定の上で取り直し、較正を読み直す（ユーザ判断 5・T151/T152 の
報告で「同じ単位で行う」と予告していた作業）。手順は 2026-09-20 §5（1 パス・cross 規約）＋ `w̄` は σ_T を書き換えた
後の 2 段目。**新定数ゼロ**。

**記録**: 実 = `w41`（300 局）／合成 = `w39 + w42`（900 局）。既定 = `mirror`＋`half`＋`game`・`blockers`。

---

## 0. 結論（先に書く）

**予告 7 本のうち 5 本当たり・2 本外れ**（§3）。当たった中でいちばん大きいのは P1:

| 較正（決着前の行・theory 読み・rel） | T151 採用時（旧表） | T152 後（旧表のまま） | **T154（新表）** | コイン |
|---|---|---|---|---|
| 対数損失・実 | 0.684 | 0.812 | **0.570** | 0.692 |
| 対数損失・合成 | 0.778 | 0.946 | **0.682** | 0.693 |
| ECE・実／合成 | 0.121／0.141 | 0.148／0.166 | **0.059／0.084** | — |
| AUC・実／合成 | 0.792／0.715 | 0.792／0.717 | 0.792／0.717（不変） | — |
| 優勢側の gap／劣勢側の gap・実 | +0.116／−0.133 | +0.109／−0.176 | **+0.037／−0.074** | — |
| 同・合成 | +0.146／−0.133 | +0.142／−0.186 | **+0.081／−0.087** | — |

**合成の記録で対数損失がコインに勝ったのは初めて**（T118 以来ずっと負けていた）。実は歴代最良（0.570）。
順位づけ（AUC）は 1 ミリも動いていない＝**幅だけを直した**。T152 で「悪化して見えた」のは予告どおり表の古さだった。

**途中で器の欠陥を 1 つ直した**（§2）: 新しい既定では両席の τ がともに 0 の行（実 4／合成 36 行）が現れ、
`σ_rel` の計算がそれを床 1e-9 で割って **1e7 級に壊れていた**。相対残差は 0/0 で定義できないので母数から外す
（`n_scale0` に数を出す）。旧既定ではそういう行が無かったので露見していなかった。

**取り直した表（`blockers`・旧 → 新）**:

| 量 | 実 | 合成 |
|---|---|---|
| `σ_T`（curve の残差） | 1.099 → **0.994** | 1.263 → **1.154** |
| `σ_rel`（theory） | 0.1688 → **0.3078** | 0.1687 → **0.3301** |
| `σ_rel`（curve） | 0.2237 → **0.2456** | 0.2418 → **0.2693** |
| `w̄` | 0.1782 → **0.1894** | 0.2098 → **0.2238** |
| 輪郭（`real`／`syn`）・理論の速さ（`theory_slope`） | ビット一致 | ビット一致 |

`σ_rel`（theory）が 1.8 倍になったのが較正を戻した本体——旧表の幅は「相手の時計が 1 段古い」「第 1 段が 0」の
2 つの偏りを含んだ τ の上で測られていて、偏りを直したら残差の**相対**の幅が広がった（絶対の幅 σ_T は 9% 縮んでいる）。

## 1. 問い

T152 の較正の悪化は「σ の表が古い」だけで説明がつくか。表を新しい既定で作り直せば、実の対数損失は再びコインを
下回るか（合成はどこまで戻るか）。

## 2. 器（判定は変えない・新定数ゼロ）

* `crossing_bridge.summarise`: `σ_rel` の母数から尺度 0 の行を外し `n_scale0` を出す（テスト 1 本）。
* fixture の 4 表を取り直し（手順は §8）。`_note_*` に旧値と経緯を残した。

## 3. 予告と当否（`t154_predictions.md` に測る前に書いた）

| # | 予告 | 結果（実／合成） | 当否 |
|---|---|---|---|
| P1 | 実の対数損失が再びコインを下回る（殺す基準: 上回ったまま）・合成は 0.778 付近へ | **0.570／0.682**（両方コインを下回る） | **当たり（予告以上）** |
| P2 | σ_T（curve）は ±10% | −9.6%／−8.6% | **当たり** |
| P3 | 輪郭はビット一致 | 一致 | **当たり** |
| P4 | 理論の速さの表は不変 | 一致 | **当たり** |
| P5 | 交点の橋の的中・偏り・within1・MAE は表の書き換え前後でビット一致 | 一致（`win_by_D` も） | **当たり** |
| P6 | `w̄` 書き換え後の κ の平均は 1±0.1 | 0.846／1.182 | **外れ**——cross 規約の構造的な帰結（2026-09-20 と同じ 0.850／1.178）。予告を書くとき前例を確かめていなかった |
| P7 | 決着前の行だけの σ_rel は全行と ±2% | **−22%／−19%**（0.2408／0.2660） | **外れ**——新既定では決着後の行が相対残差を膨らませる |

P7 が外れたので、**決着前の σ で読む腕も測った**（T146a の再測）: 対数損失 0.610／0.754・ECE 0.090／0.118・
gap +0.069／−0.105・+0.115／−0.122——**全行の σ（表の値）の方が全部良い**。決着後の行が σ を「広げる」向きに
効いていて、それが決着前の行の自信過剰をちょうど和らげている。**表は規約どおり全行の値を置く**（T146a の結論
「幅の問題ではない」は旧既定の話で、新既定では幅が効く——ただし決着前だけに絞るのは逆効果）。

## 4. 表

**線形の橋**（`theory_bridge`・cross の新 σ_T で・`w̄` は書き換え前＝AUC／相関には効かない）:

| | 実 | 合成 |
|---|---|---|
| `dG` AUC／相関 | 0.653／0.255（T151-6 時 0.660／0.267） | 0.700／0.310（0.706／—） |
| `dS` AUC | 0.601（0.595） | 0.468（0.459） |
| `dS_atk` AUC | 0.625（0.620） | 0.472（—） |
| κ の平均（旧 `w̄` で） | 0.903 | 1.256 |

σ_T が 9% 縮んだぶん κ が接戦帯を少し鋭く重み付けし、`dG` は 0.007 落ち・`dS` は 0.007 上がった——**どちらも
誤差の範囲**（線形の橋は幅の表をほとんど感じない）。

**`abs` の読み**（比較）: 対数損失 0.686／0.747——`rel` の 0.570／0.682 より悪い＝T118 の判断は新既定でも保つ。

## 5. 不変量

* `opcg_sim/`・`rust/` は不変。fixture の書き換えは τ・的中・偏りに影響しない（P5・実測で確認）。
* fixture を読むテスト 158 本は書き換え後も green（値は関数経由で引いており固定値のラチェットは無い）。
* AUC は `abs`／`rel`／σ の値に依らず 0.792／0.717（幅は順位を変えない）。

## 6. 上手くいったこと／いかなかったこと

**上手くいった**: T151／T152 の「較正は表を作り直すまで読まない」という留保が正しかったことを数字で閉じた。
合成の記録がコインに勝ったのは T118 以来の目標だった。器の欠陥（尺度 0）を予告の前に見つけられた
（最初の計測で σ_rel が 3e7 と出た時点で原因を特定・修正・再計測）。

**いかなかった**: P6 は前例（2026-09-20 §7「κ の平均が 1 に載らない」）を読まずに書いた予告——書く前に台帳を
引くべきだった。P7 は T146a の結論をそのまま持ち込んだが、前提（旧既定）が変わっていた。

## 7. ユーザ判断をお願いしたいこと

なし——表の取り直しは規約（T97／T98／T118）どおりで、既定の切替は無い。次は T18（判断 1 の暫定方針で回す）。

## 8. 再現

```bash
S=<records>
python tests/scripts/crossing_bridge.py --in $S/w41 --out real.json                 # by_slope.*.sigma_T / sigma_rel / n_scale0
python tests/scripts/crossing_bridge.py --in $S/w39 $S/w42 --out syn.json
# fixture に σ_T（curve）・σ_rel（theory／curve）を写す → 2 段目:
python tests/scripts/theory_bridge.py --in $S/w41 --out tb_real.json                # stats.w_mean → w_bar
python tests/scripts/theory_bridge.py --in $S/w39 $S/w42 --out tb_syn.json
python tests/scripts/win_calib.py --in $S/w41 --pre-settle on --json wc_real.json   # rel.logloss / ece / auc
python tests/scripts/pre_settle_asymmetry.py --in $S/w41 --json pa_real.json        # favorite/underdog gap
```
