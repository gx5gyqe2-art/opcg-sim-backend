# bb7 — v11 定義確定ラウンド（被覆86%）＋セッション並列運用の初回実戦（2026-08-14）

bb6b（`backbone_bb6b_v11fix_20260814.md`）の続き。被覆監査で残った実質的な欠落
（DISCARD=純手札経済・REST/ACTIVE=擬似除去/テンポ）を配線し（71%→**86%**・残り14%は
LOOK/REVEAL 等の情報行動と GRANT_KEYWORD 等の v2 送り）、bb3 と同一シードで再判定。

## 1. 判定 — bb6b と同水準＝v11 定義を被覆86%版で確定

| 物差し | bb3(v10) | bb6b | **bb7(86%)** |
|---|---|---|---|
| 実L接戦帯 全帯106 | +0.490 | +0.653 | +0.631 |
| 実L接戦帯 中間帯49 | +0.117 | +0.249 | **+0.244** |
| 　ハンニャバル n=12 | +0.443 | +0.619 | +0.667 |
| 　ナミ:シャンクス n=31 | +0.098 | +0.311 | +0.312 |
| 　エネル n=6 | −0.696 | −0.682 | −0.676 |
| 域内 val MSE | 0.708 | 0.675 | 0.664 |

- 追加配線は転移を変えず（±0.02＝雑音圏）意味論を正しくした（例: ナミの draw_rate は
  +0.7 →**純手札経済 −0.3**＝パンプコストの捨て込み。錨テストは非ゼロ条件に更新）。
- 決め手は bb6b の BUFF/防御トリガー修正で、**現コーパス規模（400局）の上限
  ≈0.24-0.25 に到達**。次の伸び代はコーパス規模・trait 継承・条件ゲート・G系適用。

## 2. セッション並列運用の初回実戦（`docs/session_orchestration.md` 準拠）

- 親→`create_session` 直接発行は **-32003（承認）で不成立**（ドキュメント §2 の既知問題。
  本セッションの承認ポップアップはユーザ側に表示されず）。**ユーザが別途オーケストレータ
  セッションを立て、そこへ起動指示プロンプトを渡す方式**で解決＝今後の運用形。
- 子1（テストゲート係）: `make test` **green**（1611想定・661秒・HEAD 6ceef7a）を
  `claude/test-report-bb7gate` へ納品。**親はゲートの10分間ブロックされなかった**。
- 子2（bb7 生成係）: 400局・8048行を `claude/bb7-corpus` へ納品。親のスモーク＋
  プロンプト設計どおり一発完走。
- **教訓（ロールバック耐性）**: 判定中に親コンテナがロールバック（3回目）し、ローカルの
  v11 コード・評価行列・bb6b ネットが消えたが、**push 済みブランチと子の成果ブランチから
  全て復旧**できた。成果を即 push する規約と、子の成果が git にあることが保険として機能。

## 3. 次の選択肢（優先順）

1. **G系適用（gen15 スパイク）**: G14 の訓練を v11 で再実行（埋め込み＋リーダー物理の併用）。
   接戦帯 0.455 の頭がどこまで上がるかが本丸。コーパス再生成はセッション並列の本命ユース。
2. コーパス規模（bb 400→1200局・子3件並列）＝B系の 0.244 の頭打ちを規模で押す。
3. trait 継承＋条件ゲート（合成世界の発火不能32%）。
4. エネル帯（特徴で不動＝別戦線・新規対局の帯増強から）。

実費: 配線＋監査＋錨更新 ~40分・子2件（並列・ゲート11分/生成30分）・復旧＋判定 ~30分。
