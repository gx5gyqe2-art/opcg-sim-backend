# 配備案「r4 ＋ worlds 4 ＋ R2」vs 既定「r3 ＋ 既定設定」の判定（2026-09-10・非採用）

候補席だけ探索設定を上書き（`--cand-worlds 4 --cand-select-rule q_min_n --cand-q-min-frac 0.125 --cand-root-prior-temp 2.0`）。
基準席は r3 ＋ 既定（worlds 1・visits・t=1）。

| 条件 | 局 | 勝率 | 95% CI | Elo | void | 規約 |
|---|---|---|---|---|---|---|
| 主（random×synth・帯 481000〜488000）| 384 | 0.4818 | [0.4242, 0.5394] | −12.7 | 0 | 未達 |
| 副（固定ミラー・帯 491000〜498000）| 384 | 0.4609 | [0.4170, 0.5049] | −27.2 | 0 | **退行シグナル**（下限 <0.45）|

台帳 `claude/arena-r4w4-random`／`claude/arena-r4w4-mirror` の `n1_results/arena_r4w4/`。

## 読み
- 「教師どおりの物差し（worlds 4 ＋ R2）で打たせれば r4 が伸びる」は外れ。r3 に同じ設定を載せた §20.5.3-1（主 0.531・
  副 0.544）より低い＝**r4 は探索設定に関わらず r3 を上回らず、w4R2 と組むとミラーで退行**。
- `r4_validity_20260910.md` と合わせて、r4 の評価帯の改善（新分布 in-distribution）は打ち方の強さに乗っていない。
- 運用の注記（作業セッション）: 素の nohup はコンテナ再起動で 2 回落ちた。`arena_shard` は台帳追記式なので同じ
  `--out` で再開できた。以後の長い実行は Bash の run_in_background で起動する（指示書に反映済み）。
