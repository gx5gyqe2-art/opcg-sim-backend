# 既定の切替 4 本を採用（ユーザ決定 2026-09-19「1は規定、2は正しいものに直してください」）。**数字で採る 1 本**（`SLOPE_EFFECT_MODE=hand`・T108）と**規則の正しさで採る 3 本**（`RATE_T1_MODE`・`RATE_RUSH_MODE`・`THETA_HAND_BLOCKER_MODE`）。**`Θ` が変わったので `σ_T` と `w̄` を測り直した**（σ_T 1.093/1.247 → **1.091/1.259**・w̄ 0.1823/0.2104 → **0.1811/0.2107**）。**新しい定点**: 交点の橋 勝者の的中 **0.6628（実）/0.6421（合成）**・終局の偏り **+2.39/+2.44**、帳簿は**ほぼ不動**（`ΔG` AUC 0.6942 → **0.6949**）

2026-09-19 実施（ユーザ決定「1は規定、2は正しいものに直してください」）。
**当てはめない・新定数ゼロ**。本報告は特定時点のスナップショット。

## 1. 動かした 4 本

| 切替 | 旧 | 新 | 採る理由 | T |
|---|---|---|---|---|
| `SLOPE_EFFECT_MODE` | `off` | **`hand`** | **数字**——`off` → `on` → `hand` と単調に良くなり、両記録・的中と偏りの両方で改善・帳簿は構造上不変（費用ゼロ） | T105／T108 |
| `RATE_T1_MODE` | `off` | **`on`** | **規則**——どちらの席も自分の最初のターンはアタックできない（`battle.rs` の `turn_count <= 2`）。**実測が桁で裏づけ**（1 自席ターン目の実際の損害 0.001／0.000 対 理論 0.055／0.054＝**79 倍／134 倍**） | T103 |
| `RATE_RUSH_MODE` | `off` | **`on`** | **規則**——速攻は出したターンからもう殴れる。**帳簿側は T84 が既に例外にしており、歩きだけが持っていなかった** | T103 |
| `THETA_HAND_BLOCKER_MODE` | `off` | **`on`** | **規則**——ブロックに召喚酔いは無い（`has_blocker` は登場ターンを見ない）＝**手札のブロッカーは避けて通れない体の予備** | T106 |

**1 本目は数字で、3 本は正しさで採った**——3 本は**それぞれ単独では数字を下げる**（T103・T106 で測って報告済み）。
**量はどれも小さい**（速攻はデッキの 1.55%／0.33%・出せるブロッカーが手札に在る行は 3%／11%）。

## 2. 測り直した 2 つの借り物（**`Θ` が変わったから**）

`σ_T` と `w̄` は **fixture の写し**で、T97／T98 の規約に「**既定やエンジンを変えたら測り直す**」と書いてある。
**`THETA_HAND_BLOCKER_MODE=on` は `Θ` の体の項を増やす**ので、両方を測り直した。

| | 旧 | 新 |
|---|---|---|
| `σ_T`（`blockers`・実／合成） | 1.093／1.247 | **1.091／1.259** |
| `w̄`（`blockers`・実／合成） | 0.1823／0.2104 | **0.1811／0.2107** |

**どちらも 1% 前後の動き**（`Θ` の増分が小さいので当然）。**規約どおり別のセットの値を使う**（`cross`）。

## 3. 新しい定点（以後この数字と比べる）

### 交点の橋（実 300 局／合成 900 局）

| | 勝者の的中 | 終局の偏り | σ_T | `curve` 的中 | `curve` 偏り |
|---|---|---|---|---|---|
| 旧既定（T104 時点） | 0.6541 | +2.53 | 4.012 | 0.5861 | +0.14 |
| T108 単独（効果だけ） | **0.6642** | **+2.20** | 3.872 | 0.5861 | +0.14 |
| **新既定（4 本）実** | **0.6628** | +2.39 | 4.145 | 0.5839 | +0.17 |
| 合成 旧既定 | 0.6449 | +2.56 | 3.951 | 0.6077 | +0.41 |
| 合成 T108 単独 | **0.6469** | **+2.23** | 3.818 | 0.6077 | +0.41 |
| **新既定（4 本）合成** | **0.6421** | +2.44 | 4.169 | 0.5996 | +0.46 |

**正しさで採った 3 本の代金がここに出ている**——T108 単独より**的中 −0.0014／−0.0048・偏り +0.19／+0.21**。
**旧既定よりは的中 +0.0087／−0.0028**。

### 線形の橋（帳簿）

| | `κ` の平均 | `ΔG` AUC | 傾き | 必要な `κ` | 接戦帯の `κ` | 相関 |
|---|---|---|---|---|---|---|
| 旧既定 実 | 0.866 | 0.6942 | 0.458 | 0.501 | 0.559 | 0.386 |
| **新既定 実** | 0.859 | **0.6949** | **0.463** | 0.498 | 0.555 | 0.387 |
| 旧既定 合成 | 1.154 | 0.7302 | 0.444 | 0.581 | 0.627 | 0.501 |
| **新既定 合成** | 1.163 | 0.7301 | 0.441 | 0.582 | 0.628 | 0.501 |

**帳簿はほぼ不動**——**理由は構造**: 帳簿（`theory_bridge`）は**速さ `A` を 1 度も呼ばず**、
`Θ` も `curve_d_of_row` 経由で読むので**手札のブロッカーが入らない**（1 行からは相手の手札が見えない）。
動いたぶんは `σ_T` の 1.093 → 1.091 だけ。

## 4. 残る非対称（記録として）

**`Θ` の手札のブロッカーは交点の橋にしか入っていない**。帳簿の `D` は `curve_d_of_row`（1 行）で作るので、
**相手の手札を見られない**（T75 以来の `g_hand_of_me`／`g_hand_of_opp` と同じ型の非対称）。
**完全情報で組む方針（§0.05）からすると、帳簿側も両席の行を組にして読むべき**——
**未着手の宿題として残す**。

## 5. ゲート

green（Rust 429・pytest **1,209**）。**既定の変更・fixture の測り直し・テストの手当てをまとめてゲート 1 回・push 1 回**
（ユーザ決定 2026-09-18「ゲートは時間がかかるので頻度を下げてください」）。

## 限界

- **`σ_T`・`w̄` は 2026-09-19 の構成で測った写し**。**次に既定やエンジンを変えたら測り直す**。
- **帳簿は手札のブロッカーを見ていない**（§4）＝**2 つの橋で `Θ` が完全に同じではない**。
- **3 本の正しさは規則から出るが、数字は下がる**。**代金は上の表に明示した**——
  「正しいものを既定にした結果、指標が少し下がっている」という状態を**隠さずに定点とする**。
- **観察であって因果ではない**。
