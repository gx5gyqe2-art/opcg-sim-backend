# v55 — ドン箱 Phase 1（攻撃箱）: 単独では中立・固有価値は P2（温存）へ（2026-08-12）

`docs/cpu_don_box_plan.md` Phase 1 の実装と A/B 判定。v44 規約（買った/買わないを記録）。

## 1. 実装（`a4149e7`・make test 1603 全green）

- DON_BOX マクロ候補（付与k→リーダー攻撃・k∈{通る最小, 2枚要求}・算術で高々2点/攻撃者）
- 探索内部のみ: 子盤面生成は原始列へ展開、実対局出力は先頭原始手 ATTACH_DON に変換
  ＝記録/再生/API の行動空間は不変（ステートレス実行・次 decide で計画を選び直す）
- 旧 policy 互換: 箱は「付与後マージンの ATTACK」として特徴化（新 action_type を語彙に
  足さない・素攻撃 prior 継承・マージン列のみ付与後値）
- seam: `don_box`（既定 OFF）・`arena_resume --cand-don-box`
- 単体5件（列挙算術/展開等価/原始変換/seam/キー分離）green

## 2. A/B 判定（基準＝(C) 採用済みの現行・同一 gen14）

> **200ペア（400局）: wr 0.522・95%CI [0.480, 0.565] ＝ 中立**（昇格基準未達）

推移: 39ペア 0.564 → 130ペア 0.527 → 最終 0.522（序盤の正リードが収れん）。

## 3. 読み — なぜ攻撃箱単独では出ないか

1. **(C) が既に同じ計画をチェーン経由で到達可能にしている**。v54 の +7% は「マージン攻撃が
   候補に存在するか」の穴埋めで、箱はその同じ計画の**探索表現の改善**（1 edge 化）に過ぎない。
   sims=160 ではチェーンでも十分掘れており、表現改善だけでは勝率に出ない。
2. 箱化の**固有価値**（チェーンでは原理的に表現できないもの）は「使わない」＝**温存 r** の
   候補化（`cpu_don_box_plan.md` §2.3・Phase 2）。v54 のエネル層別（20ペア無反転＝中立）が
   「エネル戦の勝敗は打点マージンでなく資源配分で決まる」ことを示しており、P2 の照準と一致。

## 4. 帰結

- **P1 単独は採用しない**（`don_box` 既定 OFF のまま）。実装・seam・テストは P2 の土台として
  維持（P2 は P1 の箱列挙に r 次元を足す差分実装）。
- 次: **Phase 2（温存 r）** → 一般ミラー＋**エネル対面**の二重 A/B（エネルが本命の試験場）。
- 実費: P1 実装〜A/B 完走 ~3.5時間。
