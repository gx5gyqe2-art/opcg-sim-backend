# r3 採用報告（2026-09-08）

出荷既定を **NRel a1 から NRel r3** へ（`opcg_sim/data/learned/nrel_r3.npz`・`opcg_sim/loop/engine.py::DEFAULT_NET`）。
生成役も r3＝**era7** を開始する。ロールバック先は a1（同梱のまま）。

## 根拠

- 判定 `docs/reports/2026-09-08_r3_judgment.md`: 主条件（ランダム対面×合成デッキ・192 ペア）**0.615 [0.560, 0.669]・
  Elo +81**＝N 系の系譜で最大。副条件（固定ミラー・192 ペア）0.510 [0.465, 0.556]＝互角（退行なし）。void 0/0。
- 規約「2 条件とも達成」に対しては副条件が未達だが、**ユーザ決定（2026-09-08）で「主条件達成＋副条件は退行なし
  （CI 下限 ≥0.45）」を採用条件として読み替えた**（CLAUDE.md に追記）。汎化を優先する開発判断（2026-08-25）と整合。
- 訓練: warm-start a1・`--ablate rel`・`--backend torch`・π＝波25〜28（28,140 局・a1 生成・Python エンジン時代）・
  z＝波23〜24・2 epoch（best ep0）・val v_mse 0.5288／v_sign 0.810／pi_top1 0.602。73 分。
- 構成は a1 と同じ（関係 R なし・相手デッキ知識あり・hidden 192）＝serve のレイテンシは a1 と同じ。

## 変更

| 項目 | 変更 |
|---|---|
| 出荷既定 | `DEFAULT_NET` → `nrel_r3.npz`（生成・アリーナ・serve が同じもの） |
| 同梱 | `nrel_r3.npz` を追加。`nrel_a1.npz` は残す（ロールバック先・判定に使ったネットは消さない） |
| 生成役 | r3（era7）。次の波（29〜）は Rust の `opcg_sim.loop.record_gen`（dump v3）で r3 が打つ |
| 訓練窓 | π＝era7 の波のみ・z＝新しい波から載るだけ（運用は CLAUDE.md「学習データ窓の運用」のとおり） |
| golden | 不変（再生 golden は記録された手を再生する＝ネットに依存しない。`--policy a1` 帯の名前は生成時の既定ネットの意） |
| 規約 | CLAUDE.md の昇格規約に「副条件は退行なしで可」を追記 |

## ロールバック

`DEFAULT_NET` を `nrel_a1.npz` に戻すだけ。
