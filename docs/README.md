# ドキュメント索引

`opcg-sim-backend` のドキュメントは、**文書の種別（ライフサイクル）** で分類する。
種別を混在させず、それぞれの更新ルールに従って維持する。

| 種別 | 役割 | 更新ルール |
|---|---|---|
| **仕様（正本）** | システムの「現在のあるべき姿」 | 実装変更に追従して**常に最新**に保つ |
| **報告（点）** | 特定時点の調査・検証スナップショット | 追記・改変しない（履歴として残す） |

> 計画書（plan）は実装完了後に正本（SPEC / TEST_SPEC）へ吸収し、文書としては残さない。
> 設計の経緯は git 履歴を参照する。

> **エンジンは Rust**（2026-09-07・第 2 段 `rs-archive-cutover` 完了）。ルール・効果・探索は
> `rust/opcg_engine`、Python が持つのは**パーサ・学習・API** だけ。旧 Python エンジンは
> `legacy/python_engine/`（tag `py-engine-final` で凍結・テストゲート対象外）。
> 経緯と受け入れは [`rust_engine_plan.md`](rust_engine_plan.md) §8。
> **文書中の `opcg_sim/src/core`・`opcg_sim/src/learned` の記述は、断りが無ければ
> 退避前（＝tag の中）の所在**として読む。

## 仕様（正本）— `docs/` 直下

| 文書 | 内容 |
|---|---|
| [`SPEC.md`](SPEC.md) | **システム仕様書**。全体アーキテクチャ／コアゲームルール（ターン・戦闘・召喚酔い/速攻・場5体上限）／オンライン対戦（ルーム・WS）／**CPU 対戦・AI**（§2.5）／カード効果システム／ファイルマップ／**ログ・可観測性**（§5.1）／既知のモデル化制約（§6.1） |
| [`TEST_SPEC.md`](TEST_SPEC.md) | **テスト仕様書**。テスト戦略／スイート一覧／診断・監査ツール／**効果検証ハーネス**（CPU 対 CPU 自己対戦・インバリアント検出, §3.1）／**CPU 思考トレース＋決定論リプレイ**（§3.2）／品質ゲート／デッキ単位の手動検証 |
| [`cpu_thinking_logic.md`](cpu_thinking_logic.md) | **CPU 思考ロジック詳細図**（**L1 の図＝2026-09-07 に廃止**。現在の CPU は N 系＋Rust の MCTS で、本書は tag `py-engine-final` の姿を残した歴史文書として読む）。決定パイプラインと暴走防止の責務分担を1枚で俯瞰 |
| [`LOGGING.md`](LOGGING.md) | **ログ仕様**。汎用ログ（`log_event`/GCS/Slack）は撤去済み。唯一のログ＝ CPU 思考トレース（ローカル自己対戦／実アプリ `/replay`）の正本 |
| [`parser_v2.md`](parser_v2.md) | カード効果パーサ（EffectParserV2）の設計・ルール一覧・既知のパース制約 |
| [`leader_specs/`](leader_specs/README.md) | 全137リーダーのカード個別仕様（テキスト／期待挙動／テストケース）。作成ガイド [`_GUIDE.md`](leader_specs/_GUIDE.md)、テスト方針 [`_TEST_GUIDE.md`](leader_specs/_TEST_GUIDE.md)、既知差異 [`ISSUES.md`](leader_specs/ISSUES.md) |
| [`test_worker_prompt.md`](test_worker_prompt.md) | **テスト実行ワーカー プロンプト**。品質ゲート（`make test`・約10分）を別セッション＝別コンテナに実行させ、レポートを枝経由で回収するための貼り付け用プロンプト。依存インストール手順を含む（コンテナにテスト依存は入っていない） |
| [`game_theory.md`](game_theory.md) | **ONE PIECE カードゲームの基本理論**（正本・2026-09-13）。**このゲーム自体の理論**——有限二人零和・不完全情報から何が導けるか／**受けるのはライフ 1 枚を手札 1 枚に両替する**という交換の恒等式／予算 `G`・`S` と**価格 `Θ = λ/μ − 1 − τ_value`**（実測 `λ/μ` 2.67 ⇒ **2000 までは守り 3000 からは受ける**）／**整数性＝「7000 に積む」は 1 枚で足りない高さへ跨ぐ段を買う行為**／しきい値は状態の関数（固定ライフは原理的に誤り）／**ドンは 2 段の決定で窓では埋没費用**／時間とハザード／**このゲームの実測値の表**。実装・CPU の話は書かない |
| [`cpu_theory_gap.md`](cpu_theory_gap.md) | **理論とうちの CPU の差分**（正本・2026-09-13）。層ごとの判定（列挙は律速でない／探索の PIMC は接戦帯だけ壊れる／**価値の符号は正しいが零和が破れている**／**方策の順序がコイン＝一次対象**／価格は使われていない／ドンを使い切れない）・**候補 F の設計要件**（渡す量は `c(x)`・`Θ`・`Δpressure` の 3 つ＋価格は方策の教師にも入れる）・CPU の挙動の数字の一覧・計器の対応表 |
| [`critique_index.md`](critique_index.md) | **ユーザ指摘の索引と状態**（正本・2026-09-13）。レポート／台帳／規約／仕様／リーダー個別差異／**ソースコードの注釈**まで一巡して洗い出した27 件を「量になったか・測ったか・実装したか・未着手か」で並べた表。**この洗い出しで覆った自分の結論 7 件**（超過パワー別の守りは合理だった／終盤は「どれを守るか」／board は終盤限定／「守れた」は DON を見ていない上限／V_guard−V_take は偏りの検査をしていない／ゲートの 2 条件はどちらも ±35 Elo／192 ペアの Elo はノイズ）と、**未測定の宿題 8 件の優先順**も載せる |
| [`life_budget.md`](life_budget.md) | **ライフと手札の交換の定式化**（正本・2026-09-13・ユーザ指摘「懸念を数式化して定量化できないか」）。**受ける＝ライフ 1 枚を手札 1 枚に変換する**という恒等式から予算 `S = H + (N−G) + T − K − Σc` を立て、**この窓の判断は `c(x) − c̄` の符号で決まる**（安い攻撃を守り、高い攻撃は受ける）ことを導く。**守りのしきい値は定数ではなく `θ = A·T − B`**（相手の攻撃回数 × 自分が決めるまでのターン数 − ブロッカー）＝デッキごとに変わる理由も式で出る。**過去の 2 つの結論を覆す**（「終盤はもっと守れ」は「どれを守るか」の問題・「超過パワーが上がるほど守らない」は合理だった） |
| [`measurement.md`](measurement.md) | **何を測っているのか（推定量の定式化）**（正本・2026-09-13）。「しない／できない」＝`Δ_obs = Δ_causal + Δ_selection` の分解と、消せるのは強制選択の A/B だけという帰結／「どの方針が勝ちやすいかを教える」＝**方針の後悔 `R = E[max_q V(s,q) − V(s,p_played)]`**（実測 0.033〜0.084）／「離散・連続は時系列の話」＝V は `P(T=t|s)` を潰している＝次はハザード／**判定の分解能 `N ≈ (680/Δ)²` 局**——現行ゲート 384 局は **±35 Elo** しか見えず、実測の世代差は 5 Elo 級＝**48 倍の局数が要る** |
| [`n_loop_ops.md`](n_loop_ops.md) | **N系学習ループの分散運用**（正本・2026-09-01）。教材収集／学習／検証を独立セッションで回し、コーディネータは指示書と回収・判定のみ。受け渡しはブランチ経由・`RESULT.json` 必須・seed 帯の台帳・役割別の指示書テンプレート |
| [`rust_engine_plan.md`](rust_engine_plan.md) | **Rust エンジン移行計画**（生き文書・2026-09-06 起案・ユーザ決定「段階的に Rust 化」）。目的（決定 1 回の 7 割が Python の解釈オーバーヘッド＝ノード 3ms→0.1〜0.3ms を狙う）・到達形（PyO3 wheel・Cloud Run は今の Python コンテナのまま）・段階 P0〜P5（骨組み→盤面と journal→ルール→効果解決→符号化/forward/探索→切替）と受け入れ基準（Python 版をオラクルに差分照合ハーネスで一致）・WP の分散運用 |
| [`session_orchestration.md`](session_orchestration.md) | **セッション並列運用**。親セッションから子セッション＝別コンテナを起動して CPU 律速の作業を分散する手順（起動／進捗確認／追加指示 poke／後片付け）と実測値。**成果の受け渡しは git push のみ**（`post_turn_summary` は要約が別生成されるためデータ経路に使えない）ほか既知の制約 |

フロントエンドの仕様は `opcg-sim-frontend/docs/`。

> エージェント（Claude）の運用ルール（開発・PR・マージのリズム／品質ゲート／文書更新方針）は
> リポジトリ直下の [`../CLAUDE.md`](../CLAUDE.md) に定義する（毎セッション自動読込）。CI は無く、
> 品質ゲートはローカル実行のみ（2026-07-11 廃止）。

## 報告（点）— `docs/reports/`

| 文書 | 内容 |
|---|---|
| [`reports/effect_verification_iter1.md`](reports/effect_verification_iter1.md) | 効果検証イテレーション1（2026-06）のトリアージ報告 |
| [`reports/effect_verification_iter2.md`](reports/effect_verification_iter2.md) | 効果検証イテレーション2（2026-06）の修正報告（EB01-001／お互い同時両側／検出器精度） |
| [`reports/quality_postmortem_categoryH.md`](reports/quality_postmortem_categoryH.md) | 品質ポストモーテム（2026-06）。カテゴリH（先頭条件が「。その後、」をまたぐ漏れ・~119能力/全弾）の見逃し原因分析と横展開調査（Duration/chooser/すべては健全） |
| [`reports/cpu_search_accel_pypy_20260620.md`](reports/cpu_search_accel_pypy_20260620.md) | CPU 探索 高速化 調査（2026-06-20）。「速くした分を horizon に回す」目的の手順と対照。PyPy 実測 ~2.1x（改変ゼロ・挙動ビット一致・同一337step/280decide）＝horizon +1 相当。エンジンは stdlib-only で PyPy 動作実証・配信スタック互換のみ課題。高速化手段の総覧対照表（差分評価/lazy/parked/LMR/mypyc/root並列/native）。ベンチ=`tests/scripts/bench_decide.py` |
| [`reports/pypy_migration_runbook_20260620.md`](reports/pypy_migration_runbook_20260620.md) | PyPy 移行 ランブック（2026-06-20）。方式選定（A 単一プロセス／B プロセス分離）→Phase0 互換スパイク→Phase1 移行→Phase2 Cloud Run デプロイ→Phase3 検証ゲート（CPython/PyPy 双方緑・挙動ビット一致）→Phase4 段階切替/ロールバック。配信スタック（pydantic-core/grpcio）の PyPy 非互換を方式Bで回避・`_USE_PYPY_WORKER` フラグで即ロールバック。リスク対照表つき |
| [`reports/pypy_phase0_result_20260620.md`](reports/pypy_phase0_result_20260620.md) | PyPy 移行 Phase 0 互換スパイク 実行結果（2026-06-20）。pypi 実 install 判定：純依存（uvicorn/websockets/requests/h11）✅／**pydantic-core ❌（PyPy wheel 無し・PyO3 が 3.11 未満を拒否）**／**grpcio ❌（wheel 無し・ソースビルド長大）**。方式A（単一プロセス全 PyPy）の2大依存が PyPy で建たない |
| [`reports/cpu_strength_plan_20260628.md`](reports/cpu_strength_plan_20260628.md) | CPU 強化 計画・設計＋**実測結論**（2026-06-28）。L1 単一系統化後、強化レバーを順に実装・計測した記録と総括（§K）。**結論＝検討した全レバーが「出荷済み／失敗済み／既出／幽霊」**で、現アーキ（L1 eval＋α-β/ビーム horizon=4＋PIMC K=4）の CPU は**達成可能上限に近い**。内訳: 速度系(PyPy/計画キャッシュ/ポンダリング/PV ordering/PIMC按分)=出荷済み／TT・地平線外静的項=失敗／L1係数SPSA=Elo余地≈0／①マリガン方策・④settle-PASS過大検出=幽霊(Elo中立・実測)／②隠れ情報サンプラ=既出(超幾何分布)／③動的時間配分=棄却。さらなる伸びは NNUE/ISMCTS 級の質的転換が要るが Python 1秒予算では棄却。**幽霊/失敗レバーの実装は §K の結論を受けて撤去済み（2026-06-28）** |
| [`reports/cpu_replay_ambiguity_r0_20260704.md`](reports/cpu_replay_ambiguity_r0_20260704.md) | 実対局リプレイ R0（2026-07-04）。記録アクション（card_id 基準）の一意復元可否を実デッキで計測＝曖昧率 3.5〜4.5%・fan-out 小・effect 選択は 0/515。判断＝(A) 決定論タイブレーク逆引きを主とする。計器 `tests/scripts/replay_ambiguity_probe.py` |
| [`reports/cpu_replay_roundtrip_r1_20260704.md`](reports/cpu_replay_roundtrip_r1_20260704.md) | 実対局リプレイ R1/R2（2026-07-04）。リプレイヤ＋ラウンドトリップで実デッキ 10/10 完全一致。副産物＝`cpu_ai._find_card` が stage/temp_zone 未探索で ACTIVATE_MAIN 等の記述が uuid 漏れ→修正で 8/10→10/10。(A) タイブレークは R0 見積りより頑健（場複製由来の分岐 0） |
| [`reports/gen3_adoption_20260711.md`](reports/gen3_adoption_20260711.md) | v3（旧称Gen3）採用報告（2026-07-11）。蒸留→分散追い学習・round60凍結・対L1多様97=0.854・15k追跡で過ピーク忘却を実証 |
| [`reports/cpu_learned_mark_review_20260711.md`](reports/cpu_learned_mark_review_20260711.md) | 人間マークレビュー第1回（2026-07-11）。8指摘→探索/読み出しの一般機構3修正（LCB読み出し・終局深さ減衰・sticky世界線）＋net起因(F4)の切り分け |
| [`reports/cpu_learned_mark_review2_20260711.md`](reports/cpu_learned_mark_review2_20260711.md) | 人間マークレビュー第2回（2026-07-11）。初版LCB読み出しの楽観乗り換え退行を発見→二重ゲート則へ再較正（2局×16マーク全数回帰）・「2つの時計」根因仮説へ接続 |
| [`reports/v4_adoption_20260712.md`](reports/v4_adoption_20260712.md) | **v4採用報告（2026-07-12）**。混合ラベル×残りターン補助×防御データ被覆の10k run・ピークround40凍結・対L1=0.812/対v3=0.583・防御マーク獲得・既知の限界（攻め付与ニュアンス）と運用知見 |
| [`reports/cpu_rl_pilot_p3_v2_ephemeral_session_20260705.md`](reports/cpu_rl_pilot_p3_v2_ephemeral_session_20260705.md) | P3 v2本走 実行記録（2026-07-05）。**常設CPU VMではなくエフェメラルなClaude Codeセッション**でも本走が完遂できることを実証（v1本走の遺構調査＋v2本走で確認）。温スタート・チャンク分割運用・別セッション由来の3改修（mu-mcts/重複解消/config集約）を隔離worktreeで検証しチャンク境界でfast-forward投入・コンテナ再起動からの回復（shard単位checkpointで進捗無傷）を記録。実測: クローン版245秒/shard→mu-mcts版126〜134秒/shard（約1.9〜2.2倍） |
| [`reports/cpu_v41_battle_exit_head_20260807.md`](reports/cpu_v41_battle_exit_head_20260807.md) | **v41 戦闘出口専用ヘッド（2026-08-07）**。v38（既存ヘッドへ同居→8点 3.06 < 本番 3.44）と v40（本体 value を全面順位学習→ゲート 8.00 満点なのにアリーナ 0.447 CI[0.409,0.485] の有意退行）を受け、較正の**置き場所**を変えた。`EXIT_HEADS` の残差ヘッドで戦闘出口だけを測り、胴体と本体 value は物理的に凍結（m1@14 の本体 value は gen12 と bit 一致のまま戦闘出口だけ +0.33 動く）。**判定＝機構は成立、教師が不足**: gen12 の枝間マージンは 0.02〜0.03 なのに defcf 584群775ペアで学習したヘッドの摂動は標準偏差 0.23〜0.27＝約10倍。5腕すべて出口順位 4/8 で、m1@14 を直す腕は必ず m2@44 を壊す（ゲート16seed でも m1@14 0.00→1.00 / m2@44 0.88→0.00）。次の律速は「マージン 0.03 を超える信号を持つ教師」 |
| [`reports/gen13_adoption_20260808.md`](reports/gen13_adoption_20260808.md) | **gen13 採用報告（2026-08-08）**。gen12 に戦闘出口専用ヘッド（EXIT_HEADS "battle"・hidden4）を載せ、人間裁定7点の注入順位ペアでヘッドのみ学習（v43）。胴体・本体 value は物理凍結＝通常の葉評価は gen12 と bit 一致。摂動最小化（margin0.03・中心化＝一般盤面 std0.038・v42 の1/9）が核。判定＝コーチ PASS 7.4 vs 6.6（m1@14 0.00→1.00 の獲得のみ・退行ゼロ・ただし8点中7点は訓練データ＝確認用）× アリーナ 400ペア 0.5212 CI[0.494,0.549] Elo+14.8 中立。**昇格基準 FAIL のままユーザ判断で採用**（gen9〜12 と同じ取引）。前段の失敗（v40 置き場所・v41 教師解像度・v42 摂動過大）の記録を含む。ロールバックはヘッドを外すだけ（gen12 と bit 一致へ） |
| [`reports/cpu_v44_turn_teacher_budget_20260808.md`](reports/cpu_v44_turn_teacher_budget_20260808.md) | **v44 ターン出口教師の予算判断（2026-08-08）**。13〜15時間の本生成を買う前に測って「買わない」と決めた記録。(1) **ラベルは足りていた**——worlds=32 の因果 z は真正答率 0.899（独立2組の δ選抜後一致率 0.818 から逆算）で、世界を倍にしても +0.05 程度しか動かない。過去に矛盾していた 0.231（11群）と 0.841（5群）はどちらも小標本の偶然だった（**5〜11群の測定値は読まない**）。(2) **律速は群数**——33群75ペアでは gen13 が既に 57本を正しく並べ |gap| 中央値 0.179 はマージン 0.03 の6倍、正味の教える余地は約9.4ペア＝学習しても摂動 std 0.0075 の恒等ヘッドにしかならない。(3) **決定打**——ターン末ヘッドの唯一の消費者であるプラン読み出しを ON にすると 8点は 7.44→7.50＝2σ=0.35 の内側で、**m1@3 の退行（v37② 0.25 / v39 0.50）は解消＝安全になったが得は無く serve コストは約5倍**。終点に利得が無いので経路ごと投資対象から外す。併せて `exit_head_probe` が `turn_all` 形式の accept を dict のまま `hit()` に渡して黙って常時不一致に数えていた計器の欠陥を修正（gen13 は 7/8 でなく 7/7） |
| [`reports/cpu_v45_m2at66_root_cause_20260808.md`](reports/cpu_v45_m2at66_root_cause_20260808.md) | **v45 m2@66 の原因特定（2026-08-08）**。gen9〜gen13 のどの世代でも 0.62〜0.69 から動かなかった唯一の常時未達点の正体。**value でも policy でもなく、ply0 でリーダー攻撃を選ぶかの一点**——最初の攻撃がリーダーなら 11/11 達成、ナミなら 0/5 で**例外ゼロの完全分離**（Fisher p≈0.00023）。16seed 全てで TURN_END は唯一の合法手＝CPU は一度も早期に畳んでいない。機序は相手の2つの**このターン中**デバフ（シャンクス −1000／カマクラ十草紙 −2000・どちらも相手が対象を選ぶ）が自リーダーに集中し 7000→4000 となり、リーダー攻撃が無駄手になって枝刈りで消えること。エンジンも枝刈りも正しい。**裁定の理由づけ「攻撃の順序はほぼ等価」は事実と食い違う**（2つ目のデバフ・永続性・リーダーへの集中が勘定されていない）ため要再裁定。ただしレフェリー32世界では攻撃間の z 差がノイズ幅内＝**直しても勝率に効くとは限らない**（v44 と同じ構図・着手前に勝率を測る） |
| [`reports/cpu_v46_m2at66_winrate_20260808.md`](reports/cpu_v46_m2at66_winrate_20260808.md) | **v46 m2@66 の勝率測定（2026-08-08）**。v45 の留保「直しても勝率に効くとは限らない」をレフェリー32世界で実測し、**裁定が不合格にしている側のほうが勝率が高い**と判明。効くのは「Mr.3 の起動を攻撃より先に置くこと」で 18〜19/32 対 13〜14/32（ライフ差も −0.91〜−0.34 → +0.44〜+0.59 と反転・下2腕は同価値バンド外）。**リーダー先攻か否かは 1勝差＝ノイズ**＝v45 の完全分離は事実だが4件消化に勝率上の価値は無い。計測上の落とし穴2件も記録——プランモードは攻撃を含む複数手順を表現できない（相手の防御窓で手番が移る＝「4件消化 vs 3件」を直接測る計器がそもそも無かった）／プランを効果解決ダイアログの途中で終えると壊れる（単手順 ACTIVATE_MAIN は 0/32＝約19勝ぶんの人工物・一度誤読して追試で反転させた）。**v44 と同じ教訓の2例目**＝ゲートの点を動かす前に勝率に効くかを測る |
| [`reports/cpu_v47_value_calibration_audit_20260809.md`](reports/cpu_v47_value_calibration_audit_20260809.md) | **v47 手順0: 本体 value の較正一次監査（2026-08-09）**。「パッチの前に本体が正しいか確認する」方針の第一歩。既存ラベル済みコーパス3系統（ターン末×w32純粋204盤面／ターン末×w4混合7,286盤面／戦闘出口×w4混合2,198盤面）で gen13 predict() と実測勝率を突き合わせた。**結論＝本体は系統的に誤っている**: (1) **ライフ差の重みが過大**（劣勢を悲観 −0.45〜−0.19・優勢を楽観 +0.20・戦闘出口で完全単調。手札差の同表は最大 −0.14＝半分以下）、(2) **中間域の圧縮**（予測 +0.20 の盤面の実測は +0.66〜+0.76）。どちらも枝間マージン 0.02〜0.03 の10倍超＝交換判断（防御・ドン付与）で順位が反転する構造的理由。m1@14 はこの一般傾向の1標本だった。混合ラベルの blend はライフ差バイアスを打ち消す向き＝真値は表示より大きい（保守側測定）。限界＝自己整合基準（当時エンジンのロールアウト勝率）・出口分布中心。計器は `value_calibration_audit.py` |
| [`reports/cpu_v47_step0_correction_20260809.md`](reports/cpu_v47_step0_correction_20260809.md) | **v47 手順0 の訂正（2026-08-09）**。手順0 の「混合ラベルの blend はライフ差バイアスを打ち消す向き＝真値はより大きい（保守側）」は**支持されない**。同じ手順0 の純粋ラベル（worlds=32・204盤面）で層別すると、blend ラベルで見えた劣勢側の負バイアス（−0.29〜−0.45）は **−0.00〜−0.02 に消える**。`margin_blend` は劣勢のラベルを下げ優勢を上げるので「圧縮して見える」と「劣勢で悲観して見える」を**同時に**作るが、手順0 は前者だけを勘定していた。訂正後に残るのは (1) 中間域の圧縮、(2) ライフ優勢側（≥+2）の楽観 −0.28（単調変換では −0.23 にしか下がらない＝特徴依存）の2つで、どちらも小標本のため**大きさは未確定**。教訓＝**較正（水準）の議論に順位学習用ラベルを使わない** |
| [`reports/cpu_v47_step1_general_boards_20260809.md`](reports/cpu_v47_step1_general_boards_20260809.md) | **v47 手順1: 一般盤面での本体 value 監査（2026-08-09・62対局744盤面4対面）**。純粋勝率ラベル×木の葉が見る一般盤面で測り直した本体。**(1) 手順0 の2所見（中間域の圧縮・ライフ差の単調バイアス）はどちらも再現しない**＝blend ラベルと出口分布の産物だった。**(2) プールすると系統誤差なし**（全体 +0.037 ±0.060・20層中 2SE 超は1セルのみ＝偶然の期待値）。**(3) しかし対面別では符号が逆で 2対面が 2SE 超**（bg_luffy:shanks −0.135 ±0.119 / bg_luffy:p_enel +0.230 ±0.174・ライフ差+1層では −0.406 と +0.436）＝プールは打ち消し合いだった。ただし当該2対面は 8〜10対局で**未確定**（4対面中2つが偶然 2SE を超えることはあり得る）。解釈＝本体が壊れているのではなく**訓練分布（nami:shanks 特化）の外での較正の甘さ**。**射程の限定**: 測ったのは水準であり順位ではない（1盤面=1群なので順位は未検証）。順位は v44 実測で 0.826 対 ラベル上限 0.899＝**約7ポイントの余地**があり、過去の指摘はすべて順位の誤りだった。方針＝本体の再学習も単調再較正も当てない／パッチ（裁定注入・出口ヘッド）が正しい戦略と支持される |
| [`reports/cpu_v47b_enel_seat_bias_20260809.md`](reports/cpu_v47b_enel_seat_bias_20260809.md) | **v47b: 対面依存バイアスの正体（2026-08-09・110対局1320盤面4対面）**。手順1 の宿題「対面依存バイアスは実在するか」の答え＝**実在するが value の較正問題ではない**。**(1) 共通の系統誤差は無い**（プール +0.031 ±0.044・訓練対面は両席とも ±0.03＝計器も本体も健全）。**(2) 正体は対面でなくリーダー**: 手番リーダーで割ると **エネル席 +0.42／相手席 −0.23〜−0.27** で、自リーダーの異なる2対面が +0.419 / +0.428 と一致する。プールした +0.14 は席間の打ち消し合いの残差だった（手順1 で「プールは対面の打ち消し合い」と書いたのと同じ誤りが一段深くにもう一度あった）。**(3) しかし実測側が怪しい**: 同じ盤面の実測勝率がエネル席で 3〜34%（訓練対面は実測 67.6% 対 予測 69.3% と一致）＝**シミュレータのエネルがほとんど勝てていない**。ラベルは自己整合基準なのでエンジンがエネルを回せていなければラベルの側が誤る＝手順0 §3 の限界がそのまま現実になった。**(4) ユーザ仮説「ドン最大6枚だから楽観」は向きが合わない**（楽観が最大なのは自ドンが少ない層・ドン多層では負）。リーダー能力の不発は否定済み（自ターン盤面の 51.8〜54.7% で使用済みフラグ・対照のシャンクスは 0%）。**手違いの記録**: w4/w5b に同じ seed base を渡し bit 同一のコーパスを二重生成（`--cluster game` のため数値は無傷・宣言した各40対局は未達で30対局）。次の一手＝較正ではなく **p_enel が正しくプレイされているかの確認**（1局リプレイ→デッキリスト→カード実装監査）。未解決＝ `bg_luffy:shanks` のシャンクス席 −0.308（同じシャンクスが nami 対面では +0.027）。計器に**ドン層別**と**手番リーダー別**を追加 |
- `reports/cpu_v48_human_baseline_20260810.md` — v48: 人間基準線によるエネル欠陥の特定。計器バグ6件（重大1=復元盤面のドン系リスト非Journaled）を発見・修正し、旗艦欠陥「6cエネルを出せない」は計器汚染の偽欠陥と判明。実在乖離は 掘り(turn1)/防御資源(turn4/6/8)/展開順(turn9) の3系統に収束。ゲート7点は全修正の前後で不変＝VERIFIED履歴有効・m2@44=0.75 三重確認。本番仕様レフェリー serve_referee 新設
- `reports/cpu_v48b_serve_referee_defense_20260810.md` — v48b: 防御3点（h1 turn4/6/8）の本番仕様裁定（計器修正後）。3点とも全候補が完全同値＝B系（防御資源選択）は裁定注入の対象から外す。gen5錨の turn4 万雷支持は本番仕様で再現せず非頑健。裁定注入の照準は A系（turn1 掘り・turn9 展開順）の2点に確定
- `reports/backbone_bb0_20260811.md` — **bb0: 合成カード実現性スパイク（骨組み線 Phase 0）**。能力収穫2963件の再結合デッキ×バニラリーダーで300局: 完走295/300・退化0.7%・ゲーム形健全＝メカニズム GO。構造違反5件は現行エンジンの実在欠陥2件（キャラ対象にリーダー混入→増殖／合法手列挙と declare_attack の不整合・決定論再現 seed 付き）＝修正後 bb0b で再監査
- `reports/backbone_bb0b_20260811.md` — **bb0b: 再監査＝骨組み線 Phase 0 GO 確定**。欠陥A/B修正（リーダー移動の中央ガード＋CANNOT_REST 列挙整合・テスト7件・ベースライン一致）後の300局: 完走300/300・構造違反0・退化2.3%。合成世界はデータ供給路であると同時にエンジンのファジングハーネス（実在欠陥2件を初日で還流）
- `reports/cpu_v52_lethal_distance_spike_20260812.md` — **v52: リーサル距離Δ特徴スパイク（G/B合流点）**。説明不能58点での系列実測: ライフ差 r=−0.23（逆向き）→無抵抗距離 +0.26→**防御込み距離 +0.35・符号62%**＝忠実度を上げるほど単調改善で概念を支持。乖離族の正体＝相互リーサルで「最初の詰め手を守り切れるか」（攻撃本数×マージン vs カウンター予算＝ユーザの7000理論の一般形）。台本忠実度の改善（ブロッカー/イベントカウンター/ドン付与しきい値）後に v10 Δ特徴として採用判定
- `reports/cpu_v51_negative_20260812.md` — **v51: 乖離盤面の較正学習は転移ゼロ（負の結果）**。実現証明つき教師50点（480局採掘）で G14 を MSE+蒸留アンカー微調整→教師は暗記（1.68→0.37）されたが学習外の乖離族7点へ転移ゼロ（1.32→1.33）＝representation-bound の3例目（v24/v41 同族）。G系へのデータ追加パッチはこの欠陥族に効かない＝解は B系 Phase 2 の導出量（リーサル距離）へ合流
- `reports/backbone_bb1_20260812.md` — **bb1: 情報量スパイク第1R＋ドメインギャップ分離**。ID無し骨組み（合成400局訓練）は実リーダー実盤面で r=0.20（G14 参考線 0.42・第1回届かず）だが、**実デッキ×バニラリーダーでは r=0.51**＝合成→実カードの転移は成立・落差の主因はリーダー効果と確定（ユーザの確認質問が設計動機の分離テスト）。第2Rは リーダー能力ランダム合成（Phase 3 第2段の前倒し）が正しい投資
- `reports/gen14_adoption_20260811.md` — **gen14 採用報告（2026-08-11）**。gen13 の value 本体を符号化 v9 へ拡張し掘り裁定ペア（v49 B腕）で蒸留アンカー付き順位微調整。戦闘出口ヘッド・vocab は gen13 と bit 一致（v43 較正保存）。ゲート9/9 PASS（h1@2 掘り 0→1 が実獲得・注入点はゲートと独立）×アリーナ 0.5038 CI[0.473,0.534] 中立＝昇格基準 FAIL のままユーザ判断で採用。既知の限界＝非エネル漏れ +0.09・サトリ移植プローブ ✗（経済でなくカード紐づけ）。ロールバックは既定を gen13 へ戻すだけ
- `reports/2026-09-04_human_samples_user_commentary.md` — **見本棋譜へのユーザ解説（一次資料・2026-09-04）**。エネル＝相手の展開を見て必要札を集める（放電もドロー目的）／ロジャー＝終盤にリーダーパワーが上がるのでカウンターは終盤に固める（時間で変わる手札効率）／ドフラミンゴ＝対象変更は KO 時効果でなく受け先のパワーと手札温存、10c のためにリーダー効果を使わないターンが多い（ドン予算）／共通＝デッキの中身と方針を意識。分析の誤読 2 点を訂正し、設計要件（デッキ知識・時間で変わる価値・予算・受け先・手札効率）に反映
- `reports/2026-09-04_human_sample_doflamingo.md` — **対照の見本 2: ドフラミンゴ（2026-09-04）**。型A が「守りの組」（リーダーの対象変更を KO 時効果持ちにだけ使う）として現れ、B（アクティブ加速）・C（ドン不要の攻撃を先に 5 回→最後に 10c）・D（ライフ 1 で止める）は同じ骨格。c10 との一致 0.55（PLAY 5/5・不一致は殴る順のみ）。一致率は エネル/ロー 0.21〜0.28 < ロジャー 0.37 < ドフラミンゴ 0.55＝組を要するほど離れる
- `reports/2026-09-04_human_sample_roger.md` — **対照の見本: ロジャー（殴り合い系）を人間が握った局（2026-09-04）**。型A（組）は消え、B（資源の前倒し）・C（殴る順＝中型→最大打点でカウンターを吐かせる）・D（ライフ 0 で 3 ターン戦う）・E（サーチで太らせる）は残る。c10 との一致 0.37（エネル／ロー 0.21〜0.28 より高い）＝殴り合いの判断は半分共有し、組を要する局面ほど離れる。順序と守りの節約は殴り合いでも不一致
- `reports/2026-09-06_r2_judgment.md` — **r2 vs a1 の判定（2026-09-06・1 サイクル指示書の初回）**。era6（a1 生成・14,400 局）で warm-start 1 epoch の r2: 主 0.495 [0.437,0.553]／副 0.596 [0.554,0.639]＝**非昇格**（c9 vs c8 と同型＝固定対面だけ伸びる）。次の候補 3 つ（教材追加で r2b／1 epoch の検証／設計側へ）
- `reports/2026-09-08_r3_judgment.md` — **r3 vs a1 の判定（2026-09-08・Rust 化後の最初のサイクル）**。教材を波25〜28（28,140 局）に広げ 2 epoch・新経路（torch＋memmap）で 73 分訓練。主 **0.615 [0.560,0.669]・Elo +81**（N 系で最大）／副 0.510＝互角・void 0。規約上は非昇格（副条件未達）だが r2 と逆の構図。次の候補 3 つ（r4／r3 採用／r3 を生成役だけ）はユーザ判断
- `reports/2026-09-07_train_profile.md` — **NRel 学習の時間・メモリ内訳（2026-09-07・計測 WP `train-profile`・`rust_engine_plan.md` §18.1）**。1 シャード・200 バッチの部分計測を行数比例で外挿（学習コードは無変更）。1 行あたり 読み込み 6.99e-5 秒・保持 2,610B・学習 2.86ms。**律速は backward（46〜54%）と、`--ablate rel` では捨てられる関係 R の再計算（29〜37%）で、どちらも 1 コアのみ（4 コア中 25%）**。効く手は ①`relations_batch` を呼ばない（value 1.46 倍・損失ビット一致・1 エポック 1.44 倍）と ④torch(CPU) 化（forward が numpy と 8.8e-7 一致・1/2/4 スレッドで 2.28/3.59/5.33 倍・①と併せて value のみ 2.83 倍／policy も移植で 7.67 倍）の 2 つだけ。③float16/int16 memmap は常駐 2.08 倍圧縮＝時間でなくメモリの手。②バッチサイズ拡大は逆効果（4 倍 0.74・16 倍 0.64）・BLAS スレッド増も効かない（0.93）
- `reports/2026-09-08_scenario_analysis_01.md` — **分岐点シナリオの分析 #1（2026-09-08・計画 §20・r3／a1）**。人間のプレイログの一部を CPU に打たせて Claude が比べる最初の 2 本。ロー T5（コンボ）: 4 本中 3 本が人間と同じ連鎖（-3000 → ロー起動でブロッカーをデッキ下＋レイジュ 2 ドロー）・差は手順。エネル T4（除去）: 4 本とも神の裁き／ガンマナイフを使わず（候補上位 5 にも現れない＝探索が触っていない）、相手ターンにパワー 0 の成長エンジンを脅威と見ていない。次は除去シナリオを 3〜4 本
- `reports/2026-09-08_scenario_analysis_02.md` — **分岐点シナリオの分析 #2（2026-09-08・9 シナリオ・r3／a1×2 seed＝36 本）**。人間ログ 6 局を Claude が全ターン読んで検証範囲 9 本を切り抜き note を書いた版。同じ方針: ロー T5 のコンボ（4/4）・ロー T9 の手順（攻撃先行→ドン!!-3）・ロジャー T6 の小駒殴り（半分）・エネル T9 は結果同一。違う: **メインフェイズのイベント PLAY が 36 本で一度も選ばれず**（除去・コンボの部品が全部イベント＝再現性の低さはこの 1 点に帰着）・相手キャラを攻撃対象に選ばない（ドフラ T10 は 4 本とも T11 に負け）・手札を回さない・相手の手札を読まない。r3 と a1 の差はこの観点では出ない。次: イベント PLAY の訪問割合を測る／ドフラ T10 を価値の回帰テストに。**「イベントが探索されない」の原因は #3 で訂正**（合法手に無かった）
- `reports/2026-09-08_scenario_analysis_03.md` — **分析 #3: 「イベントを打たない」の正体は合法手の穴（2026-09-08・計画 §8.26／§8.27）**。探索の生 stats（全合法手・P・N）を取ると PLAY 480 手にイベント 0＝`rules/legal.rs` が全イベントを列挙から外していた（Rust 化後の CPU は【メイン】イベントを一度も打てず・人間は直接 PLAY するので気付かない）。修正して 9 本を回し直し: イベントは普通に探索される（N=0 は 13%・P はキャラと同水準）が**除去イベント（ガンマナイフ・ゴムゴムの雷）の事前分布だけ 1 桁低い**＝訓練データ（波 28: イベント選択率 5.5%・除去系は下位）の写し。副産物: 効果の対象選択が「対象なし」で解決される欠陥（万雷カウンター・神の裁き KO・§8.27・未修正）。再生 golden は singleton デッキ（イベント 0 枚）で作られておりこの穴を守っていない
- `reports/2026-09-08_select_default_rca.md` — **WP `rs-select-default`: 対象なし解決の原因分析（2026-09-08・計画 §8.27／判定 §8.27.2）**。(a) 万雷カウンターの +1000 が空＝既定解決 `choose_selection` が「自分のリーダー＋キャラ混在」を判別できず空を返し、それが候補の先頭に並び、3 枝の値が完全一致するタイで先頭が勝つ。(b) 神の裁きの KO が空＝既定解決は正しいが箱の浅い先読みの価値が「KO しない」を高く見た（除去の過小評価・ネット側）。修正案③（can_skip）は前提誤り。推奨①（効果の種別を既定解決に配線）を WP `rs-select-fix` で実装。残る疑問 Q7（3 枝の値が 15 桁一致は不自然）は同 WP で確認。RESULT は同名 `.RESULT.json`。**修正は WP `rs-select-fix` で本線へ**（計画 §8.27.4／§8.27.5・RESULT は `2026-09-08_select_fix.RESULT.json`・(a) 万雷／神の裁き／放電の自分への BUFF が非空に・再生 golden 133 局変化＝想定外 0・監査 golden は既定解決を通らないため不変）
- `reports/2026-09-08_scenario_analysis_04.md` — **分析 #4: 思考ログで読む「CPU は何をしようとしていたか」（2026-09-08・計画 §20.4・WP `rs-think-log` の出力 18 本）**。V はリーダー 2 枠でほぼ決まり相手の盤面の「数」を見ない（ドフラ T10 は V 0.8〜0.9 のまま負け）／相手のパワー 0 の 1c ルフィを「自分に有利」と評価（+0.06〜+0.33＝消すと損）／探索は事前分布に支配され 47 手中 3 手しか訪問しない／読んだ範囲では Q が除去・パンプを推しているのに訪問数で負ける例（神の裁き 0.989 vs 0.862・ガンマナイフ -0.185 vs -0.374）／PV は自分の番で尽きる。縛り A〜E の表と、今日動かせる A（sims・選択規則）の実験を提案。RESULT は `2026-09-08_think_log.RESULT.json`
- `reports/2026-09-09_search_a.md` — **WP `rs-search-a`: 探索の設定（sims 160→16,000 の段階掃引・N 下限つき Q 最大・根の P 平坦化）を 9 シナリオで測った（2026-09-09・r3・ネット不変・計画 §20.5）**。数字のみ（判定は分析 #5）。ガンマナイフは sims 2,560 で反転（Q が -0.47→+0.35）＝探索予算／神の裁き→攻撃の順は 16,000 でも反転せず（この基点ではオーム攻撃の Q が 1.000）／ドフラ T10 の根の訪問手は 7/47 で飽和（16,000 でも増えない＝事前分布の門）が 640 以降は負けない／「Q が高いのに N で負けた手」142→66→37→13／レイテンシ中央値 160=0.39s・640=1.6s・2,560=4.9s・16,000=18.6s。副産物: `rs_scenario_play.py` の探索乱数の基点を固定（以前は起動ごとに世界サンプルが変わっていた）。RESULT は `2026-09-09_search_a.RESULT.json`
- `reports/2026-09-09_scenario_analysis_05.md` — **分析 #5: 探索の設定で動く分と動かない分（2026-09-09・計画 §20.5.2）**。縛り A は 2 つに割れる: (A1) 予算＝ガンマナイフは 2,560 で拾える・ドフラ T10 の負けは 640 で消える／(A2) 事前分布の門＝根の訪問手は 16,000 でも 7/47。神の裁き→攻撃の順は sims では動かず、根の平坦化＋N 下限つき Q 最大（R2）で 8 seed 中 6 本が人間の順（既定 0/8・補足実験）。serve と生成の候補設定＝R2（640・q_min_n 1/8・t=2・レイテンシ 4 倍）、採否はアリーナ（WP `rs-search-arena`）。判断に使う決定は seed 8 本を標準に
- `reports/2026-09-09_pimc_worlds.RESULT.json` — **WP `rs-pimc-worlds` の成果（2026-09-09・計画 §20.7.1／判定 §20.7.5）**。1 回の decide で世界サンプルを K 本引き木を並列に回して根で束ねる（`DecideOptions.worlds`・既定 1＝不変）。神の裁き→攻撃の順は visits 規則では K=1〜8 で 0/8 だが、オーム攻撃の Q が 1.000→0.904（K=8）に落ち、229 決定の 27.9% で世界ごとの最善手が割れた。レイテンシ K=2 1.27 倍・K=4 2.32 倍（世界ごとのコストが二峰で壁時計が最大に張り付く）。材料 `scenario_out_w/`
- `reports/2026-09-09_setup_box.RESULT.json` — **WP `rs-setup-box` の成果（2026-09-09・計画 §20.7.2／判定 §20.7.5）**。準備の手（メインイベント／起動メイン／登場時持ちの PLAY）を「続きの攻撃 1 回」まで箱にする（`SearchOptions.setup_box`・既定 false＝不変）＋全箱に共通の「自分の対象選択を枝にする」規則。ガンマナイフ 0/8→5/8・神の裁きの KO 0/8→2/8・神の裁き→攻撃の順は 0/8 のまま。レイテンシ 3.3 倍（全節で候補化）。要判定 3 点（多段の枝・素の手と P の等分・レイテンシ）は §20.7.5。材料 `scenario_out_b/`
- `reports/2026-09-09_scenario_analysis_06.md` — **分析 #6: 探索の 3 段を束ねる（2026-09-09・計画 §20.7.5）**。worlds 4 ＋ R2（sims 160）が最も効く: 神の裁き→攻撃 5/8・リーサル決着 4/8・ガンマナイフ 4/8・レイテンシ 0.39 s（既定の 1.9 倍）。準備箱は除去→攻撃と KO 対象に効く（5/8・5/8）が、worlds と組むと神の裁きの訪問が「素の手 ×2 ＋ 箱 8 本」に割れて 0/8 に落ちる（素の手が箱に勝つのが 624 枚中 366）。アリーナの候補設定は worlds 4 ＋ R2（§20.5.3 差し替え）、準備箱は素の手を落とす／P を等分しない直し（`rs-setup-box-2`・§20.7.6）の後に再測。材料 `scenario_out_c/`
- `reports/2026-09-09_search_arena.RESULT.json` — **WP `rs-search-arena` の成果（2026-09-09・計画 §20.5.3／回収 §20.5.3-1・判定 §20.7.10）**。候補＝r3 ＋ 探索設定〔sims 160・worlds 4・q_min_n 1/8・t=2〕vs 基準＝r3 ＋ 既定。主条件（random×synth・382 局）wr 0.5314・CI [0.476, 0.587]＝**規約未達**／副条件（固定ミラー・384 局）wr 0.5443・CI [0.502, 0.586]＝退行なし。+22 Elo 相当で有意ではない。レイテンシ 1.36 倍。アリーナに候補席の探索設定 seam（`--cand-*`）が入った。台帳 `n1_results/arena_search_r2/`
- `reports/2026-09-09_leaf_rollout.RESULT.json` — **WP `rs-leaf-rollout` の成果（2026-09-09・計画 §20.7.9・判定 §20.7.10）**。葉をターン終了まで方策で打ち切ってから評価する（`leaf_rollout="turn_end"`・既定 none＝不変）。単独（F1）は全鍵 0/8、R2 と組んで神の裁き→攻撃 4/8・決着 2/8、worlds 4 も足して決着 4/8＝打ち切りなしの worlds 4 ＋ R2（5/8・4/8）を上回らず。ガンマナイフは 0/8 に落ちる（攻撃先行の葉も同じだけ得をする＝公平な比較になった結果、V は攻撃先行を選ぶ）。レイテンシ 1.9〜2.5 倍。材料 `scenario_out_f/`
- `reports/2026-09-09_setup_box_3.RESULT.json` — **WP `rs-setup-box-3` の成果（2026-09-09・計画 §20.7.8・判定 §20.7.10・status=partial）**。準備箱を「発動 → 対象選択 → 効果」までに改め、素の手を落とし、枝の束ね（select_groups）と診断つまみ（select_branch）を足した。E1（worlds 4 ＋ 箱 ＋ R2）は全鍵で最下位、E2（箱 ＋ R2）ガンマナイフ 3/8・KO 3/8、対照 E5（R2 単独）ガンマナイフ 6/8・KO 0/8＝**箱はガンマナイフを下げ KO を上げる**。共通規則は中立（E4 7/8）。対象選択の P は一様（方策の候補行が選択肢を区別しない）。レイテンシ E2 0.44 s・E1 1.0 s。材料 `scenario_out_e/`
- `reports/2026-09-10_removal_decks.RESULT.json` — **WP `rs-removal-decks` の成果（2026-09-10・計画 §20.8.1・判定 §20.8.1-1・status=partial）**。除去の「型」（form:source:band）の分類器 `deck_roles.classify` と型 × 色の差し込み `synth_roles`（率 0〜25%・死に札とカウンター枚数を悪化させない）、dump v4 の `deck_kinds` 列と part ごとの `meta_games.json`。硬い除去 0 のデッキ 19.6% → 9.1%。**発見 2 件**: パーサはバウンスを `BOUNCE` で出すが符号化の `_REMOVAL_OPS` は `MOVE_TO_HAND` しか見ておらず対象ゾーンも見ない（符号化側は未修正＝別 WP）／synth_roles で PRB02-006 ゾロの置換効果（相手の効果でレストになる場合に代わりに他をレスト）が SEARCH_AND_SELECT を消費せず無限ループ＝void 1/120（エンジンの欠陥・§20.8.4）
- `reports/2026-09-10_aux_heads.RESULT.json` — **WP `rs-aux-heads` の成果（2026-09-10・計画 §20.8.2・判定 §20.8.2-1）**。対称な補助教師（dump v4 の `aux` 10 列・`aux_tok` 6 枠 × 3・`aux_mask`）を生成時の台帳から backfill（コスト +3.7%）、NRel の別ヘッド（λ_aux・既定 0＝1 bit も変わらない・Rust は補助鍵を無視）、評価帯の層別（リーダー色／除去直後の行・`deck_kinds` があれば型で）。200 局 1 エポックでは z の予測差は誤差内（0.6025／0.6031／0.5930）、補助損失は下がる。副産物: torch backend の多スレッド学習は同一設定でも再現しない（1 スレッドか numpy でしか同一性を判定できない）／効果イベントに発生源 uuid が無く `aux_tok` の「発動したか」は card_name 一致（Rust の欄追加は別 WP）
- `reports/2026-09-10_replace_rest_fix.RESULT.json` — **WP `rs-replace-rest-fix`＋`rs-eps-explore` の成果（2026-09-10・計画 §20.8.4／§20.8.5・判定 §20.8.4-1）**。A: PRB02-006 ゾロの「代わりにレスト」はパーサが REPLACE_EFFECT にせず素の誘発能力（OPPONENT_TURN・SOURCE_STATE==IS_RESTED・REST）として出すため、常在効果の再計算が毎回実行して対象選択で中断し続けていた（pending は消費されていた）。再計算から外し、レストの現場で置換として扱う（`rules::active_rest_replacement`・0 枚なら元のレスト）。同じ経路を踏むのは 1 枚。効果イベントに `source_uuid`／`trigger` を追加＝golden 2 本を作り直し（差分がその 2 欄だけであることを機械的に確認・盤面と合法手は 0 件不変）。B: 両方向 ε 探索（`--eps-play`／`--eps-hold`・decide の後で手だけ差し替え・π 不変・`forced` 列）。synth_roles 120 局 void 0・ε 3% で play 36／hold 8（除去が合法な main 行 1,516）
- `reports/r4_judgment_20260910.md` — **r4 の判定（2026-09-10・era7 第 1 回・規約上は非昇格）**。r4＝warm-start r3・π 波 29（worlds 4 ＋ R2・synth_roles・両方向 ε）・z 29+28+27・補助教師 0.1・lr 2e-4。評価帯は r3 を 16 倍の幅で上回る（0.5198 vs 0.5327）が、アリーナ主 0.4555 [0.40, 0.51]（−31 Elo・未達）／副 0.5469（退行なし）。疑いの本命は教師（平坦な π）と serve 既定（visits・worlds 1）の不一致。次は配備案「r4 ＋ worlds 4 ＋ R2」vs 既定。副産物: lr 5e-4 の warm-start は V を崩す（切り分け §20.8.7-2）・訓練の再現性（同一設定の幅 0.0008）
- `reports/r4_validity_20260910.md` — **r4 の妥当性検討（2026-09-10・§20.8.8-2／-3）**。(a) 旧分布（波 28 holdout）で r4 は全バケットで r3 より悪い（0.5515 vs 0.5348）＝新分布に寄って忘却。(b) 教師と同じデッキ分布（random × synth_roles）のアリーナは 0.5182 [0.465, 0.571]＝分布内でも優位なし。結論: r4 不採用・評価帯は旧分布と新分布の両方で測る・era7 は 2 波揃えてから訓練・主条件を synth_roles にするかはユーザ判定
- `reports/r4w4_judgment_20260910.md` — **配備案「r4 ＋ worlds 4 ＋ R2」vs 既定の判定（2026-09-10・非採用）**。主 0.4818 [0.424, 0.539]・副 0.4609 [0.417, 0.505]（退行シグナル）。r3 に同じ設定を載せた場合（0.531／0.544）より低い＝r4 は探索設定に関わらず r3 を上回らない。r4 の総括は不採用。次は WP `rs-q-pi`（π を Q で補正した教師・§20.6）
- `reports/2026-09-10_q_pi.RESULT.json` — **WP `rs-q-pi` の成果（2026-09-10・計画 §20.6.1／§20.6.2）**。dump に `pol_p`／`pol_v0`、訓練に `--pi-teacher q_improved`（Q で補正した改良方策 π'・c_scale 0.3・既定 visits はビット一致）、評価帯に方策 2 目標と「門」の指標。r4q＝同じ教材で教師だけ替えた対照。材料と npz は同ブランチ
- `reports/2026-09-11_enc_v14.RESULT.json` — **WP `rs-enc-v14` の成果（2026-09-11・計画 §20.9／§20.9.1・実装メモ §20.9.2・判定 §20.9.3・採用）**。符号化 v14＝append-only（S 20→22: `power_opp_turn`／`act_avail`・EXTRA 29→33: `deck_removal_fixed`／`opp_pool_removal_fixed`／`deck_bounce`／`opp_pool_bounce`）、候補行に効果の対象選択の対象を載せる（`enc_version ≥ 14` のネットだけ・r3 は 20/20 同一）、ε の対象ランダム化（`forced=3`・`--eps-target`）、`dump_io` が v13 の波を 0 埋め（PACK_VERSION 3）。golden 不変・`Game.encode` +2〜9%・559 passed・void 0。20 局で forced=3 は 2 行＝波 30 は `--eps-target 0.3`
- `reports/r5_judgment_20260911.md` — **r5 の判定（2026-09-11・era7 第 2 回・符号化 v14・規約上は非昇格）**。r5＝warm-start r3（v14 へ pad）・π 波 29+30・z 30→27・aux 0.1・lr 2e-4・best ep1。評価帯は r3→r4b→r5 で単調改善（波 30 0.5131→0.5044→0.4981・除去直後も）が、アリーナ主 0.5026 [0.45, 0.56]・+1.8 Elo＝未達／副 0.5365＝退行なし。要点: era7 の 3 本は全部 192 ペアの分解能（±37 Elo）の内側＝改善が 0 か +15 かを分けられない。提案は高分解能アリーナ（2,800 ペア・±10 Elo・1 時間）を先に。層別では lock 系が退行（相手ターンの進行が S に無い）
- `reports/2026-09-11_hires_arena.md` — **高分解能アリーナ r5 vs r3（2026-09-11・§20.9.7／§20.9.8・2,800 ペア・±10 Elo）**。wr 0.5050 [0.491, 0.519]・+3.5 Elo [−6.4, +13.4]・void 2＝era7 の改善は 0。評価帯の改善（−0.015）が打ち手に出ない理由は生成役 r3 の固定（π・z が r3 の打ち筋）と c 系向けの昇格閾値。提案 A: 生成役を r5 に替え門を「退行なし」に読み替え／B: worlds 4 ＋ R2 の 700 ペア診断。16 セッション使い回し・1 時間・1 ペア 4.5〜10 秒
- `reports/2026-09-12_search_vs_prior.md` — **探索は π（事前分布）を超えているか（2026-09-12・`tests/scripts/search_vs_prior.py`・波 30／31／32・各 5.6 万点）**。**写しは否定**: 探索が argmax を変えた局面 51〜57%（k≥9 で 57〜65%）・質量 23〜26% 移動。**ただし事前分布がほぼ一様**（k≥9 で H_p 2.6≈ln 14・k=2 で 0.66≈ln 2）＝π は難しい局面で意見を持たない。r3→r5 でさらに平ら（H_p 1.89→1.98・一致 0.49→0.43）。各世代の π は教師の尖り（1.6〜1.7）に届かず 1.9〜2.0＝訓練で 0.3 ぼやけ探索で 0.3 尖らせる収支。読み: 律速は教師の情報量ではなく **π の当てはまり**（r7 と同じ絵）。探索側は触らず、V と π の損失の釣り合い WP へ
- `reports/2026-09-12_r7_pi_window.md` — **r7＝π の窓の検証（2026-09-12・π を波 29〜32 の 4 波に広げ、他は r6 と同一）**。事前基準（r5 に 3 波すべてで勝つ・波 28 の top1 ≥0.577）は**未達**（0/3・0.550）。ただし**方策は r6 より 3 波すべてで改善**（忘却の半分を回復）し、**代わりに V が r5 の水準に戻った**（w32 v_mse 0.5115→0.5265）＝π の量と V がトレードオフ。アリーナは主 0.5365・+25 Elo／副 0.5234・+16 Elo と r6 から両条件とも上向き、副の退行シグナルが消えた（いずれも ±38 Elo 内）。読み: π の窓は方策の実効レバーだが単独では足りず、2 ヘッドの損失の釣り合いが次の対象。r3 以降で初めて高分解能アリーナの価値がある候補
- `reports/2026-09-12_plan_map_strat.md` — **方針の地図のリーダー／デッキ別の層別（2026-09-12・ユーザ指摘「しきい値はリーダー＝デッキで変わる。平均に強制するのは誤った学習」で r8 の提案 1 を撤回して実施）**。**答え: しきい値は帯（ライフ 2〜4）として共通・帯の中の位置は解けない**。リーダー個別 40 名の山は L2 7／L3 26／L4 7＝**40/40 が帯の中**・除去テンプレート 9/9 が L3・曲線の形は全群共通（L1 負→L2〜4 正→L5 ほぼ 0）。**帯の中の差は n 16,000 でも 0.005〜0.017**＝1 リーダーでは解けず、**山の位置は関門の設定で動く**（robust でない証拠）＝固定ライフの強制は不可。**攻めは 40 名すべてで V_face−V_board が正**（平均 +0.188・逆転ゼロ）＝デッキの性質ではなくライフレースの構造。手がかり: ライフ 1 の符号はリーダーの総ライフで変わる。事前基準の訂正（span は群内の高低差で群間のばらつきではない）と計器のバグ 2 件の修正を含む。次: 本線は「地図を対局で使う」
- `reports/2026-09-12_race_atk.md` — **守りを飛んでくる攻撃パワーで層別（候補 D）＋リーダー総ライフの追試（候補 M）・波 29/30/31/32 の 27,832 局**。**帯を固定するとライフで単調に守る**（`p3-4k` 0.341→0.644）＝**`2026-09-12_plan_drift.md` §4 の「ライフ 2〜5 で平ら＝逆算の形は無い」を訂正**（`p<=0` の占率がライフ 5 で 32.7%・ライフ 1 で 5.5% と動くため周辺分布が打ち消していた）。超過パワーが上がるほど守らない（0.704→0.525→0.431→0.415）が、カウンター単価を数えていないので合理と非合理は分離できない＝候補 G に統合。決められた 1 点は**ライフ 1 × `p5k+` で 0.474**＝最も守るべき所で半分以上通す（攻めの「相互リーサルで引く」と同じ形）。**M は閉じる**（ライフ 1 の符号は勝敗の見込みで説明でき、リーダー別しきい値の証拠ではない）。層別で消えないのは木と V の乖離（argmax_guard 0.77〜0.93 対 played_guard 0.28〜0.48・全群同じ向き）。
- `reports/2026-09-13_pi_steps.md` — **V と π のステップ比（候補 A・`--pi-steps-mult`・r9＝×2／r9h＝×0.5・教材も warm-start も r7 と同一）**。**判定: 回数は効かない＝候補 A は閉じる**。π を 2 倍回して買えたのは top1 +0.0026（3 波平均・二項の SE 0.0035 と同程度）・アリーナも r7 に及ばない（+14.6／+3.6 対 +25.4／+16.3）。**律速は π の更新回数ではなく相異なるデータ量**（同じ 1.6M 点の 2 周 +0.003 対 データ 2 倍の r7 +0.052／+0.029／+0.011）。**非対称**: π を半分にすると V が −0.009〜−0.012 改善し方策の代償は −0.005＝`--pi-steps-mult 0.5` は「V を戻す安いつまみ」。ステップ比とエポック数は絡む（r9 は best ep1・r9h は ep1 で過学習）。
- `reports/2026-09-13_time_head.md` — **時間軸ヘッド（候補 H・r10＝r7 と同じ教材に `--time-weight 0.1` だけ足したもの）**。**答え: 盤面は「いつ決着するか」を持っている**——holdout 19 万行の Huber が**学習後 0.279 対 定数予測 0.613 対 未学習ヘッド 1.698**（7 列すべてで定数を下回る）＝**決着までの自席ターン数は誤差約 1.0 ターン**（定数 1.8）・**2／3 先のライフは誤差 0.8 枚**（定数 1.3）。3 波の行列は r7 から許容内（波 32 は −0.0069 で改善）＝無害。**アリーナは動かない**（主 +1.8／副 −19.9 Elo・参考値）＝ヘッドを足すだけでは強さにならない。補助ヘッド 2 本（r8／r10）で副ミラーの CI 下限が揃って 0.45 を割った点を記録。教材集合の不一致（打ち切り part が同じブランチに後から push されていた）は`nrel_r7.npz` の `meta["src"]` を正本にして解決し、meta に `z_src` を焼いて再発を止めた。
- `reports/2026-09-13_time_plan_map.md` — **時間 × 方針の地図（波 30/31/32 の holdout 3,013 局・攻め 32,231 行／守り 24,207 行）**。**答え: 時計は使われているが、守りでは逆向き**。攻めはレースの余裕で割合が動く（face 0.356→0.580・board 0.239→0.078）が**勝率は逆転しない**（face−board が全 5 帯で正）＝「遅いから盤面を触る」は支持されない。**t≤2 では face 0.684 対 board 0.081**（n 11,949）なのに board を 10.6% 打つ。**守りが本命**: **t≤2 で guard 0.577 対 take 0.424（+0.153・n 10,276）なのに take が 0.741**・t5+ では差 +0.001 なのに guard 0.586＝**完全に逆**（3 波で小数第 2 位まで一致）。ユーザの守りの考え方（終盤に必要なライフを逆算して守る）は**実測で正しく、CPU がやっていない**。方法上の訂正: **守りの true 軸は決定に汚染される**ので決める前の軸は pred。次は候補 F（方針の価値で探索の事前分布をバイアス）。
- `reports/2026-09-13_can_or_wont.md` — **「しない」のか「できない」のか**（ユーザ指摘 2026-09-13・同じ 3,013 局を「選べた行だけ」に絞り直したもの）。**守り＝選択の誤りで確定**: t≤2 の guard−take は絞らない +0.153 → 守る手段が在った行だけ +0.147 → **止めるだけ足りた行だけ +0.141**（3 波一致）。受けた 7,612 行のうち **92.6% は守る手段を持ち・71.0% は足りていた**＝holdout だけで約 5,400 行が「止められたのに受けた」。**中盤の board は能力の限界**: board を打った行で face も在った割合は t≤2 98.9% に対し **t5+ は 54.8%**＝中盤の 45% は強制なので「board はどこでも割に合わない」は**終盤に限る主張へ狭める**。候補 D の宿題（カウンター単価の管理）も閉じた（足りていても受けている）。計器の穴（窓の行は候補一覧を持たない→符号化の `counter_value` から読む）も記録。
- `reports/2026-09-13_hires_arena_r7.md` — **高分解能アリーナ r7 vs r5（6 シャード 2,095 ペア）**。**wr 0.4936 [0.4769, 0.5102]・−4.5 Elo [−16.1, +7.1]・void 0.24%＝r7 は r5 と同じ強さ**。192 ペアの「+25.4 Elo」はノイズで、**世代差が誤差だったのは 2 世代連続**（r5 vs r3 も 2,798 ペアで 0.5050）＝**192 ペアで世代交代を判断してはいけない**。r7 は π の窓を 4 波に広げた世代なので「π は現 era のみ」の規約見直しは根拠が消えた（現状維持）。事前登録は 2,800 ペアだったが、残り 705 ペアが wr 0.59 級でない限り結論は動かない算術を添えて 6 シャードで締めた。
- `reports/2026-09-13_deck_profile.md` — **デッキ素性の実測（合成 800／実デッキ 240・記録も対局も不要）**。**`c̄(x)` は 1000 あたり +0.66 枚**（+1 枚ではない＝2000 カウンターが在る）で**段はでこぼこ**（2000→3000 が +0.97 で最大＝ユーザの「7000 に積む」の正体は**1 枚で足りない高さへ跨ぐ段**）。**実デッキ 4 リーダーで τ 0.00〜0.50・`c̄(3000)` 2.09〜3.00**＝同じ x でも `c(x) − c̄` の符号がリーダーで逆になる＝**固定ライフしきい値は原理的に誤り**（ユーザ指摘 2026-09-12 が実測で成立）。実デッキは合成より守りが重い（`p_cant(4000)` 0.33 対 0.19・【カウンター】イベント 6.65 対 3.97 枚＝ドンが要る守りが多い）＝**実デッキの弱さは「汎化の失敗」と「予算が厳しい」に分けて読む**。
- `reports/2026-09-13_search_limits.md` — **探索は律速ではない**（被覆・`worlds` 2 腕・PIMC 診断の 3 本）。**枝予算は候補を 1 つも落としていない**（40 局 2,606 判断点で `coverage` 1.000・`missing_any` 0・`best_not_in_a` 0・予算切れ 0）＝2026-08-12 の「候補にすら無い」は当時の Python の話。**`worlds` は効果の証拠なし**（腕 4 +12.6 Elo／腕 8 +3.0・用量反応は逆向き）だが **250 ペアでは +20 Elo 級を棄却できていない**（事前想定 1,200）。**PIMC の融合は接戦帯に限局**（`|v0|≤0.2` で世界の最善が 44% 割れ・2.0% はどの世界の最善でもない手・Q の幅 0.361 対 決着帯 0.133）＝守りの判断が住む帯と同じ場所。**K をまたいだ比較は機械的な偏りが入るのでできない**。
- `reports/2026-09-13_zero_sum.md` — **零和の制約が破れている**（初測定）。同じ盤面を両席から評価した `V_p1 + V_p2` が **−0.138**・対局クラスタ 95% CI [−0.187, −0.092]（r3・24 局 3,380 点）＝両席の `P̂` の和が 0.93＝**片側あたり約 3.5 ポイント悲観**。**判定に使う量を途中で直した**——局面ごとのずれは私的情報の差で正常なので、零和が縛るのは**平均**（SE は対局でクラスタ）。影響が本質的なのは**窓のミニマックス**（相手側の価値を符号反転で使う）。切り分け（全体に悲観か・非手番側の評価が分布外か）は手番側の較正で付く＝未実施。直すなら訓練に**反対称の正則化**（教師不要・ユーザ決定）。
- `reports/2026-09-13_lambda_w32.md` — **ライフ 1 枚は札 2.5 枚＝予算モデルは自己整合、ただし私の予測の当て方が誤っていた**（波 32 の holdout 1,041 局・r3）。`λ` = +0.1158（相手ターン +0.1362）・`μ` = +0.0433（同じ holdout の独立な再現で小数第 4 位まで一致）⇒ **`λ/μ` 2.674**・`c̄ + τ_value = 1.674`。§11.1 で `c̄` に「3000 帯の 2.2 枚」を当てて `λ/μ ≈ 3.4` と予測したのは**誤り**——`c̄` は x の分布で重みを付けた平均（1.0〜1.3）なので、逆算した `τ_value` は 0.37〜0.67 で密度 0.17 と整合＝**恒等式は生き残る**。**新しく出た打ち回しの数字**: 無差別点は**札 1.5 枚あたり**＝x≤2000 は守り・x≥3000 は受けるが平均的な最適（実プレイは安い側を 47.9% 受けている）。**新しい欠陥**: ネットの内部の交換レートは 3.60／4.69 で実測より 35〜90% 高い——`λ` は合っているのに **`μ` が 24〜48% 過小**＝ライフを正しく恐れ札を軽く見ている。**歪みの向きは守りすぎなのに実プレイは守らなすぎ**＝行動の誤りは価値から来ていない（本日の「壊れているのは方策の順序」を別角度から支持）。`λ` は逆因果に弱い（ライフが低い＝既に負けている）ので過大の可能性あり。
- `reports/2026-09-13_order_option.md` — **攻撃順序の取り逃がしは実在する**（反事実で初測定・記録では測れない量）。攻撃の入口が 2 つ以上ある判断点で盤面を複製し、入口を強制してターンの終わりまで打たせて同じ席から V を比べた（r3・48 局・288 点を複製・147 点が判定対象）。**`opt_gap_honest` 0.0420・対局クラスタ CI [0.0218, 0.0663]**＝事前基準の下限 0.02 を超える。腕の差は続きの雑音の **7.9 倍**（＝雑音ではない）・**勝者の呪いの上振れは 0.004 しかない**（A で選び B で測る設計）。木の選択は無作為な入口より 0.8 ポイント良いだけで、**最良を選ぶのは 55%**（無作為 38%）。大きさは後悔の実測 0.033〜0.084 と同じ桁＝**`order_acc` の「兄弟手を並べられない」を勝率の側から見たもの**の可能性が高い（母集団が違うので足し合わせない）。**副産物**: 攻撃の入口が 2 つ以上あるのに **49% は攻撃を打っていない**（判定から外した）＝`attack_budget` のドン余り・殴らないターンと同じ場所で、攻めの取り逃がしの全体はこれより大きい。次は `k̄`（攻めのシャドー価格）を同じ計器で。
- `reports/2026-09-13_zero_sum_calib.md` — **零和の破れの正体＝手番でない席の評価だけが悲観**（同じ seed 帯 9400・32 局を r3／r5／r10 で）。r3 は**手番側の較正が +0.006（正しい）のに非手番側は −0.035**＝偏りは席の役割で偏っており、原因の候補は「訓練の行が決定点の席からしか作られない」こと。**r5 は破れていない**（−0.008 CI [−0.089, +0.050]）・**r10 が最も悪く両側が悲観**（−0.170・補助ヘッドを足したネット＝監視対象）＝**世代で単調ではない**。**前の点推定を訂正**: 同じ r3 が帯によって −0.138 と −0.058＝「片側 3.5 ポイント」は上振れで、r3 は片側 1.4 ポイント（3.5 は非手番側だけの値）。**実害は変わらない**——探索は窓で「そのとき手番でない側」の値を使う＝歪んでいるのはまさに畳み込みに入る値。直し方 2 案（反対称の正則化／非手番側の行を訓練に入れる）はユーザ決定。
- `reports/2026-09-13_readout_w32.md` — **波 32 の読み出し 7 本**（7,290 局に 7 計器）。**結論: 探索でも価値の符号でもなく、方策の「順序」が壊れている**——接戦帯の `order_acc(p,q)` は **0.5016（コイン）**で、そこで失う Q 0.070 は**実測された後悔の全量**（0.033〜0.084）に等しい。**手札 1 枚の価値の符号は 3 ネットとも正しい**＝`V_guard − V_take` の交絡は否定された（守りの読みは残る）。**DON 補正で落ちたのは 5.3 ポイント**＝「守れたのに受けた」は残る（89.4%／64.7% と読み替え・相手ターン中のドンは 0.55〜0.68 個＝カウンターイベントは構造的に打てない）。**ライフ予算は使われていない**（`θ` 平ら・`c(x)<c̄` を受けた 47.9%・`p<=0` の無駄な守りが 23%・払い過ぎ +0.406 枚）が**価格そのものは正しい**（実プレイの最小枚数がデッキ側の `c̄` と一致＝7000 理論が実測で成立）。**ハザードに形が在る**（帯ごとに 0.26〜0.63・同じ帯で `P(win|T=t)` が 0.12→0.53）＝時間軸ヘッドが学ぶべきは `h(t)`。~~**ドンを 2.32 個余らせる**（77.1% のターン・相手ライフ 1 で 4.79）~~＝**2026-09-14 に訂正**（`2026-09-14_don_accounting.md`）: 計器の誤りで、**真値は 0.51 個・76.9% のターンで使い切っている**。**公開情報からは読めない**（`lift` ≈ 0）＝混合は今は不要。帰結として**候補 F は「価格を方策の順序の教師にする」形へ組み直す**（着手はユーザ指示により最後）。
- `reports/2026-09-14_entry_gain.md` — **登場時の利得＝中盤と大型は勘定が閉じた・最弱帯は 39% しか埋まらない**（300 局・打たれた登場 2,608 件・`coverage` 1.000・記録だけ）。ユーザ指摘の一覧 1 番目（サーチ・ドロー）を測った。**設計の要点は勝率に回帰しないこと**——利得は他の通貨の増減として現れるので、動いた量を数えて既知の価格を掛ければ出る（選択交絡が入らない）。**先に器のバグが 1 つ**: **ステージ 124 件がパワー 0 で最弱帯に混ざり**、体を持たないのに `(Δ自場 − 1)·ν` を引かれて**利得の符号が変わっていた**（−0.0016 → **+0.0125**）。結果: **リーダー〜飽和は 0.0138 [0.011, 0.0165] が要る額 0.0158 をCI に含む＝勘定が閉じた**／**飽和超えは 0.0323 で 1.48 倍の超過**／**リーダー未満は 0.0125 で充足率 0.39＝0.0193 が未説明のまま残る**（**報告本文の「0.43」は訂正**——器の既定の要る額がステージを外す前の 0.0290 のままで、そこから割っていた。正しい要る額 0.0318 なら 0.39。未説明の 0.0193 は 0.0318 基準で書いてあるので変わらない）。**弱いキャラの利得はほぼ全部「札の入れ替え」**（`Δ手札` が −1 ではなく −0.769＝戻した 0.231 枚 × `μ` が利得の 8 割）＝**ユーザ指摘の 1 番目が弱いキャラの価値の主成分**だと数字で出た。残り 0.0193 の候補は 手札の「質」（`μ_search > μ`）／`ν` 自体の過小／**CPU の払い過ぎ**で、**向きが逆なので反事実でしか分けられない**。副産物: **ステージは枠組みに居場所が無い**（体が無いので `ν` で説明できず、継続効果が在るので登場時の利得だけでも説明できない）。
- `reports/2026-09-14_don_accounting.md` — **「ドンを余らせている」は計器の誤りだった＝CPU はドンを使い切っている**（同じ 300 局・3,739 自席ターン・記録だけ）。T17（`δ` の層別）に着手する前の下見で**前提が崩れ、代わりに T10 が閉じた**。**`ATTACH_DON` は選ばれた手として 1 件も無い**（300 局 約 1.9 万手）——**付与は全部 `DON_BOX`（巨視手）の中で起きる**ので、`don_idle = don − ATTACH_DON の行数 − PLAY のコスト` という再構成は**付与を丸ごと余りに計上していた**。**真値は 0.51 個／ターン・76.9% のターンで使い切っている**（旧 2.32／「1 個以上余るターン 77.1%」は**ちょうど裏返しの意味の偶然**）。付与は 2.94 個（旧 1.16）・相手ライフ 1 で余り 1.11（旧 4.79）。会計は閉じた（残差 −0.08）。**中盤 T5-8 はほぼ完全に使い切る**（0.14 個・余るターン 9.6%）で、余りが出るのは序盤（端数）と終盤（使い道が尽きる）。**`DON_BOX` が中の原始手を隠す罠は 3 度目**（`ATTACK` が無い・`don_k` が在るのに無いと報告・本件）＝対策は**状態が答えを持っているものを行動の再構成で出さない**。**帰結**: `δ` 過大請求の仮説は根拠を失い（ドンは希少）**`δ = 0.0277` は擁護された**／逆に**拘束条件なら影の価格は平均より高い**（請求は過小）可能性が生まれた／**過払いの最有力の説明は「CPU が払い過ぎ」に移り、反事実の封鎖が解けて最優先になった**。
- `reports/2026-09-14_bodyless_and_power.md` — **体を持たない札とパワーの軸＝勘定はもう一意に閉じない**（同日の `2026-09-14_entry_gain.md` の続きで**結論を上書きする**・同じ 300 局・記録だけ）。ユーザ指摘「ステージだけでなくイベントもそうじゃない？」から。**イベントは体ゼロ・場に残らない純粋な形**で、**`ν` が 1 つも混ざらない＝登場時の利得の較正に最も向いた帯**。同時に**理論がイベントを値付けできなかった理由が判った**——`score_candidate` が `None` を返すのは**「登場時の利得」の項が無かったから**で、効果が読めないからではない。イベント 337 件の役割は KO 53・**`reduce` 36**・**`lock` 18**・deck 4 で、**`reduce` と `lock` は 4 通貨 + `ν` のどこにも無い**。しかも**`scalars` の場の集約は印字パワー**＝**パンプはトークンの `power_now` でしか見えない**。**値付けは既に在る**（`δ` は「+1000 パワー = 0.66 枚 × μ」由来）のでパワー変化を `δ/1000` で足したら利得が 1.3〜1.4 倍になり、**2 帯が要る額を超えた**（リーダー〜飽和 1.39 倍・飽和超え 2.05 倍）。**制約 3 本に対して動かせるものが 3 つ以上ある＝「勘定が閉じた」は反証できない**ので**項を足す作業をここで止める**。**いちばん疑わしいのは `δ` の一様な過大請求**——イベント（−0.049）もステージ（−0.060）も純価値が負でキャラも全帯で過払い＝**種類を問わず払い過ぎに見えるなら共通の係数を疑う**。逆算すると上 2 帯が **0.0242／0.0245 でほぼ一致**（実測 0.0277 の 13% 減）で、**ドンを 1 ターンに 2.32 個余らせている**実測と整合する（余っているなら 1 個の機会費用は平均より低い）。最弱帯（含意 0.0124）だけは別の説明が要る。
- `reports/2026-09-14_nu_measure.md` — **`ν` の検算＝式は両端を切り落として真ん中を盛っている**（300 局・3,739 自席ターン・記録だけ）。**`ν` は式を組む前から実測値が在る**（Phase 1a の 0.1087）＝**どんな式も平均がそこに合わなければ間違い**。同じ within 推定を**多変量**に広げて**種類ごとの実測価格**を出した。**器の突き合わせ**: 1 本で 0.1230 [0.0985, 0.1475]＝**Phase 1a を CI に含む**。**パワー帯の検算**——**リーダー未満は実測 0.0690 [0.031, 0.107] でCI が 0 を含まない＝「価値 0」は間違い**（体数の 40%・これが「理論はターン終了・探索は登場」の直接の原因）／**飽和超えは 0.2112 > 式の 0.1860 かつ > 真ん中の 0.1503＝飽和点から上もパワーは効き続ける**（`ko_p` をパワー非依存にしている分）／真ん中の帯だけ式が +0.011 高い（複利にしていない過大がここに出る）。**帯ごとに符号が逆＝定数直しでは埋まらない・パワーに対する形そのものが違う**。ブロッカーの上乗せは大きさが偶然一致（どちらも +0.035）だが体数 0.115/ターンで**分解能不足**（`leader_to_sat_blk` は差 +0.001 でぴたり当たる）。
- `reports/2026-09-13_nu_calib.md` — **`ν` の較正＝当てずっぽうを実測に替えたら理論は少し悪くなった**（300 局・3,139 自席ターン・守りの窓 10,574・記録だけ）。**`block_p` は値ではなく「種類」が誤っていた**——**ブロックできるのはブロッカーだけ**なのに定数 0.3 を全キャラに掛けていた。実測は**ブロッカーなら 0.773・それ以外は 0**で、**ブロッカーは自場のキャラの 10.4% しか居ない**＝母平均 0.080 に対し旧定数は **3.7 倍の過大**（窓あたりの 0.034 はブロッカーが居ない相手ターンで薄まった別の量）。`ko_p` 0.289 [0.276, 0.302] 対 旧 0.25・`R` の代理は実測より 0.151 ターン低い。**較正すると `ν` は非ブロッカーで 21% 下がる**（0.164→0.130）＝**「`ν` が低すぎる」という私の仮説は逆向きに否定された**。**そして較正した理論は探索との一致が全帯・全指標でわずかに悪化した**（`q_recovered` 0.322→0.311／0.339→0.307／0.419→0.411）＝**より正しい定数で当てはまりが落ちるのは式が間違っている徴候**。診断: **旧定数 0.3 はたまたま「場に居ること自体の価値」の代理になっていた**——4 通貨の `ν` は攻める・ブロックする・KO されるの 3 つしか価格化しておらず、**身代わり**（相手がキャラを殴ればその攻撃はリーダーに来ていない）が抜けている。次は定数の較正ではなくこの項を式に足す。
- `reports/2026-09-13_theory_vs_search.md` — **接戦帯の食い違いを解剖する（300 局・ユーザ指示「事実の調査が先です」）**。**事実調査の最初の成果は器の誤り 4 件**: **記録に `ATTACK` は存在せず攻撃は `DON_BOX`（対象付き）で来る**のに付与の式で値付けしていた（**全候補の 44%**＝理論の中心が未検査）・印字パワーを使っていた・**付与枚数は `pol_k` 列に最初から在った**（仮定 1 に対し実測は**攻撃の 58% が k=0**）・**損の母数を食い違った行だけにして理論に不利に偏らせていた**（同じデータで −0.51 → +0.32 と符号が反転）。**うち 2 件は結論の符号を変えるもの**。**確定値: `Phase 1b` の「接戦で勝ち決着で負ける」は再現しなかった**——`q_recovered`（無作為 0・探索の結論 1）は **close +0.322／mid +0.339／decided +0.419** で全帯で正・比率は単調増加＝**`|v0|` 重み付けの設計根拠は消えた**（ただし絶対値では接戦帯が最大＝0.0246 対 決着 0.0109）。**いちばん強い事実は非対称**: `th_recovered` は全帯 ≒ 0＝**探索は価格を見ていない**のに価格は探索の良し悪しを部分的に当てられる＝**価格は探索が持っていない情報を持っている**。食い違いの 8 割は「殴るか、殴らずに支度するか」（理論「攻撃」対 探索「付与だけ」が接戦 39.3%）で、**理論「ターン終了」対 探索「登場」12.0%** は `ν` が低すぎる＝**理論側の欠陥**を名指しする。**理論は 35% の判断点で無言**（価格は飽和すると同値を量産する）。
- `reports/2026-09-13_phase1_w32.md` — **Phase 1＝理論の価格は当たっていた。だが入力に足す価値は無い**（計器 3 本・波 32 の holdout 1,041 局・出荷既定 r3・ユーザ指示「ゲーム理論と対照して何が足りないか検証し、補助ヘッドまたは入力パラメータを足して再学習する」への回答）。**(1a) ネットは 4 通貨を正しい水準で値付けしている**——観測の傾きは実測と一致（`μ` 0.0453/0.0433・`λ` 0.1165/0.1158・`δ` 0.0291/0.0277・`ν` 0.1039/0.1087）。**`δ` の理論値 `Δpressure(1000)·μ` = 0.0286 は実測と −3% で一致＝自由パラメータ無しの予測が当たった**（`ν` は自由パラメータ 3 つなので整合のみ）。**場のキャラ 1 体 ≈ ライフ 1 枚 ≈ 手札 2.5 枚**。応答（反事実）は集約列を据え置く下限で、枠も消せば `μ` は真の 0.94 まで埋まるが `ν` は 0.28 止まり＝残りは自場集約 3 列の持ち分。**(1b) 理論の順序は方策が壊れている帯でだけ勝つ**——接戦 0.5259 対 0.4975（+0.0284）・中盤 +0.0245 に対し**決着帯 −0.0229**・全体は互角＝**価格は方策の教師にするが `|v0|` で重み付けする**（当時は付与枚数を仮定していたので判定が 0.5165↔0.5407 と動いた。**この仮定は不要だった**——`don_k` は `pol_k` 列に最初から記録されている・`cpu_theory_gap.md` §7.3 訂正 1）。**(1c) 理論の状態量 23 個は残差を 1 つも説明しない**（全部 `|β|·sd < 0.01`・有意ゼロ）＝**入力列を足す線は捨てる**。突き抜けたのは未来の 2 量だけ＝**`life_spent` 0.163（β −0.147・`band_r2` 0.57）／`life_taken` 0.162** に対し **`t_real` 0.0067**＝**r10 の時間軸ヘッドが学んだ量には説明力がほとんど無い**（アリーナが動かなかったことと整合）＝**同じヘッドで学ばせる量を替える**。**外れていたのは理論ではなく「ネットがその計算を持っていない」という想定**——ネットは水準を知っていて、知らないのは資源の流れ。
- `reports/2026-09-12_plan_head_r8.md` — **方針ヘッド r8（2026-09-12・WP `rs-plan-aux`・V(盤面, 方針) を 5 列の補助ヘッドに・不採用）**。r7 との差は `--plan-weight 0.1` と方針ラベルの sidecar だけ。**補助ヘッドは無害**（3 波の行列で r7 との差は v_mse ±0.004・top1 ±0.003 の内側）だが**アリーナは動かず微減**（主 0.5000・Elo −0.0／副 0.4896・Elo −7.2・r7 は +25.4／+16.3）。**成果は地図**: 較正が合ったうえで**両方の圏で V_face − V_board +0.20〜0.26・argmax_face 0.80**＝手計算の「殴れば 0.81・見送れば 0.45」をネットが再現、**なのに打った face は 0.62**・守りは argmax_guard 0.78 に対し played_guard 0.40＝**ネットの評価と木の打ち手がずれている**（π と探索の側の問題）。**新発見: 守りどきはライフ 3（ライフ 1 では V_guard − V_take が負）＝現行の「終盤ほど受ける」と逆**。全体の agree 0.38〜0.41 が低いのは差が無いから（gap>0.1 は 1 割）＝方針が効くのは両方の圏と序盤 t1〜4 だけ。次: 地図を対局で使う／反事実の教材／守りの時期を単独検証
- `reports/2026-09-12_plan_drift.md` — **自己対戦の「方針」の計測（2026-09-12・`plan_drift.py`＋`race_state.py`・波 28／30／31／32・27,466 局）**。ユーザの仮説「π が平らな分岐で方針を持たずブレる」に対し: **方針は持ち越されていない**（分岐点で前ターンの方針を選ぶ率＝無状態の期待値 +0.03・探索の argmax は期待値以下）は当たり。**ブレと負けの相関は 4 波で一貫（敗者が多い 0.56〜0.57）だが序盤に無い**＝逆因果と区別できず因果は不支持。終盤の規則（ユーザの①②）: **両方の圏（お互いにリーサル圏）で r3／r5 は 30% 殴らず、殴れば勝率 0.81・見送れば 0.45**＝勝敗に直結する唯一の箇所。リーサル圏だけの見逃し 30% は相手手札で選別している面あり。守りは「ライフ 1 まで受ける」で平ら・逆算の形なし。r5 は r3 の写し。次: 見送り局面を深い探索で再判定
- `reports/2026-09-12_policy_band.md` — **方策の同一行比較（2026-09-12・a1／r3／r5／r6 × 波 28／30／32・`n_rel_band --policy`）**。同じ holdout 行の 4×3 行列。**方策は現在の分布では劣化していない**（波 32 で単調改善）が、**忘却が世代ごとに深い**（波 28 の top1 が r3 0.631→r6 0.498＝a1 以下）。3 波の平均で r6 は r5 より良くない。**候補が基準に勝った波の数（3/3・2/3・1/3）がアリーナ（+81・+3.5・0）を当てる**＝最新波だけの評価帯は候補を地元で測っていた。機構の仮説は π の窓（現 era のみ・2 波）による特化＋忘却。提案: π を直前 era まで広げた r7／評価帯は 3 波の行列で読む／r6 の高分解能アリーナは不要
- `reports/r6_judgment_20260911.md` — **r6 の判定（2026-09-11・era8 初回・生成役 r5・規約上は非昇格）**。r6＝warm-start r5・π 波 31+32（era8・28 シャード 13,440 局）・z 6 波・lr 2e-4・best ep0。評価帯は波 32 の同じ行で v_mse 0.5263→0.5115（−2.8%）と**また改善**したが、アリーナ主 0.5132 [0.458, 0.568]・+9.1 Elo＝未達／**副 0.474 [0.428, 0.520]・−18.1 Elo＝CI 下限が関門 0.45 を割った（NRel で初）**。要点: 「V は伸びる・打ち手は動かない」が r4→r4q→r5→r6 の 4 世代続き、**生成役を替えれば伸びるという era8 の仮説が 1 回反証された**。次は (1) `n_rel_band --policy` で同じ行の方策比較 (2) r6 の高分解能アリーナ (3) 探索側。符号化の列追加と教材の積み増しは見送り推奨
- `reports/2026-09-11_r5_agreement.md` — **r5 は r3 と違う手を打つか（2026-09-11・§20.9.9）**。同じ盤面・同じ探索 seed で decide を比較（生成設定・40 局・2,435 判断点）: 一致率 0.589（r3 vs r3 の seed 違いは 0.825）・除去が合法な局面 0.535・除去を選ぶ率 0.145→0.166（終盤 0.110→0.155）。違う手を打つが平均すると同程度＝生成役を r5 にすれば教材は更新される
- `reports/2026-09-10_q_pi_judgment.md` — **π' 教師（r4q）の判定（2026-09-10・§20.6.3・不採用）**。新分布の z 予測は r4 より 0.002 良く π' は学べるが、門は開かず（除去候補の P 質量 0.2595 → 0.2529）、同じ物差し（synth_roles）で r4 0.5182・r4q 0.5182＝強さ同一、旧分布の忘却は r4 より大。総括: 探索設定・教材・補助教師・学習率・教師の作り方のどれも +13 Elo を超えず、共通の壁は V が除去の後の盤面を評価できないこと。次は符号化 v14・容量・教材 2 波
- `reports/2026-09-10_removal_world_census.md` — **除去を打つべき展開は起きているか（2026-09-10・合成 vs 実デッキ・§20.8.9）**。除去が合法な main 決定 41〜44%・探索が除去を最良と読むのは 15%・キャラ打点の比率 57〜63%・ε の対照で「木の判断に反して除去を強制すると z が −0.06〜−0.07」＝世界は原因ではなく、この CPU は除去を打っても得にできない（除去後のテンポを実現できていない）。合成は実デッキより除去の価値が出にくいが主因ではない。`--decks user`（ユーザの 4 デッキ・census 専用）と `forced_sig` 列を追加
- `reports/r3_adoption_20260908.md` — **r3 採用報告（2026-09-08）**。出荷既定と生成役を a1 から **r3**（同じ構成・波25〜28 で warm-start 再訓練）へ＝era7。根拠は主条件 0.615 [0.560,0.669]・Elo +81（N 系最大）と副条件 0.510＝退行なし。規約は「主条件達成＋副条件は退行なし（CI 下限 ≥0.45）」に読み替え（ユーザ決定・CLAUDE.md に追記）。ロールバックは `DEFAULT_NET` を a1 へ
- `reports/a1_adoption_20260905.md` — **a1 採用報告（2026-09-05）**。出荷既定を N系 c10 から **NRel a1**（R なし・相手デッキ知識あり・符号化 v13・`nrel_a1.npz`）へ。根拠は r1 vs c10 の 2 条件達成と a1 vs r1 同等・レイテンシ c10 比 1.67 倍はユーザ受容。生成役も a1（era6）・訓練は `--ablate rel` が既定・ロールバックは `_C10_VALUE`
- `reports/2026-09-05_r1_ablation.md` — **r1 の要因分解（2026-09-05）**。同じ教材・同じ訓練で 3 本（a1=関係 R なし・a2=相手デッキ知識なし・c12=c10 を同じ教材で再訓練）を r1 と主条件アリーナで直接対決: a1 0.544 [0.487,0.601]・a2 0.503 [0.448,0.557]＝**R と相手デッキ知識は強さに寄与せず**／c12 0.430 [0.378,0.482]＝**構造（枠トークン＋状態 S）が寄与**。評価帯・一致率・レイテンシの補助指標つき
- `reports/2026-09-07_train_torch.md` — **NRel 学習の torch（CPU）化（2026-09-07・WP `train-torch`・`rust_engine_plan.md` §18.4）**。value と policy の両方を torch で書き、numpy の手書き backward は `--backend numpy` の参照実装として残す。1 シャード 1 エポックの学習ループが **146.7s → 34.7s（4 コア・4.22 倍）**・1 スレッドでも 2.29 倍（計測 WP の予告 2.28 倍と一致）。forward 一致 1.9e-6／勾配は正規化 2.86e-6 で **numpy 自身を加算順だけ変えて回した誤差 3.03e-6 より小さい**（指示書の「相対 1e-4・|g|>1e-6」は float32 の丸めの下限より下にある基準で達成不能＝下限を実測して並べた）。学習の等価性は warm-start で v_mse 0.53%・p_loss 0.05%（ゼロからの 1 エポックは混沌が支配し、**numpy 同士でも BLAS のスレッド数だけで 2.44% ずれる**）。**npz は無変更**＝Rust の `load_net`＋`net_eval` が 5.4e-7 で一致。4 コアが予告の 5.33 倍でなく 4.22 倍なのは、エポック全体には numpy のままのバッチ切り出しが残るため
- `reports/2026-09-07_dump_f16.md` — **dump v3（float16/int16）と memmap 読み（2026-09-07・WP `dump-f16`・`rust_engine_plan.md` §18.5）**。生成は **dump v3 が既定**（tokens/scalars を float16・card_idx を int16＝cast しただけ・`enc_version` 13 のまま）、学習は `learned/train/dump_io.load_dump` が波ごとに pack を作って **memmap で読む**（V を RAM に載せない）。**2 波 218,502 行の常駐 0.524GB → 0.035GB（6.6%）**・V は 2,432 → 1,176 B/行。1 エポックの val v_mse 差 0.09%・学習ループ +4.6%（読み込みが速くなるので通しでは −8.7%）。**v2 の npz も同じ関数で読める**。npz のディスク上は 8% しか縮まない（zlib が既に潰している）＝利得は RAM。**注意**: 全波規模ではページキャッシュに乗らず実 I/O になる／fp16 の tokens から R を再計算すると `feasible` が 0.05% 反転する（現行線は `--ablate rel` で無影響・serve は無関係）
- `reports/2026-09-05_r1_judgment.md` — **r1 vs c10 の判定（2026-09-05）**。分散アリーナ 16 セッション（条件×シャード）×24 ペア: 主 0.557 [0.503,0.611]／副 0.591 [0.547,0.635]・void 0＝**2 条件とも達成**。NRel Stage A の初回で c10 を超えた。出荷既定への切替・生成役交代はレイテンシ 2.0 倍の受容と合わせてユーザ判断
- `reports/2026-09-05_r1_first_signal.md` — **r1（NRel Stage A 初回）の先行シグナル（2026-09-05）**。アリーナ前の補助指標: 評価帯（同じ holdout 38,164 行）v_mse c10 0.556→r1 0.531・全ターン帯で改善／人間リプレイ h1〜h6 一致率（seeds 4・sims 160・317 点）0.322→0.355（PLAY・ACTIVATE_MAIN が伸び ATTACK はやや下がる）／レイテンシ sims 160 で c10 比 2.0 倍（関門 1.3 倍は未達）
- `reports/2026-09-04_human_samples_divergence.md` — **見本棋譜と c10 の選択乖離（数値・2026-09-04）**。メイン窓の決定点で c10 が人間と同じ手を選ぶ割合: h1 0.41／h2 0.21／h3 0.28／h4 0.26。不一致の最多は PLAY→ATTACK（払う場面で殴る）・PASS→SELECT_COUNTER（ライフで受ける場面でカウンター）・ACTIVATE→ATTACK。守りの決定点 9 点は一致 0。計器の訂正 2 点（攻撃の対象欠落・復元不能な効果対話の除外）を含む
- `reports/2026-09-04_human_samples_qualitative.md` — **見本棋譜の定性分析（設計フェーズ・2026-09-04）**。ユーザ採取の人間 3 局（エネル vs ルフィ／ロー vs ルフィ／エネル vs ロジャー・全勝）から手順の型を抽出: 型A 減算＋しきい値除去の 2〜3 枚コンボ／型B ドンを 1 ターン内で循環（払う→補充→付与→殴る）／型C 順序の原則／型D ライフを資源に守りは最後に集中（ライフ 0 で 3 ターン戦って勝つ）／型E 手札の役割分担と保護。次の本体（注意機構）への要件 5 点（対の差分入力・条件充足フラグ・資源の時系列・守り手札の可視化・方策が組を先に出す）
- `reports/2026-09-03_c10_vs_g15_formal.md` — **c10 vs G15 正式測定（2026-09-03）**。8シャード×24ペア×2条件・主 **0.633 [0.580,0.686]**／副 **0.589 [0.541,0.637]**＝2条件とも昇格基準達成・void 0。c10 の既定採用（PR #205）を事後に確定。24ペアの参考値（0.604/0.583）が192ペアで再現
- `reports/c10_adoption_20260903.md` — **c10 採用報告（2026-09-03）**。出荷既定を G15 から **N系 c10**（効果構造符号化・カードID埋め込みなし）へ。判定＝分散アリーナ c10 vs c8 主0.589[0.534,0.643]/副0.568[0.525,0.611]（昇格）× c10 vs G15 24ペア×2条件 0.604/0.583（参考・正式な8シャード測定は別途）。serve 配線は `opcg_sim/learned/n_eff.py`（forward の正本）＋ `LearnedEngine` の npz 鍵判別。decide レイテンシは G15 比 約1.7倍（同一盤面5点）。ロールバックは既定を G15 ペアへ戻すだけ
- `reports/gen15_adoption_20260815.md` — **gen15 採用報告（2026-08-15）**。符号化 **v12**（=v9＋リーダー物理要約24・リーサルΔは serve コスト 25ms/盤面＝decide 13.5s で外した）の本体＋戦闘出口ヘッド載せ直し。**歴代初のアリーナ昇格**＝225ペア450局 **0.5756 CI[0.533,0.619] Elo+52.9**（帯別 0.595/0.593/0.546/0.565）×コーチゲート20点 PASS（11.9 vs 11.1・退行ゼロ）×ns2 中間帯 +0.709（G14 +0.455）×decide 0.49s（予算内）。寄与＝リーダー物理要約（ユーザ提案）／B・G混合コーパス（合成リーダー世界が未見リーダー過適合を治癒＝B系の意義を G系の数字で実証）／入口コミット（ユーザ決定）／ヘッド載せ直し。ロールバックは既定を gen14 へ戻すだけ
- `reports/battle_commit_20260815.md` — **入口コミット（2026-08-15 ユーザ決定）**。防御窓のプランを入口で1回立て以後は実行のみ（人間はカウンターを切った後に考え直さない）。初回A/B 0.320 は採取側 `box_depth=0` で評価と実行の継続規約がズレた実装バグ＝修正後 **0.520 CI[0.451,0.589]（同等）×31%高速**で既定 ON。「払い始めたら払い切る」を構造で保証（m1@15 型の折衷ラインが原理的に出ない）。`SERVE_BATTLE_COMMIT=False` でロールバック
- `reports/gen16_negative_20260815.md` — **gen16 不採用（負の結果・2026-08-15）**。オラクル不要の不変量監査（ユーザ指摘「過去バージョンでは今わからない問題は検出できない」への処方）が**世代跨ぎの実在欠陥**を検出（相手のパワー/ドン↑を自分に有利と誤読 39/41%・零和バイアス +0.113・ゲートでもアリーナでも ns2 でも未検出だった）。後付けの単調性順位学習で破れは 13/23% へ、ns2 接戦帯も +0.709→+0.761 と改善、ゲートは退行ゼロだったが、**アリーナで有意に劣化**（225ペア450局 0.4533 CI[0.415,0.492] Elo−32.5）＝不採用。教訓＝**代理指標（ns2）の改善はアリーナの代わりにならない**（v40 に続く2例目）／論理的に正しい教師でも実戦を弱くしうる（順位ヒンジは目盛りを歪める・教材盤面が分布外）。監査計器自体の価値は残る（失敗したのは後付けの処方）
- `reports/arena_random_leader_20260816.md` — **アリーナ再測定（ランダム対面×生成デッキ・2026-08-16）**。歴代の判定は両者ハンニャバル固定ミラー1条件だけだったので、137リーダーから引く対面＋リーダーに合わせた合成デッキで測り直した。**判定A: gen16 vs gen15 は 313ペア wr 0.4521 CI[0.4087,0.4954]＝旧判定 0.4533 と一致し不採用が確定**（条件が狭かったから負けた、という留保が消えた）。**判定B: gen15 vs gen14 は 177ペア wr 0.5198 CI[0.4608,0.5787]＝新条件では有意でない**（採用時は固定ミラーで 0.5756 CI[0.533,0.619]）。「汎化しなかった」のか「検出力が足りない」のかは177ペアでは分離できず、旧条件での再測定が分離実験。void（対局が成立しないペア）20件の扱いとリーダー別内訳も記録
- `reports/void_root_causes_20260816.md` — **void の根因＝交差対面でしか出ない3欠陥（2026-08-16）**。seed 単位の再現は CPU の選択が変われば消えるので、生成デッキ監査を交差対面へ広げて面で測った（240件で修正前 ok=237/hang=3・ミラー監査は ok=137/hang=0＝**ミラーでは1件も出ない**）。(1) ドン!!を対象にしたフリーズで対局が落ちる（OP10-033・FREEZE は カード専用で `DonInstance` に flags が無い）、(2) 継続効果の再計算がコスト確認の対話を出す（OP09-080・「離れた時」が反応型と判定されず613回連続）、(3) 状態を変えるコストが「既にその状態」でも払える（OP15-099・297回連続。レスト限定だった規則を `_cost_state_noop` へ一本化）。併せて戦闘箱の枝予算で「1手が終わらない」暴走も有界化
- `reports/gen15_recheck_20260816.md` — **gen15 の採用根拠は再現しない（2026-08-16）**。「歴代初のアリーナ昇格」0.5756 CI[0.533,0.619] に対し、現HEADでの3測定は **ランダム対面 0.5198 / 固定ミラー新seed帯 0.5028 / 固定ミラー71000帯（採用時と同じ帯）0.5222** で**いずれも昇格基準（wr≥0.55・CI下限>0.50）を満たさない**（束ねて 0.515 CI[0.487,0.543]・582ペア）。原因は「採用時の標本が有利だった」か「08-15以降のエンジン変更で優位が消えた」かで、本測定では分離不能（分離には当時のコミットでの再走が要る）。**既定は gen15 据え置き・ただし gen15 は gen14 と中立として扱う**。運用変更＝昇格には条件を変えた2本以上の測定を要求する／エンジンを触ったら過去の昇格証拠は失効しうる
- `reports/void_zero_20260817.md` — **アリーナ void 20件の全滅＋測定運用の改定（2026-08-17）**。残っていた「時間上限に当たる型」を追い切り、**判定A 17/17・判定B 3/3 が成立**（判定Aは gen16 が`tests/fixtures/candidates/gen16/` に残っていたので当時のネットで再生できた＝前報の「再生不能」は誤り）。追加の2欠陥＝**自己参照コストが別カードで払える**（P-081 ほかDB全体で96節・OP02-035/P-074 は**相手のキャラ**を手札に戻して払っていた実害つき）と**イベントが場に登場する**（ST31-002→ST01-016・場のイベントの起動メインを426回連続で空振り）。この2種は**手数では捕まらず時間上限としてしか現れない**＝void を放置すると永久に見つからない型。併せて `make audit-cross` の常設ゲート化と昇格判定の要件（条件2本以上／判定はコミットに紐づく／void率2%超で判定保留／候補ネットを消さない）を TEST_SPEC・CLAUDE.md に固定
- `reports/cpu_v49_dig_injection_20260811.md` — **v49: 掘り裁定の注入と50ペア注入のトリレンマ**。勝率CF教師の不成立を実測（32世界で両腕0/32＝飽和・ブートストラップ問題）→裁定注入へ。6腕対照で「エネル掘り獲得（B腕・ゲート h1@2 0→1）／漏れ抑制（見送りペア→防御較正破壊）／ナミ逆裁定（対照ペア→破壊拡大）」は同時に買えないと確定。犯人切り分け＝見送りペア×防御較正の衝突（v32同族）。サトリ移植プローブ（ユーザ発案）新設。B腕: ゲート9/9・アリーナ0.5038 CI[0.473,0.534]中立・採否ユーザ判断待ち

> 実装中の設計計画（一部未完・実装完了後に SPEC/TEST_SPEC へ吸収）: [`replay_verification_plan.md`](replay_verification_plan.md)（実対局リプレイ検証 R0-R3 実装済＋残少）／[`cpu_perf_testing_plan.md`](cpu_perf_testing_plan.md)（CPU 性能テスト運用 A1-A3 実装済）／[`cpu_v5_plan.md`](cpu_v5_plan.md)（**v5 学習計画**＝v4実測14マーク＋v4採用報告§3残課題(D1-D3)の統合。攻守の程度較正・守りすぎ過補正・ドン付与双方向誤り・負けq飽和。防御温度の条件化＋マーク局面シード＋忘却抑制(KL蒸留)＋自デッキ残encoder拡張＋v4温スタート）／[`refactoring_harness_driver.md`](refactoring_harness_driver.md)（検証ハーネス共通ドライバ化 ⑥）／[`cpu_v8_plan.md`](cpu_v8_plan.md)（**v8 教師CPU本線**＝真盤面レフェリー・プラン自動列挙・同価値バンド・捲りモード）／[`cpu_v9_plan.md`](cpu_v9_plan.md)（**v9 外部アンカー学習**＝レフェリー再ラベル）／[`cpu_v9_ops.md`](cpu_v9_ops.md)（**v9 学習ループ運用手順書**＝ワーカー運用・監視・学習イテレーション・ゲート運転・採用手順の正本）／[`cpu_backbone_plan.md`](cpu_backbone_plan.md)（**骨組み価値ネット計画**＝リーダー非依存・合成カードのドメインランダム化による基底事前学習。ユーザ提案 2026-08-11・Phase0/1 スパイクの Go/No-Go から）。
- `n_attention_plan.md` — **N系 v3「対と手順」本体（NRel）の設計計画（2026-09-04 起案・生き文書）**。見本 6 局から逆算: 関係（対の差分）は符号化側で計算・条件の充足はエンジンの真偽・ID 非依存を維持・Stage A（対 MLP＋プール）→ Stage B（注意）の段階。入力（トークン状態 S・関係 R・グローバル追加列）／ネット／dump v2／判定（レイテンシ c10 比 ≤1.3・見本一致率・分散アリーナ）／段取り P0〜P4

## クイックスタート

```bash
make test    # テスト（全カード構造不変条件・挙動ベースラインも含む。コマンドの正本は Makefile）
```

詳細な検証フローは [`TEST_SPEC.md`](TEST_SPEC.md) を参照。
