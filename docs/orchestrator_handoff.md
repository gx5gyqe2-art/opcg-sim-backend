# 司令塔 引き継ぎ書 — 学習CPU 分散実験（2026-07-08）

新しいセッション（別コンテナ）が**司令塔（監視・測定・グラフ・判断）**を引き継ぐための文書。
「このリポジトリの `claude/opcg-cluster-learning` を checkout し、`docs/orchestrator_handoff.md` を読んで司令塔を引き継いで」で渡せる。

> **あなた（新司令塔）の役割**: 自分では重い自己対戦を走らせず、**別セッションで走っている各runを git 経由で監視**し、
> 節目で**強度測定**を行い、**リーダー数スケーリングのグラフ**を作り、**続行/打ち切りを判断**する。
> 測定は自分のコンテナのCPUで走るが、各訓練runは別コンテナなので競合しない。

---

## 1. 目的とこれまでの結論（背景）

**目的**: 出荷CPU（学習ネット v1・`opcg_sim/data/learned/gen2_*.npz`）を超える学習CPUを作る。
出荷v1の実力＝**対製品L1（α-β+PIMC4）で多様デッキ 0.833**（紫デッキ単体では 0.750）。

**確定している結論**（すべて実測）:
- **v1本走（成功・過去）**: 弱いSLネット（対L1 0.45）から**単一デッキ**で自己対戦 2世代(2万局)→ 出荷Gen2（対L1 0.925）。
- **v2本走（失敗）**: 出荷強ネットを温スタート＋137リーダーrotate で再訓練 → **劣化**（出荷v1に負け）。
  主因＝**出発点が既に天井**（強いネットを弱いsims40自己対戦で訓練すると引き戻される）＋**137希釈**。
- **案A（弱出発・97リーダー・進行中）**: v1と同じ**弱い出発点**に戻し、97リーダーで連続学習。Gen1(1万局)=対L1多様 **0.433**。
- **案B（色クラスタ・進行中）**: 6色に分けて**狭い分布**で並列学習（v1的な速い climb を狙う）。
- **リーダー数スケーリング（pool6/12/24/48・未起動）**: 色を交ぜてリーダー数を変え、「1リーダーあたり局数→強度」の曲線を描く実験。

**現アーキは「達成可能上限に近い」が繰り返し実証されている**（`docs/reports/cpu_strength_plan_20260628.md`）。
RL・sims増・eval微修正はいずれも純増を出せなかった。

---

## 2. 最新の重要結果（2026-07-08）

**紫クラスタを紫デッキで対L1測定**（2点目を 2026-07-08 に司令塔が追加）:
| ネット | 局/リーダー | 対L1（紫デッキ・sims160・pimc4・N=24） |
|---|---|---|
| 出荷v1（バー） | — | **0.750** [.551,.880] |
| 紫クラスタnet（cum 15,420） | 640 | **0.479** [.296,.668] |
| 紫クラスタnet（cum 19,620） | **813** | **0.542** [.351,.721]（13-11/24・所要72分） |

→ **狭いクラスタで濃く訓練しても L1 互角止まり・出荷v1に大きく届かず**。97汎化Gen1(0.433)から6倍濃くして+0.05のみ
＝**濃縮の効きが弱い＝希釈だけが原因ではなく、アーキ天井が本質**の可能性が濃厚。この方向は**打ち切り寄り**の暫定判断。
- **2点目（813局/リーダー）の追加所見**: 640→813（+27%濃縮）で 0.479→0.542。**+0.063 だが両点のCIは大きく重複＝傾きはほぼフラット**。
  出荷v1（0.750）には依然遠く、打ち切り寄り判断を補強。ただし測定1本=約72分と重く、N=24でCIが広い点は割り引く。
ただし各runはまだ climb 途中なので、**リーダー数スケーリングのグラフ（pool版）で傾きを確認してから最終判断**するのが筋。

**6色一括 対L1測定**（2026-07-08 司令塔が全色を同一条件で測定・各色は自分の色デッキ・sims160/pimc4・N=24）:
| 色 | N | 局/リーダー | 勝率 | 95%CI | 対L1 |
|---|---|---|---|---|---|
| 赤 | 31 | 664 | **0.542** | [.351,.721] | 互角圏 |
| 紫 | 24 | 813 | **0.542** | [.351,.721] | 互角圏 |
| 黒 | 22 | 835 | **0.542** | [.351,.721] | 互角圏 |
| 黄 | 24 | 640 | 0.375 | [.212,.573] | 及ばず |
| 緑 | 23 | 634 | 0.312 | [.164,.512] | 及ばず |
| 青 | 27 | 538 | **0.208** | [.092,.405] | 及ばず |

→ 訓練後は 赤/紫/黒＝0.542（L1互角）／黄/緑/青＝0.21〜0.38（L1劣後）。青が最弱。**ただし下のgen0切り分けで「色相性」説は棄却**。
- **総括**: 最良の赤/紫/黒でも 0.542 で **出荷v1(0.750)には全色届かず** → §10「打ち切り寄り」を全色で裏取り。
- **構造チェック（発散なし）**: 全色 value.npz は NaN/Inf=0・ノルムは gen0(L2=38)→80〜90 で約2倍に収束、局あたり重み移動は最も進んだ紫が最小
  ＝「発散」でなく「プラトーへの収束」。

**gen0切り分け＝出発点(共通弱Gen0)を各色デッキで対L1測定**（2026-07-08 司令塔・同一条件・N=24）:
| 色 | gen0(出発) | 訓練後 | Δ |
|---|---|---|---|
| 赤 | 0.292（最弱） | 0.542 | **+0.250 ↑** |
| 黄 | 0.542（最強） | 0.375 | −0.167 ↓ |
| 緑 | 0.438 | 0.312 | −0.126 ↓ |
| 青 | 0.417 | 0.208 | −0.209 ↓ |

→ **「弱群は色相性」説は棄却**。青/緑/黄のgen0は0.42〜0.54と健全で、**自己対戦が積極的に引き下げた**（劣化）。赤だけ最弱出発から+0.25で伸びた。
- **核心の像＝sims40自己対戦のアトラクター（平均回帰）**: 出発点で並べると（赤< 青< 緑< 黄）**最下位の赤だけ上昇・他は全部下降**。
  訓練は出発点がどこでも net を共通バンド(~0.4–0.54)へ引き寄せる。**gen0がバンド未満（赤）→上げ、超（黄/緑/青）→下げ**。
- **＝「ネットは自分の教師を超えられない」**: 教師信号＝sims40自己対戦の強さが評価バーL1(α-β+pimc4)と同等以下。汎用化＝多デッキ平均は
  「大半が既に天井以上」を平均するので天井へ引き下げられる。**v2失敗（強出発を弱自己対戦で全面劣化）を色ごとに再現**したもの。
- **レバーの含意（更新）**: 第一のボトルネックは**容量より「訓練時サーチ＝教師の弱さ(sims40)」**。効くのは**訓練simsを上げる/より強い探索・相手で自己対戦**。
  ただし過去「sims増は純増なし」との整合を要確認（訓練時か評価時か・範囲）。この gen0 所見は訓練時sims＝教師強度が本丸だと指す。
- 留保: 各色N=24でΔ単体はノイズ内。だが**4色を貫く「高い出発点ほど下がる」反相関は系統的**で頑健。紫/黒のgen0は未測（赤対照で十分と判断）。

**原因分析（機構・コードレベル 2026-07-08 司令塔）** — `value_net.py`/`encoder.py`/`p3_run.py`/`deckgen.py` 読解＋重みブロック診断:
1. **アーキ天井の中身＝表現階級の欠陥3つ**（ValueNet = 1隠れ層MLP・hidden128・入力120=scalars16+field80+emb24）:
   - **カード埋め込みが平均プーリング（bag-of-cards）**: カードの組合せ/シナジーを構造的に表現できない。「持ってるカードの平均的な良さ＋スカラー
     ヒューリスティック」しか学べない。推論部の実容量は W1+W2 ≈ **15.5k**（79kの大半は埋め込み表 2652×24）。
   - **マッチアップ条件付けが構造的に希薄**: リーダーIDは22スロット平均の2枠＝プール後1/10以下に希釈され、120次元入力の24次元スライスに埋もれる。
     97（や24）デッキを弾き分ける value をこの関数族は実質表現できない。**v1（単一デッキ）は条件付け不要**だったから同じ器で0.925に届いた。
   - field はスロット固定順の平坦連結（順不同不変性なし）＝同一盤面が並び順で別入力になり、さらにデータを薄める。
2. **教師信号の質**: value 教師＝sims40＋ルートDirichlet(ε0.25)＋序盤8手温度サンプルの**着地勝敗**＝分散大。policy 教師＝40訪問の分布＝ほぼargmax
   でノイジー。**gen0はpolicyを持たない(uniform)・訓練後は学習policyを持つ**→「学習policyがuniformより有害」の可能性を青でプローブした:
   **結果＝policy犯人説は棄却・valueが本体**。青の 訓練後value＋uniform policy ＝ **0.167** [.067,.359]（フル0.208と同等・gen0の0.417に戻らない）。
   劣化は value ネット自体に刻まれている＝bag-of-cards では青の「カード条件分岐だらけの真の価値曲面」を表現できず、sims40着地勝敗への回帰が
   gen0の汎用ヒューリスティックを**上書きして壊す**（表現不能ターゲットへの回帰＝負の転移）。アーキ第一原因説の最終確認。
3. **【前提の訂正】色クラスタは実は狭くなかった**: `OPCG_LEADER_COLORS=赤` は**その色を1つでも含む**リーダー（2色リーダー込み）を拾い、
   `build_realistic_deck` はリーダーの**どれかの色に合う全カード**＋不足時は**色不問補完**でデッキを組む。実測: 赤クラスタでも**全2652種中2545種
   （96%）のカード埋め込みが更新されていた**＝カード空間ではほぼ全域を見ている。案Bの「狭い分布で濃く」はリーダー空間でしか成立しておらず、
   **カードあたりデータ密度は v1（固定50枚×2万局）の数千分の1のまま**。
4. **色特性の説明**: 赤/紫/黒（テンポ・パワー計算系）＝スカラー特徴(パワー/ドン/ライフ数)で読める＝この器の均衡が高い(0.542)。
   青/緑/黄（カウンター・バウンス・トリガー系）＝カード固有の条件分岐が本体＝bag-of-cards では見えず、しかも L1(α-β探索)が得意な領域＝均衡が低い
   （青0.208は gen0(0.417) 未満まで沈む）。**自己対戦は各色を「そのアーキの均衡」へ引き寄せる**——これが平均回帰の正体で、共通バンドではなく色ごとに
   均衡が違う（アーキとの相性で決まる）。
5. **総合**: ボトルネックの序列は **(1)関数族（bag-of-cards・条件付け不能・15.5k推論容量）→(2)教師の質（sims40+ノイズ着地・40訪問policy）→(3)データ
   希釈**。局数追加は均衡を動かさない（紫640→813フラットで実証）。動かすなら**アーキ変更（カードペア相互作用・リーダー条件付け・順不同プーリング・
   容量増＝torch移行）**が第一レバー、次点で訓練時sims増（教師強化）。過去「sims増で純増なし」は評価時simsの話なら矛盾しない（要確認）。

**アーキ欠陥の直接実証プローブ（2026-07-08 司令塔・青条件・gen0自己対戦 sims40 を局所生成 67ゲーム/11,000局面）**:
- **[A] リーダー感度**（相手リーダーIDだけ差し替えたときの value 予測変化）:
  gen0 は |Δ|=0.021（ライフ1減の変化0.128・予測std0.679 に対し）＝**マッチアップ実質盲目**を定量確認。
  blue訓練後は |Δ|=0.132 だが **p95=0.794**（tanhスケールで±0.8振れ）＝リーダー依存を「彫った」が極端に不安定＝マッチアップ勝敗の
  丸暗記に近い。この不安定な彫り込みが gen0 の汎用ヒューリスティックを壊した機序と整合（negative transfer の実体）。
- **[B] 条件付けフィット（ゲーム単位 train/val 分割＝リーク無し・同一データ・同一MLP・3シード）**: 未見ゲームの value MSE
  | 変種 | val MSE |
  |---|---|
  | 現行(素) | 1.92 |
  | **+リーダーone-hot（自/相手・実ID）** | **1.39（−28%）** |
  | +ランダムone-hot（容量対照） | 1.90（効果なし） |
  → ①現行は未見ゲームで **MSE>1＝定数0予測より悪い**（自信を持って逆符号）＝一般化が根本的に壊れている。
  ②リーダーIDは card_idx に**既に入っている**のに素の経路（22枠平均プール）では抽出できず、**同じ情報をone-hotで直結しただけで−28%**。
  ③ランダム対照が不動＝容量増ではなく**リーダー情報そのもの**の効果。＝「平均プールによる条件付け不能」の**因果的実証**。
- **v3への実務的含意**: encoder の append-only seam（SCALARS_V3）に「自/相手リーダー one-hot（97×2）」を足すだけで、
  既存の温スタート機構（`expanded()`）のまま**torch移行なしに第一レバーの最小版を試せる**。プローブはその効果を事前に−28%と見積もった。
  （プローブのスクリプトはセッションscratchpad・使い捨て。再現は本節の条件で可能。）

---

## 3. 現在走っている全run（監視対象）※各cumは 2026-07-08 時点

| run | checkpoint枝 | 直近cum | 場所 |
|---|---|---|---|
| **A（97汎化）** | `claude/p3-checkpoints` | gen=1 cum≈15,240（**通算=+10,000**） | 元セッション（別コンテナ） |
| B: red | `claude/p3-cluster-red-checkpoints` | 17,400 | 各別コンテナ |
| B: green | `claude/p3-cluster-green-checkpoints` | 12,480 | |
| B: blue | `claude/p3-cluster-blue-checkpoints` | 12,360 | |
| B: purple | `claude/p3-cluster-purple-checkpoints` | 19,380 | |
| B: black | `claude/p3-cluster-black-checkpoints` | 15,480 | |
| B: yellow | `claude/p3-cluster-yellow-checkpoints` | 14,520（※停止しがち） | |
| pool6/12/24/48 | `claude/p3-pool{6,12,24,48}-checkpoints` | **0（未起動）** | — |

各runは連続モード（`--target/--max-shards` 実質無限）＝世代境界で止まらず、各shardで checkpoint枝へ force-push。

---

## 4. 資産一覧（ブランチ・ネット）

- **コード枝**: `claude/opcg-cluster-learning`（ハーネス改修入り。case A の元コードは `claude/opcg-cpu-decision-analysis-49solv`）。
  - `deckgen.all_leader_ids`: `OPCG_LEADER_COLORS`（色フィルタ）／`OPCG_LEADER_POOL_SIZE`（色バランス入れ子・POOL>COLORS>全97）／block_icon==1除外で97。
  - `p3_run.py`: `OPCG_P3_WT` / `OPCG_P3_BRANCH` で checkpoint worktree/枝を上書き（run隔離）。
- **弱Gen0**（全runの共通出発点）: 各checkpoint枝の `p3ckpt/gen0_value.npz`（SHA1 `92ae0c1f8a4e`）＝v1のSLネットをv2温スタートしたもの。`value.npz`と同一で開始・policyなし(uniform)。
- **アーカイブ**: `claude/p3-checkpoints-v1-archive`（v1本走の最終・出荷Gen2含む）／`claude/p3-checkpoints-v2-failed-archive`（失敗v2）。
- **出荷v1バー**: `opcg_sim/data/learned/gen2_*.npz`（本番同梱・v1エンコード）。
- **共通プロンプト**: `docs/cluster_training_prompt.md`（新ワーカーが空き色を拾って学習開始する用）。

---

## 5. cum_games の正規化（重要な落とし穴）

グラフ・比較では**「弱Gen0からの通算局数」に揃える**こと:
- **色クラスタ・pool版**（manifest `gen=0`＝リセット無し）: cum = 弱Gen0からの通算。**そのまま**。
- **A**（manifest `gen=1`＝Gen1スナップショットで一度 cum=0 にリセット済み）: 表示cum は「Gen1以降」。
  **通算 = 表示cum + 10,000**。
- 「1リーダーあたり局数」= 通算 ÷ リーダー数（A=97, red=31, green=23, blue=27, purple=24, black=22, yellow=24, pool=N）。

---

## 6. 監視・re-arm の手順

各runは別コンテナ。**司令塔は git の最終push時刻で死活を判断**（force-pushは~2.5分ごと）:
```bash
for b in cluster-red cluster-green cluster-blue cluster-purple cluster-black cluster-yellow pool6 pool12 pool24 pool48; do
  git fetch origin claude/p3-$b-checkpoints -q 2>/dev/null
  echo "$b cum=$(git show origin/claude/p3-$b-checkpoints:p3ckpt/manifest.json 2>/dev/null|python3 -c 'import sys,json;print(json.load(sys.stdin)["cum_games"])') last=$(git log -1 --format=%cr origin/claude/p3-$b-checkpoints)"
done
```
- 最終push **>10分**（cum不変）= その色は**停止**。**司令塔は別コンテナのrunを直接再起動できない**ので、
  該当色のセッションに「同じ起動コマンドで再実行」を促す（checkpointから自動継続）。
- **注意**: yellow は停止しがち。各ワーカーが自分で死活監視＋re-armする運用が望ましい（共通プロンプトに追記余地）。

---

## 7. 強度測定の手順（司令塔の主業務）

対象runの現netを凍結して、その分布で対L1測定。バー＝同条件の出荷v1。
```bash
# 例: 紫クラスタnetを取得
git fetch origin claude/p3-cluster-purple-checkpoints -q
git show origin/claude/p3-cluster-purple-checkpoints:p3ckpt/value.npz  > /tmp/x_value.npz
git show origin/claude/p3-cluster-purple-checkpoints:p3ckpt/policy.npz > /tmp/x_policy.npz
# 紫デッキで対L1（バーは出荷v1で同条件）
OPCG_LEADER_COLORS=紫 OPCG_LOG_SILENT=1 PYTHONPATH=tests python tests/scripts/p3_vs_l1.py \
  --value-path /tmp/x_value.npz --policy-path /tmp/x_policy.npz --label purple-net \
  --rotate-leaders --sims 160 --pimc 4 --pairs 12
```
- pool版は `OPCG_LEADER_POOL_SIZE=<N>`（色指定の代わり）で対象プールに絞る。
- N=24(pairs=12)は速い代わりにCI広い。有望なら pairs 増でCIを締める。
- **軽い代替**: 各runの共通弱Gen0を凍結し、`p3_gate`系で net vs Gen0（sims40）を測ると速い（L1不要・相対climb）。ただし絶対バー比較は上のvs-L1。

---

## 8. リーダー数スケーリングのグラフ（アウトプット）

- **横軸 = 1リーダーあたり通算局数**（= 通算 ÷ N・対数）／**縦軸 = 強度**（共通の色バランス評価で。推奨は
  `OPCG_LEADER_POOL_SIZE=6`（各色1・全プールに内包）で固定した6リーダー評価にして全版を同一土俵で測る）。
- 各版（pool6/12/24/48 と A=97）を同じ軸に載せる。**1本の曲線に重なれば「強度は局/リーダーで決まる（希釈が本質）」／
  バラければリーダー数（容量）が効く**。
- 現状の暗示（紫の1点）: 濃くしても伸びが緩い＝**希釈以外（アーキ天井）が支配的**の可能性。グラフで確証する。

---

## 9. 未起動: pool版4つ の起動（フル4版でユーザ合意済み）

各版=別セッション。`git checkout claude/opcg-cluster-learning` 後、N=6/12/24/48 で:
```bash
OPCG_LEADER_POOL_SIZE=6 OPCG_P3_WT=/tmp/pool6-wt OPCG_P3_BRANCH=claude/p3-pool6-checkpoints \
OPCG_LOG_SILENT=1 PYTHONPATH=tests python tests/scripts/p3_run.py \
  --enc-version 2 --rotate-leaders --shard-games 60 --sims 40 --workers 4 --target 100000000 --max-shards 100000000
```
（枝はpristine弱Gen0にリセット済み・cum=0。赤汚染は修正済み。）

---

## 10. 判断基準・次の一手

- **成功条件**: いずれかの版が **対L1で出荷v1（多様0.833／色別0.7-0.8）を超える**。
- **打ち切り条件**: リーダー数スケーリング曲線が**寝て**、どの版も出荷v1に届かない見込みが立つ（＝アーキ天井確定）。
- **暫定**: 紫の1点は打ち切り寄り。だが**曲線を数点そろえてから**最終判断するのが誠実（pool版4つ起動→各節目で測定→作図）。
- グラフが「濃くすれば届く」形なら、**per-leader級の極端な濃縮**（≒v1条件）まで詰める価値を再評価。
- **【2026-07-08 更新】原因分析の帰結として、次の一手は LC-ValueNet（リーダー条件付け・アーキ第一レバーの最小実装）の
  青パイロットに決定（ユーザ合意）。設計・判定基準は `docs/reports/lc_value_net_plan_20260708.md`。**
- **【実装完了・2026-07-08】** `ValueNet.lead_slots`／`to_leader_conditioned()` を実装（`opcg_sim/src/learned/value_net.py`）。
  `_net_enc_version`／`p3_run._vguard`／`p3_loop` の次元計算を `vnet.feat_dim` プロパティへ集約（lead_slots対応）。
  新規 `tests/test_value_net_leader_slots.py`（7件）・全品質ゲート green（既存1091件・fastapi/httpx起因の既知失敗のみ・
  構造監査0）。**§4-1のオフライン事前確認も実測で通過**: 実装したLC net（lead_slots=2）で青データ11,000局面を
  ゲーム単位分割fitすると、legacy比 **val_mse -25.1%**（設計時プローブの-28%とほぼ一致）。実データでの恒等性は
  max|Δ|=0.00e+00（完全一致）。
- **【枝を種付け済み・起動待ち・2026-07-08】** `claude/p3-lc-blue-checkpoints`（commit a14477d）を作成。共通弱Gen0を
  LC化(lead_slots=2)した net を value/gen0_value に配置（policyなし=uniform・cum=0・status=INIT）。LC net の
  自己対戦→学習→保存→再読込の全経路をスモーク確認済（lead_slots=2 維持）。**訓練は別セッションで起動（ユーザ決定＝競合ゼロ）。
  司令塔（本セッション）は §4-4 を軌跡サンプリング（cum≈3k/6k/10k/14.5k で凍結測定）で早期判定する。**
  起動コマンド（別セッションで・LCコード枝で回すのが必須）:
  ```bash
  git fetch origin claude/opcg-cluster-learning && git checkout claude/opcg-cluster-learning
  python -m pip install -q numpy
  OPCG_LEADER_COLORS=青 OPCG_P3_WT=/tmp/lc-blue-wt OPCG_P3_BRANCH=claude/p3-lc-blue-checkpoints \
  OPCG_LOG_SILENT=1 PYTHONPATH=tests python tests/scripts/p3_run.py \
    --enc-version 2 --rotate-leaders --shard-games 60 --sims 40 --workers 4 --target 100000000 --max-shards 100000000
  ```
  legacy青バー: gen0=0.417 / 訓練後(538局)=0.208。**LCがこの下降を止める/反転すれば「アーキが効いた」**（§4-5判定表）。
- **【効果セマンティクスv3 設計完了・2026-07-08】** LCの一般化＝EffFeat（AST由来の決定的効果特徴 ~92次元）＋スロット別
  条件付け＋scalars v3（山札/トラッシュ/今ターンKO）＋turn1フラグ＋hidden256。恒等温スタート連鎖で弱gen0の実力を保って移行。
  設計書 `docs/reports/effect_semantics_v3_plan_20260708.md`・特徴空間の一次資料 `effect_feature_inventory_20260708.md`
  （未決事項は解決済み・残りは間接参照の1bit妥協のみ）。検証はオフラインfit（ゼロショット検証込み・数分）→青パイロット
  （LC本判定との4点比較）→ v3≥LC なら97汎用へ。
- **【LC青 1点目測定・2026-07-08】** cum=9,840（364局/リーダー・lead_slots=2確認済）を青デッキで対L1（sims160/pimc4・N=24・
  value+policy＝legacy青と同条件）: **勝率 0.396 [.228,.592]**（9-1-14）。
  | net | 局/リーダー | 対L1 | 対gen0 |
  |---|---|---|---|
  | legacy青 gen0 | — | 0.417 | — |
  | legacy青 訓練後 | 538 | 0.208 | −0.21（劣化） |
  | **LC青（1点目）** | 364 | **0.396** | **−0.02（ほぼ維持）** |
  → **期待どおりの向き**: legacy が gen0 から −0.21 崩れたのに対し、LCは 364局時点で gen0 水準を維持＝negative transfer が
  止まっている可能性。**ただし測定点が違う（LC364 vs legacy538）・CI広（N=24）＝断定は cum=14,520（538局）の本判定待ち**。
  legacy@364 の値は未測なので「同一点でLCが上」はまだ示せていない点に注意。本判定測定を 14,520 到達時に実施予定。
- **【LC青 本判定・2026-07-08】** cum=14,520（537局/リーダー＝**legacyと同一土俵**・lead_slots=2確認済）:
  **勝率 0.396 [.228,.592]**（9-1-14）。1点目(364局)の0.396と完全一致＝安定。
  → **判定確定: LCは劣化（negative transfer）を止めた**（legacy同地点0.208との差は歴然）。ただし gen0(0.417) を超えて
  **登ってはいない**＝§4-5でいう「0.42≤X<0.55」の下端相当（CI考慮）。干渉はアーキ起因と実証・climb には効果セマンティクス
  v3 等の次レバーが必要、の整理で**v3実装へGO**（ユーザ承認済み・実装開始 2026-07-08）。LC青の訓練ワーカーは停止してよい。
- **【v3実装完了・オフライン検証クリア・種付け済み・2026-07-09】** コミット `83c69e2`（effect_features.py・value_net v3・
  encoder v3・EFF_DIMガード・テスト14件・全ゲートgreen）。オフライン検証（§6-1）: (a)未見ゲームMSE **legacy -31.8%**
  （LCの-25.1%を上回る・ゲート通過） (b)未見リーダー（同色holdout）でも v3 1.431 < LC 1.493（同色内は差が出にくい条件・
  真価は97リーダー空間）。**v3青パイロット種付け済み**: `claude/p3-v3-blue-checkpoints`（`dc3e76b`・恒等連鎖
  gen0→scalars46→LC→EffFeat116→hidden256・実局面で max|Δ|=4e-16・selfplay全経路スモークOK）。
  起動は別セッション（ワーカープロンプト: `docs/v3_blue_worker_prompt.md`）。判定: 537局/リーダーで凍結測定し
  gen0 0.417 / legacy 0.208 / LC 0.396 との4点比較（v3>LC で97汎用へ）。
- **【v3青 中間測定・2026-07-09】** cum=9,840（364局/リーダー＝LC1点目と同一点）: **勝率 0.438 [.262,.631]**（10-1-13）。
  同一点比較: LC 0.396 / v3 0.438 ＝ **訓練青netとして初の出発点(gen0 0.417)超え**。legacy崩壊→LC維持→v3上昇の
  一貫したはしご＝効果セマンティクスがclimbを可能にする方向のシグナル（CI広のため確定は本判定の4点比較で）。
- **【出荷v1の青バー測定・2026-07-09・重要】 出荷v1×青デッキ対L1 = 0.938 [.769,.985]（22-1-1）**。紫(0.750)を大きく超える。
  **含意①**: 青の目標バーは0.938＝v3中間0.438はまだ半分。**含意②（アーキ天井説の重大修正）**: 出荷v1は現行と同じ極小アーキ
  （1隠れ層MLP・v1エンコード＝条件付け無し）で0.938を出した＝**この器でも0.93級は表現可能（存在証明）**。
  「v1の0.925は単一デッキの特殊解」という本書の旧解釈は**誤り**＝単一デッキ集中訓練の強さは青デッキ群へ広く転移していた。
  青で訓練netが沈むのは「青がアーキに不利」でなく「**L1は青で最も弱い（つけ込む余地最大）が、弱いnetにはそれを突く力が無い**」。
  **残る本当のボトルネックは訓練ダイナミクス**（弱出発×ローテ×sims40は教師が弱いまま均衡）。
  **戦略候補（本判定後に判断）**: v1レシピ（単一デッキ集中）で専門家netを並列量産→**v3条件付きアーキを受け皿に蒸留**
  ＝弱い自己対戦教師を強い専門家教師で置換する路線が急浮上。
- **【v3青 本判定・2026-07-09】 cum=14,520（537局/リーダー・全系統と同一点）: 勝率 0.604 [.408,.772]（14-1-9）。**
  5点はしご完成: legacy 0.208（崩壊）/ LC 0.396（横ばい）/ gen0 0.417 / v3中間 0.438 / **v3 0.604** / 出荷v1バー 0.938。
  → **判定: 効果セマンティクスは「登り」を可能にした**（0.438→0.604・+0.17/173局＝ローテ訓練で初の持続的climb・
  legacy系の全色バンド0.542も突破・基準「0.55以上=アーキが効いた」を充足・傾きはまだ寝ていない）。
  次の一手: ①青は続走して傾きの寝る点（sims40教師の天井）を実測（~800局/リーダーで次回測定）
  ②**97汎用v3パイロット**へ（種は現行v3種を流用可・EffFeatは全カード対応済み）。
- **【97汎用v3パイロット起動準備・2026-07-09】** ユーザ承認により青と**並行**で起動。種付け済み:
  `claude/p3-v3-gen97-checkpoints`（`faa5f60`・青と同一のv3種）。ワーカープロンプト: `docs/v3_gen97_worker_prompt.md`
  （色フィルタ無し=全97・ガード2つ・enc-version 3）。測定計画: **cum=10,000 で凍結し案A(legacy) Gen1=0.433 と直接比較**
  （多様デッキ評価・全97ローテ）。並行run一覧: 青v3（続走・800局/リーダーで次回測定）＋97汎用v3（新規）。
- **【蒸留パイロット・2026-07-09・機構実証成功】** 出荷v1を教師にv3生徒へvalue蒸留（青）:
  教師の自己対戦2,000ゲーム/208,844局面を生成→教師の生value予測をラベルに生徒を教師あり訓練（val_mse 0.013）
  →出荷policyを warm_start_policy(1,3) で恒等移植→対L1測定 **0.812 [.618,.921]**（19-1-4）。
  比較: v3ローテ自己対戦=0.604（7時間） vs **蒸留=0.812（1.5時間）** vs 教師(出荷v1)=0.938。
  → **「ボトルネックは教師であって器ではない」の最終確認**。集中×蒸留フル版（v1レシピで専門家量産→v3へ蒸留）は
  実証済み部品のみで構成可能になった。教師との残差はデータ量/ラベル質（MCTS改善値）で詰める余地。
  成果物: /tmp/distill_student_value.npz・/tmp/distill_blue_ds.npz（コンテナ揮発・要再生成可）。
- **【蒸留v2 完了・2026-07-10・本命ルート実証】** 出荷v1教師→v3生徒・**97リーダー多様×1万ゲーム(102.7万局面)**・
  val_mse=0.00555。対L1: **多様97=0.771 [.573,.894]／青=0.896 [.715,.967]**。
  | net | 多様97 | 青 |
  |---|---|---|
  | 案A(legacy 97汎化) | 0.433 | — |
  | v3ローテ自己対戦(ピーク) | — | 0.604 |
  | **蒸留v2(97汎用生徒)** | **0.771** | **0.896** |
  | 出荷v1(バー) | 0.833 | 0.938 |
  → **ローテ自己対戦の天井(0.43〜0.6)を蒸留が突破し出荷v1をほぼ完全写経（差0.04〜0.06・CI重複）**。「v1レシピで
  専門家→v3器へ蒸留」の①②③が全て実測グリーン＝**汎用CPUを出荷v1級で一発生成する実用ルートを確立**。
  成果物枝 `claude/distill2-artifacts`（生徒net+policy・恒久）。
  留意: 蒸留は教師の写経＝出荷v1を**超えない**（マーク再評価でも ship と生徒は MAIN手をほぼ一致・新判断は未発生）。
  出荷v1超えには次段: (a)v1レシピの**専門家**(各デッキ0.9級・出荷v1が0.75の紫等で上回れる)を量産→蒸留 (b)蒸留後に
  v3器で追い学習(強い教師/探索で新特徴を活性化＝マーク@3のドン付与が転じるかで検証)。
- **【マーク再評価: ship vs 蒸留v2・2026-07-10】** 5マーク中 MAIN手(@3/@28/@52)はほぼ一致（写経の裏付け）、
  カウンター(@47/@67)は迷い局面で挙動が割れたが決定的改善でない＝「蒸留は写経成功・教師超えの新判断は未発生」を
  ツールが正しく可視化。回帰計器として機能確認（追い学習後に @3 が ATTACH_DON へ転じれば改善の決定的証拠）。
- **【97汎用v3 10k測定・2026-07-10】** 多様97・cum=10,020: **0.438 [.262,.631]**。案A(legacy 97汎化)Gen1=0.433 と**ほぼ同点**。
  → **v3の器でもローテ自己対戦を97多様で回すと legacy と変わらない**（青パイロットの登坂0.604は幅最大では消える）。
  条件付けの利得は分布が広いほど薄まり、結局 sims40 教師の天井に支配される＝原因分析と整合。
  **結論の確定: ローテ自己対戦は器を替えても汎用で0.43〜0.44の天井。出荷v1超えは蒸留系ルート一択。**
  97汎用v3ワーカーは役目完了（天井の最終確認）＝停止可。

---

## 【セッション総括 2026-07-08〜10】測定マップ（全て対L1・sims160/pimc4）

| 手法 | 多様97 | 青 | 位置づけ |
|---|---|---|---|
| 案A legacy 97汎化 | 0.433 | — | ローテ自己対戦の天井 |
| v3 97汎用(ローテ) | 0.438 | — | 器を替えても天井は同じ |
| v3 青(ローテ・ピーク) | — | 0.604→0.40(軟化) | 狭分布では登坂も自己対戦天井で頭打ち |
| **蒸留パイロット(青2千)** | — | 0.812 | 教師=出荷v1を写経 |
| **蒸留v2(97多様1万)** | **0.771** | **0.896** | 汎用CPU一発生成 |
| 出荷v1(バー) | 0.833 | 0.938 | 写経の上限 |

**確定した因果と結論**: ①汎用が弱い真因は「器(bag-of-cards条件付け不能)」ではなく「教師(sims40自己対戦)の弱さ」
（出荷v1が同じ器で0.938を出す存在証明＋ローテ自己対戦が器を替えても0.44天井の二重確認）。②教師を出荷v1に替える蒸留で
0.77〜0.90を一発生成＝出荷v1をほぼ写経。③**出荷v1超えの残タスク**: (a)v1レシピで専門家量産→蒸留 (b)蒸留後にv3器で
強い探索の追い学習（マーク@3のドン付与が転じるかで検証）。どちらも実証済み部品のみ。
**資産**: コード枝 `claude/opcg-cluster-learning`（EffFeat/LC/v3/蒸留パイプライン/マーク再評価CLI・全ゲートgreen）、
蒸留生徒 `claude/distill2-artifacts`、報告 `docs/reports/verification_summary_20260709.md`。
- **【v3青 800局測定・2026-07-09】** cum=21,600（800局/リーダー）: 初回 N=24 **0.333** [.180,.533]・確認 N=48
  **0.438** [.307,.577]・合算 N=72 ≈ **0.40**。軌跡＝364局 0.438 → 537局 0.604 → 800局 0.40前後。
  → **v3のローテ自己対戦は 0.4〜0.6 帯で頭打ち**（0.604がピークで下降 or 0.604が上振れのプラトー・いずれにせよ
  0.9台への道ではない）。器を直しても sims40 教師の天井は残る＝原因分析どおり。**蒸留(0.812/1.5h)との差が決定的**。
  司令塔判断: 青v3ワーカーは役目完了（登坂実証＋教師天井の実測）＝停止可。97汎用は cum=10,000（案A比較）まで続行。
  **主戦場は蒸留路線へ**: 次手=蒸留v2（データ1万ゲーム・MCTS改善値ラベル(sims160)・97多様分布）で
  「出荷v1級の汎用v3生徒」を作る→その後 v1レシピの専門家量産→追い蒸留で出荷v1超えを狙う。
- **【並列切替・2026-07-10】** ユーザ判断で早期判定を待たず追い学習(b)をバッチ式並列へ切替。
  直列 postdistill97（cum=1,024 で停止・この重みを引き継ぎ）→ `pd_setup` で `claude/p3-pd-net`＋data枝 w1/w2/w3 種付け。
  **learner は司令塔コンテナで稼働中**（lr2e-4・epochs2・games-per-update128・staleness3・薄まり防止/バックプレッシャ/
  policy同梱の修正済みハーネス `68e400e`）。生成ワーカーは別セッション（`docs/pd_gen_worker_prompt.md`・番号は司令塔配布）。
  **cum の読み方**: 並列線の実効局数 = 直列1,024 + pd-net manifest.cum_games。学習量は manifest.updates（累積勾配パス）。
  測定はこれまで通り net枝凍結→p3_vs_l1（バー: 生徒0.771/出荷v1 0.833）。直列枝 postdistill97 は歴史として凍結（再開しない）。
- **【追い学習(b) 早期劣化ガード クリア・2026-07-10】** 実効1,920局（直列1,024＋並列896・round5凍結）の多様97対L1
  ＝ **0.771 [.573,.894]**（18-1-5）。出発点（蒸留生徒0.771）と同水準＝**劣化ゼロ・v2型引き戻し無し**（「強い出発点を
  壊さない教師=自身sims160」の実証）。上振れ未だ＝5k/10kの節目で0.833（出荷v1バー）超えを判定。
  運用メモ: ①司令塔コンテナ再起動でlearnerが2h死亡→ワーカーはバックプレッシャで自動待機・consumed永続で
  クリーン再開（穴対策が実地で機能・上書きロスは3バッチ384局のみ） ②3ワーカーだとround進行が速く
  max-staleness=3ではw1のバッチが期限切れ連発→**staleness=6に緩和して再起動**（低LR×勝敗教師なので鮮度の実害なし・
  learner再起動時は自分のシェルを巻き込むためpkill -f pd_learn禁止＝PID指定killを使うこと）。
- **【追い学習(b) 特徴活性化プローブ・2026-07-10】** round14（実効~3,100局）vs 蒸留直後を実対局2,000局面で感度比較:
  **相手リーダー差替 0.024→0.213（×8.8）**・召喚酔い 0.100→0.154(+54%)・ターン1 0.061→0.083(+36%)。
  山札/トラッシュ/KOは休眠のまま（分布上稀＝想定内）。蒸留直後のリーダー感度0.024≒v1系の盲目0.021＝
  **「v1教師が見えないものは写経も見えない」を確認し、実戦勝敗の追い学習がそれを9倍に活性化**＝(b)の機構が
  特徴レベルで実証。注視: ライフ感度低下(0.318→0.134)＋予測先鋭化(std0.71→0.96)＝再配分なら健全・飽和過信なら
  要警戒→5k中間測定の強度で判定。
- **【v3青 884局の裏取り点・2026-07-09・並行司令塔】** cum=23,880（884局/リーダー）凍結で対L1 **0.479 [.296,.668]**
  （11-1-12・独立測定）。軌跡 0.438(364)→0.604(537)→~0.40(800)→0.479(884) ＝ **「0.4〜0.6帯プラトー」を第4点で追加裏取り**。
  ※注記: `claude/p3-v3-97-checkpoints` は並行司令塔が正史把握前に作った**未使用の重複種**（正は `p3-v3-gen97-checkpoints`）。
  git プロキシが branch delete 未対応のため残置＝**使用しないこと**。 CAN（表現力）: 山札/ターン1の合成ターゲットを val_MSE 0.002/0.0001 で
  fit＝天井なし。DOES（実使用・800局net の感度分析）: **リーダーEffFeat が全ブロック中最大の依存**（|Δ|=0.22・生盤面0.21超え）・
  山札は状況依存で正しく使用（平均0.10/p95 0.65）・ターン1フラグは無視（0.007）＝「使いこなしは容量でなく
  訓練分布がその特徴に勝敗信号を載せるかで決まる」。蒸留→追加学習で休眠特徴が活性化する機序と整合。

---

## 11. 教訓・注意

- **長時間runは死活監視必須**（Aは監視の隙に2h停止した）。各runは自分でwatcher＋re-armするのが理想。
- **cum正規化**（§5）を忘れると軸がズレる。
- **測定は凍結スナップ**（value.npzをコピー）で行う＝訓練が進んでも測定は固定netで完結。
- **water-oil注意**: p3_vs_l1 は α-β評価器 vs NN-MCTS で完全フェアではない（参考値）。相対比較・推移で見る。
- マージ/PR はユーザ明示指示があるまで行わない（`CLAUDE.md`）。この実験群は研究用でまだ製品化しない。
