# ユーザ指摘の索引——量になったもの・まだ量でないもの

2026-09-13 作成。ユーザ要望「洗い出した結果を一覧にして見せて」。
**レポート（`docs/reports/`）・台帳（`n_loop_ops.md`）・規約（`CLAUDE.md`）・仕様（`SPEC.md` §6.1）・
リーダー個別差異（`leader_specs/ISSUES.md`）・ソースコードの注釈**を一巡して洗い出した。
式の本文は `docs/life_budget.md`（ゲームの中身）と `docs/measurement.md`（測り方）。

状態の記号: **式** ＝量として書けた／**測** ＝実測済み／**実** ＝実装済み／**未** ＝未測定・未着手。

## 1. ゲームの打ち回しに関する指摘

| 日付 | 指摘（ユーザの言葉） | 量 | 状態 |
|---|---|---|---|
| 2026-07-11 | 相手ライフが少なくゲームが短いなら受けることもある | 領域 1（`G = max(0, N−L−B) = 0` なら受けるのが最適） | 式・**測（領域 1 は全窓の 4.8%・なのに 38.2% 守っている）** |
| 2026-07-30 | 手札の価値をどう正しく判断するか | 手札の資源集約（符号化 v6）／**`∂V̂/∂手札` の符号** | 実（入力）・**測（符号は 3 ネットとも正しい・大きさは 25〜35% 過小）** |
| 2026-08-05 | 攻撃は必ず相手に損失を強いるので「止められる＝無駄」ではない | 攻め側の恒等式（相手の手札 −c か ライフ−1・手札+1） | 式・実（`TREE_BOX_BATTLE`）・測（DON 付与が動いた） |
| 2026-08-05 | 二人零和なら相手は最善応手。PUCT の訪問混合は保険ではない | 窓をミニマックスで畳む | 実 |
| 2026-08-12 | CPU はドン付与が苦手／**7000 理論**（カウンター 2 枚の強要） | `pressure(k) = c(x+1000k) − c(x)`＝**実測 +0.66 枚/1000・段はでこぼこ**（2000→3000 が +0.97） | 式・実（v54）・**測（デッキ側 +0.66 枚／1000・使い切りは `don_idle` 2.32＝77.1% のターン）** |
| 2026-08-15 | 防御プランは窓の入口で 1 回立てる（払い始めたら払い切る） | 貪欲と最適の差 `Σ_貪欲 c − Σ_安い G 本 c ≥ 0` | 実・**測（`greedy_gap` +0.406 枚・守った窓の 52.3%）** |
| 2026-09-12 | 終盤①リーサル／②通らなければ盤面を触って伸ばす | 逐次更新（残りの攻撃で `Σ force > S_opp` を作れるか） | 式・**測（`force_gap` 5.01・殴らないターン 0.295）** |
| 2026-09-12 | ターンの最初は①でも途中から②に移る | 攻撃順序の option value `V(最良順) − V(最悪順)` | 式・**測（入口の選択で 0.0420 [0.0218, 0.0663]）** |
| 2026-09-12 | どちらにしろ負け濃厚なら最初から無茶をする | **`worlds` の検出確率 `1−(1−p)^K`**（serve は K=1＝p のまま・生成だけ K=4） | 式・**測（K を上げても勝率は動かず・分解能不足）** |
| 2026-09-12 | 中盤の 3 方針（純粋な殴り合い／シーソー／手札切れ待ち） | 相手の時計 `T_opp` のどの項を買うか（交換レート） | 式・**測（時間 × 方針の地図）・未（交換レートそのもの）** |
| 2026-09-12 | デッキによって守りの比重が変わる（4 類型を列挙） | **`c̄(t)` の傾き・`K` の大きさ・`L/H` の再生** | 式・**測（`c̄(3000)` 2.09〜3.00）・未（K・再生）** |
| 2026-09-12 | しきい値はリーダー＝デッキの中身で変わる。3 に強制は誤り | **`θ(deck, state) = A·T − B`**（定数ではなく状態の関数） | 式・**測（同じ x で `c(x)−c̄` の符号がリーダーで逆＝固定しきい値は原理的に誤り）** |
| 2026-09-13 | ライフから欲しいカードがある | `(N − G)` 項（受けるたびに +1 枚）＋ トリガー `τ` | 式・**測（τ＝0.00〜0.50・デッキ差）** |
| 2026-09-13 | 次ターン守れる前提なら今は受ける（手札を残す） | `G·Θ` 項 | 式・**測（`Θ` 0.8〜1.5 枚＝2000 までは守る）** |

## 2. 測り方・検証に関する指摘

| 日付 | 指摘 | 量 | 状態 |
|---|---|---|---|
| 2026-07-08 | デッキ 0 枚勝利のような固有効果は内挿不能 | 効果 AST の意味特徴で扱う（カード ID 埋め込みでは無理） | 実（N 系の設計） |
| 2026-07-27 | 密ラベルのコーパス | — | 実（gen7） |
| 2026-08-09 | ドン最大 6 枚だから楽観している（仮説） | ドン差での層別 | 測（**棄却**） |
| 2026-08-12 | ドメインランダム化（カードの固有性を崩す） | — | 実（B 系） |
| 2026-08-12 | 人間の付与が**候補にすら無い** | **`coverage = P(強い手 ∈ 候補集合)`**（当時 36%） | 測（旧）・**測（現行は被覆 1.000＝刈っていない）** |
| 2026-08-13 | 接戦帯で較正を見る | `ECE_band`／**兄弟手の順序精度 `order_acc`** | 測（ECE）・**測（接戦帯 0.5016＝コイン）** |
| 2026-08-14 | リーダー能力の物理要約 24 | `leader_feat` | 実（gen15） |
| 2026-08-15 | 過去バージョンでは今の問題は検出できない | **`P(検出) = 1−(1−p)^n`・`n ≈ 3/p`**（ミラーは `p=0` の経路） | 式・実（不変量監査） |
| 2026-08-25 | 実デッキは完全ホールドアウトにする | 汎化の測定（synth で訓練・実デッキで検証） | 実（運用中） |
| 2026-09-12 | ブレを追うのではなく、勝ちやすい方針を教える | **後悔 `R = E[max_q V(s,q) − V(s,p_played)]`**（実測 0.033〜0.084） | 式・測 |
| 2026-09-12 | 離散・連続は時系列に対しての話 | `P(win) = Σ_t P(T=t|s)·P(win|s,T=t)`／**ハザード** | 式・測（平均 0.73 ターン）・**測（分布＝帯ごとに形が在る 0.26〜0.63）** |
| 2026-09-13 | 〜しないではなく〜できないのパターンもある | **`Δ_obs = Δ_causal + Δ_selection`** | 式・測（0.153→0.147→0.141） |
| 2026-09-13 | 懸念を言語化して数式化して定量化できないか | 本書＋`life_budget.md`＋`measurement.md` | 式 |

## 3. この洗い出しで**私の過去の結論が変わったもの**

| 変わった結論 | 変わり方 | 根拠 |
|---|---|---|
| 「超過パワーが上がるほど守らない＝交換レートを管理していない」 | **逆。予算式では `c(x) > c̄` なら受けるのが最適**＝あの単調減少は合理の証拠 | `life_budget.md` §3 |
| 「終盤はもっと守れ」 | **言い過ぎ。終盤は `G` が大きいので問題は「どれを守るか」** | 同 §3・§7 |
| 「board はどこでも割に合っていない」 | **終盤に限る主張へ狭めた**（中盤の board は 45% が強制＝殴れない） | `2026-09-13_can_or_wont.md` |
| 「守れたのに受けた 92.6%／足りていた 71.0%」 | **上限（過大）**。カウンターイベントの **DON コスト**を見ていない＝守りの費用は札とドンの 2 成分（再計算の結果は**攻撃 1 回ごと・DON 補正後で 89.4%／64.7%**＝落ちたのは 5.3 ポイント） | `life_budget.md` §18 |
| 「守った方が勝ちやすい（V_guard − V_take）」 | **検査を通した（2026-09-13）**——3 ネット × 両席で符号一致＝この偏りでは説明できない＝結論は残る | `measurement.md` §12 |
| 昇格ゲートは 2 条件で判定できている | **どちらも ±35 Elo**。副条件は **−21 Elo の退行を 88% 通す** | `measurement.md` §4・§10 |
| 192 ペアの Elo（+25.4／+81 など） | **この式が予想する大きさのノイズ**だった（高分解能で +3.5／−4.5。r7 は 2,095 ペアで **−4.5 [−16.1, +7.1]** と確定） | 同 §4・`2026-09-13_hires_arena_r7.md` |
| 「PIMC は 4 世界引いている」 | **serve とアリーナは 1 世界**（`SeatSpec.opts` は `{net, sims}` だけ・Rust の既定 `worlds: 1`）。4 は生成だけ＝出荷エンジンは教材を作った生成器より粗い | `measurement.md` §9 |

## 4. 未測定の宿題（優先順・コーディネータの意見）

1. **`∂V̂/∂手札` の符号検査** ＝ **閉じた・交絡は否定された**（2026-09-13・3 ネット × 両席で
   符号一致・`slope_true` +0.043 対 `slope_V` +0.041〜0.045）＝**`V_guard − V_take`（+0.141）は
   「手札が減った＝勝者の相貌」では説明できない**＝守りの読みと手当ての向きはそのまま残る。
   残るのは**較正**（モデルの応答 0.026〜0.033 は真の 0.043 より 25〜35% 小さく、`dv_slot` は
   r3 0.041 → r10 0.026 と世代で鈍っている＝監視対象）。`2026-09-13_readout_w32.md` §2
2. **`guard_enough` の DON 補正** ＝ **閉じた・結論は変わらない**（2026-09-13・受けた行で
   `enough` 0.797 → **0.744**＝落ちたのは 5.3 ポイントで基準の 10 ポイント未達）。
   読み替え: `can_or_wont` の 92.6%／71.0%（ターン単位）は**攻撃 1 回ごと・DON 補正後で
   89.4%／64.7%**（t≤2 の受けた行）。**書いている途中で予算式の穴が 1 つ出た**:
   守りの費用は札とドンの 2 成分で、`c(x)` は 1 通貨で書けていなかった（`life_budget.md` §18）。
   **相手ターン中の自分のアクティブなドンは 0.55〜0.68 個**＝【カウンター】イベントは
   構造的にほぼ打てない＝**予算モデルに「ドンを残す」項が要る**（宿題として残る）
3. **行動空間の被覆** ＝ **閉じた**（2026-09-13・40 局 2,606 判断点で `coverage` 1.000・
   `missing_any` 0.000・`best_not_in_a` 0.000・`exhausted_a_any` 0.000）。
   **枝予算は候補を 1 つも落としていない**＝2026-08-12 の 36% は当時の Python の列挙の話。
   **残るのは「人間の配分が列挙の設計に入っているか」**（無制限の列挙を正とみなす以上
   この方法では測れない）。`2026-09-13_search_limits.md`
4. **`worlds` を上げた効果** ＝ **効果の証拠なし・ただし分解能不足**（2026-09-13・
   腕 4 が +12.6 Elo [−20.1, +45.6]／腕 8 が +3.0 [−31.4, +37.6]・**用量反応は逆向き**）。
   250 ペア前後では **+20 Elo 級を棄却できていない**（事前想定は 1,200 ペア）。
   **前提の誤りも直した**: serve／アリーナの既定は `worlds = 1` で、4 は生成だけ。
   `2026-09-13_search_limits.md`
5. 接戦帯の **`order_acc`** ＝ **測定した。本日の一次対象**（2026-09-13・410,290 行）＝
   **close 帯 0.5016＝コイン**（全帯 0.50〜0.53 で基準 0.55 に未達）で、そこで失う
   `q_cost` 0.070 は**独立に測った後悔 0.033〜0.084 と一致**＝**後悔のほぼ全部が順序の誤り**。
   方策のエントロピーは 1.89〜2.11 bit＝**平ら**（持っている情報が順序と無関係）。
   帰結: **PUCT は事前分布で枝を選ぶので「評価が正しければ探索が拾う」は成立しない**＝
   **候補 F は「価格を方策の順序の教師にする」形へ組み直す**。
   限界: 探索の Q を正としている＝「方策が探索に追いついていない」の測定。
   `2026-09-13_readout_w32.md` §1
6. デッキ別の **`c̄(t)`**・攻撃順序の価値・貪欲と入口コミットの差・τ
   ＝**τ と `c̄` は実測済み**（`deck_profile.py`・`2026-09-13_deck_profile.md`・`life_budget.md` §19）。
   **貪欲と入口コミットの差も実測**＝`greedy_gap` 平均 **+0.406 枚**・守った窓の 52.3% で正
   （止めるのに要る最小より多く払っている）。
   **攻撃順序の価値も測った＝取り逃がしは実在**（2026-09-13・反事実の計器
   `tests/scripts/order_option.py` を作った）: 入口の選択で **0.0420** 勝率
   CI [0.0218, 0.0663]・腕の差は続きの雑音の 7.9 倍・最良の入口を選ぶのは 55%（無作為 38%）。
   大きさは後悔の実測 0.033〜0.084 と同じ桁で、**`order_acc` の欠陥を勝率の側から見たもの**の
   可能性が高い（母集団が違うので足し合わせない）。**副産物**: 攻撃の入口が 2 つ以上あるのに
   **49% は攻撃を打っていない**＝攻めの取り逃がしの全体はこれより大きい。
   `2026-09-13_order_option.md`
6b. **ライフ予算が実プレイで使われているか** ＝ **測定した。使われていない**（2026-09-13・137,711 窓）＝
   `θ` は平ら（`L − θ` が −4〜+4 で守り率 0.33〜0.39）・`c(x) < c̄` を**受けた 47.9%**・
   `p<=0` の無駄な守りが**全体の 23%**・払い過ぎ +0.406 枚。**ただし価格そのものは正しい**
   （実プレイの最小枚数曲線 1.00/1.31/2.22/2.64/3.85 がデッキ側の `c̄` と一致＝**7000 理論が実測で成立**）。
   `2026-09-13_readout_w32.md` §4
6b2. **ライフと札の交換レート `λ/μ`** ＝ **測った。予算モデルは自己整合**（2026-09-13・1,041 局）＝
   `λ` +0.1158（相手ターン +0.1362）・`μ` +0.0433 ⇒ **`λ/μ` 2.674**・`c̄ + τ_value` 1.674
   ＝**無差別点は札 1.5 枚あたり**（x≤2000 は守り・x≥3000 は受ける）。
   **私の予測の当て方が誤っていた**（`c̄` を 3000 帯の 1 点に当てた＝x の分布で重みを付けるのが正しい）。
   **新しい欠陥**: ネットの内部の交換レートは 3.60／4.69＝実測より 35〜90% 高く、
   原因は `μ` を 24〜48% 過小に見ること＝**ライフを正しく恐れ札を軽く見ている**。
   歪みの向きは守りすぎなのに実プレイは守らなすぎ＝**行動の誤りは価値から来ていない**。
   `2026-09-13_lambda_w32.md`
6c. **決着時間の分布とハザード** ＝ **測定した。形が在る**（2026-09-13・92,800 行）＝
   `hazard_spread` は帯ごとに 0.26〜0.63（基準 0.3 超）・同じ帯の中で `P(win|T=t)` が 0.12 → 0.53 と動く
   ＝分解 `P(win) = Σ_t P(T=t)·P(win|T=t)` は実際に働いている＝
   **時間軸ヘッドが学ぶべきは平均 1 個ではなく `h(t)`**。`2026-09-13_readout_w32.md` §5
6d. **攻め側の予算（ドンの使い切り）** ＝ **測定した。「ドン付与が苦手」は成立**（2026-09-13・92,800 ターン）＝
   `don_idle` 2.32／6.38 個・**1 個以上余らせたターンが 77.1%**（基準は半分超）。
   **相手ライフ 1 でいちばん余らせる（4.79）**＝リーサル圏で最も残している。
   超過パワーは 38.5% が x≤0・`x≥3k` は 24.5% だけ＝段に跨げる圧力を作れていない。
   `2026-09-13_readout_w32.md` §6
7. **零和の反対称が破れている**（2026-09-13 実測）＝ **切り分けも済んだ**:
   偏りは**手番でない席の評価だけ**（r3 は手番側 `gap` +0.006 に対し非手番側 −0.035）＝
   訓練の行が決定点の席からしか作られないことの帰結。**r5 では破れていない・r10 が最も悪い**
   （−0.170・両側が悲観）＝世代で単調ではない。**大きさは帯で 2 倍動く**ので
   最初の「片側 3.5 ポイント」は上振れ（r3 は −0.06 級＝片側 1.4 ポイント）。
   **実害は残る**——探索は窓で「そのとき手番でない側」の価値を使う。
   直し方は 2 案（反対称の正則化／非手番側の行を訓練に入れる）＝**ユーザ決定**。
   `2026-09-13_zero_sum_calib.md`・`cpu_theory_gap.md` §3.1
8. **PIMC の融合は接戦帯に限局**（2026-09-13 実測・close で割れ 44%・融合 2.0%・Q の幅 0.361）
   ＝探索は今のところ上限を作っていない。融合の**損の大きさ**は反事実が要る。`game_theory.md` §5
9. **読まれやすさ `H(行動｜公開状態)`** ＝ **測定した。公開情報からは読めない**（2026-09-13・
   守り 174,894 行・攻め 442,116 行）＝予測器の `lift` は守り 0.000〜0.002・攻め −0.000〜0.005
   （基準は「+0.05 未満なら読めない」）・`covered` 1.00・`bands_per_row` ≤0.011。
   **帰結: 混合（確率的な打ち回し）を入れる必要は今はない**。ただし「読める」≠「搾取できる」で
   本器は前段だけ・予測器は多数決＝**より強い予測器なら読める余地は残る**。
   `2026-09-13_readout_w32.md` §7
9b. **定式化だけ残っていた 3 件は片付いた**（2026-09-13・ユーザ指示「B 3 件からやりましょうか」）:
   (1) **ドンを残す項**＝`game_theory.md` §13（**窓ではドンは埋没・価格は 1 ターン前に決まる**。
   残す条件 `p_need·E[Θ − c_paid] ≥ Δpressure` は**実測値では成立しない**＝
   **平均的にはドンを残すのは損・例外はライフが薄い帯だけ**）。
   (2) **「読める ≠ 搾取できる」の後段**＝§21.1（`0 ≤ G(f) ≤ G* ≤ e(σ)`・**`G*` は
   予測器に依らずエンジンの `Q` から測れる**＝これで閉じるのが筋）。
   (3) **`max(c̄, k̄)`**＝§11.2（**窓の規則は 1 本で足りる**——`μ` が実測なら攻めの用途は
   折り込まれている＝前言の訂正。**内訳が要るのは自分のターンの「札の用途」の判断**）。
   **副産物として記号の混同を直した**＝`c(x)`（費用曲線）と `Θ`（無差別点）は別物（§12.1）。
   **新しい未検証**: `k̄_use` の符号が支配的なら「守れるのに受けた 89.4%」は**正しい打ち回し**に
   なりうる＝`can_or_wont` の結論は**予算側から独立に裏付けられていない**。
9c. **「理論の量をネットに足す」は測って絞れた**（2026-09-13・ユーザ指示「今の価値ネットが
   どのように考えているかをゲーム理論と対照して何が足りないか検証し、補助ヘッドまたは入力
   パラメータを足して再学習する」への回答・波 32 の holdout 1,041 局・r3）:
   **(1) 理論の価格は当たっていた**——`δ` の理論値 `0.66·μ` = 0.0286 が実測 0.0277 と **−3%**
   で一致（**自由パラメータ無しの予測**＝確認）・`ν` は §14.1 の式が 0.109 で整合（自由パラメータ 3 つ）・
   4 通貨とも観測の傾きが実測と一致＝**ネットの `V` は 4 通貨を正しい水準で追えている**。
   **(2) 入力列を足す線は消えた**——理論の状態量 23 個（`G`・`S`・`Θ`・`x*`・`ν_sum`・`n_rested` …）
   すべてが `|β|·sd < 0.01` で有意なものゼロ＝**ネットの残差を 1 つも説明しない**。
   **(3) 足すべきは「資源の流れ」**——`life_spent` 0.163／`life_taken` 0.162 だけが突き抜けた
   （`band_r2` 0.57＝盤面から半分以上読める）。**`t_real` は 0.0067**＝**r10 の時間軸ヘッドが
   学んだ量には説明力がほとんど無い**（アリーナが動かなかったことと整合）。
   **私の想定が外れていた**＝ネットは**水準**を知っており、知らないのは**これからの動き**。
   `2026-09-13_phase1_w32.md`
9d. **(3) の帰結として書いた「ライフの流れをヘッドに」は同日に撤回した**（2026-09-13・
   `cpu_theory_gap.md` §7.2）＝**リポジトリの中に同じ実験が 2 回在った**:
   **1 手先の流れは `record_gen.aux_from_ledger` の `aux[0]`／`aux[3]` で N 系の全世代が持っており**、
   **決着までの流れは r10 の時間軸ヘッドの列 5／6**（`my_life_end`／`opp_life_end`）で、
   **r10 は 0.8 枚の精度で当てている**のに**アリーナは動かなかった**（+1.8／−19.9 Elo）。
   ＝**ヘッドの線は「強さ」の水準で既に反証されている**（残差の分解を待つ必要は無い——
   r10 は推論ではなく実行された実験）。**生きている線は `|v0|` 重み付き価格教師だけ**。
   併せて `future` の量の β を**状態から読める部分と読めない残り**に分ける器を作った
   （`theory_residual.py --split`）が、24 局の smoke では**両方の推定器が弱すぎて断定できない**
   （`hat_r2` 0.28／0.00・器が `predictor_too_weak` で判定を拒む設計＝
   **弱い `f̂` は artefact の偽陽性を作る**）。診断として残す。
10. **候補 F**（予算を打ち回しに入れる・**ゲーム理論から再設計**＝`cpu_theory_gap.md` §6）
   ——ユーザ指示 2026-09-13「F は最後」。**渡す量は 3 つに確定**（自デッキの `c(x)`・盤面の `Θ`・
   相手の段 `Δpressure`）＝§11.2。`Θ` だけでは自分のターンの札の用途を決められない。
   **2026-09-13 に前提を直接検査した**（`theory_order`・145,335 ペア）＝**帯で符号が割れる**:
   接戦 +0.0284・中盤 +0.0245 に対し**決着帯 −0.0229**＝**価格は方策の教師にするが
   `|v0|` で重み付けする**（全帯一律は逆効果）。`2026-09-13_phase1_w32.md` §1b
11. **昇格ゲートの構造変更**（規約＝**ユーザ決定**）

## 5. 洗って何も無かった場所（記録として）

- `leader_specs/ISSUES.md`: 「現在、テキスト準拠の期待と一致しない既知差異はない」
- `SPEC.md` §6.1: 残る制約は効果解決（「お互いの〜」の同時処理・置換のネスト中断）で、
  ライフ・カウンター・トリガーの勘定には効かない
- **エンジンはカウンターイベント・ライフのトリガー・ブロッカーをすべて実装している**
  （`rules/battle.rs`・`apply_counter`）＝予算モデルの入力は実プレイに実在する。
  2026-08-12 の v52 で挙げた不足 (a)〜(d) は**当時の Python の評価特徴の話**で、エンジンの制約ではない
