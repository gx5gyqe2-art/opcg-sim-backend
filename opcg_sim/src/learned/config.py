"""学習型CPUの探索オプション既定（本番・自己対戦の単一定義。skew防止＋意図的差の明示）。

本番 `cpu_learned.decide`／自己対戦 `p3_run._gen_task` 等、呼び出し側が個別にオプションを
指定する箇所を1定義に集約する。値そのものは変更しない（挙動不変・マジックナンバーの名前化のみ）。

- **共有既定**（本番も自己対戦も同一であるべき値。ここを唯一の正にする）: `C_PUCT` / `DIRICHLET_ALPHA` /
  `VALUE_SCALE`。
- **意図的な差**（文脈で値が異なることを明示する。揃えない）: `SERVE_*`（本番・強さ優先）と
  `SELFPLAY_*`（自己対戦・速度/探索多様化優先）。
- **触らない**（このモジュールの対象外）: `enc_version`（ロードした net の重み次元から自動判別）・
  ネット形状 `d_emb`/`hidden`/`feat_dim`（ロードした `.npz` が正）。
"""

# --- 共有（本番も自己対戦も同一であるべき。ここを唯一の正にする）---
C_PUCT = 1.5
DIRICHLET_ALPHA = 0.3
VALUE_SCALE = 10000.0            # GATE-B の L1 tanh 圧縮用（learned 経路では未使用）
# 終局値の深さ減衰: terminal を ±max(TERM_FLOOR, 1 − TERM_DECAY·depth) で返す（TreeMCTS）。
# L1 の ±(W_WIN − ply) と同じ原理＝「速い勝ち＞遅い勝ち／遅い負け＞速い負け」。減衰が無いと
# 敗勢で全候補 q=-1 に飽和し、防御（カウンター）と無抵抗（PASS）が無差別になる
# （docs/reports/cpu_learned_mark_review_20260711.md §F2）。
TERM_DECAY = 0.02
TERM_FLOOR = 0.5

# --- 文脈で意図的に異なる値（揃えない・差を明示的にレビュー可能にする）---
SERVE_SIMS = 160                 # 本番 decide_learned（強さ優先）
SERVE_DIRICHLET_EPS = 0.0        # 本番は決定的
SELFPLAY_SIMS = 40               # 自己対戦（速度優先）
SELFPLAY_DIRICHLET_EPS = 0.25    # 自己対戦は探索ノイズ
SELFPLAY_TEMP_MOVES = 8          # 序盤サンプリング手数

# --- serve 専用: root 読み出し・PIMC 世界線（探索・自己対戦データ生成は不変）---
# root 読み出しの Q 乗り換え（cpu_learned._select_root_group）: 最多訪問グループを基準に、
# 「十分競った訪問（n ≥ MIN_FRAC·n_top）」かつ「明確な Q 差（q ≥ q_top + MIN_GAP）」の代替
# だけへ乗り換える（二重ゲート）。MIN_GAP=inf で従来の argmax(N) に一致（ロールバック）。
# 較正: 実対局2局×16人間マークに対する回帰（cpu_learned_mark_review2_20260711.md §S1）。
# 初版 LCB(z=1, frac=0.2) は低訪問 Q の楽観バイアス（実測 +0.14〜+0.54 ≫ 1/√n）に対して
# 甘く、ドン付与へ誤って乗り換える退行を起こしたため、この二重ゲートへ置換した。
SERVE_ROOT_SWITCH_MIN_FRAC = 0.4
SERVE_ROOT_SWITCH_MIN_GAP = 0.05
# ターン内 sticky 世界線: 同一 (game, turn, player) の連続 decide で PIMC 決定化 seed を固定し、
# 「ドン付与→（別世界を引いて）攻撃取り止め」型の計画非一貫（無駄ドン）を抑える。
SERVE_STICKY_WORLD = True

# learned MCTS の候補生成で無駄攻撃（倒せない/届かない）・無意味なドン付与を除外する（L1/α-β と同じ
# 枝刈りを learned 候補にも適用）。False で従来（v4 まで）＝枝刈り無し。docs/cpu_v5_plan.md §4-1補。
# serve に効く（OPCGGame.legal_actions 経由・インスタンス未指定時の既定）。
SERVE_PRUNE_FUTILE = True

# v6 柱⑤（生成/serve の探索設定分離・docs/reports/v5_adoption_20260715.md §4-5）: 自己対戦**生成**の
# 枝刈り既定。生成側は枝刈りを外す＝探索が訪れない枝は学習できないため、serve 用ヒューリスティクスを
# 生成に入れると「刈った枝の反例をネットが二度と見ない」自己強化盲点になる（v5 は serve と生成の両方に
# 掛けていた）。生成ハーネス（p3_run）が OPCGGame(prune_futile=GEN_PRUNE_FUTILE) で適用する。
GEN_PRUNE_FUTILE = False

# aux 粘り項（v5 §4-1・C4 負けq飽和の緩和）: 葉評価が飽和域（|v| ≥ SAT_START）のとき、残りターン
# 補助ヘッドの予測 t̂ で振幅を減衰 v' = v·max(TERM_FLOOR, 1 − AUX_TIE_DECAY·t̂·sat) する。
# 終局の深さ減衰（TERM_DECAY）の「終局に届かない葉」への拡張＝敗勢では『本当に延命する手』を、
# 優勢では『速い勝ち』を選好。sat = clip((|v|−SAT_START)/(1−SAT_START), 0, 1)＝非飽和域は不変。
# 再学習不要（v4 学習済みの aux ヘッドを手選択に初活用）。False で従来（v4）＝減衰なし。
SERVE_AUX_TIEBREAK = True
AUX_TIE_DECAY = 0.02      # 1予測ターンあたりの減衰（TERM_DECAY と同スケール）
AUX_SAT_START = 0.8       # この |v| から減衰を線形に効かせ始める（中間域の較正は不変）

# --- 静止探索（quiescence・v35・2026-08-04）---------------------------------
# **戦闘の途中で葉評価しない**: カウンター/ブロッカー選択の最中は戦闘が未解決で、
# 「1000 を切った子」と「2000 を切った子」が**どちらも手札-1・ライフ不変で同一に見える**
# （どちらが命を救うかは解決後にしか盤面に現れない）。実測 m1@15（gen11・攻撃7000 対
# リーダー5000＋既カウンター1000）: 正解の 2000 カウンターが3択の**最下位** −0.4502、
# 不発の 1000 カウンターが最高 −0.4281 ＝ 手札の最大カウンターを温存する汎用癖が逆向きに
# 働いていた。葉が戦闘中なら**戦闘が解決するまで priors 最良手で進めてから評価**する。
# 延長中の手の質には依存しない（1000 なら 7000 同値で被弾・2000 なら 8000 で凌ぐ、が
# 算術的に確定して盤面へ現れる）。延長は transaction で巻き戻し・global random も復元＝
# 探索の副作用ゼロ。False で従来（gen11 まで）と完全に同一挙動。
# **既定 ON（gen12・2026-08-05 採用）**: 単体では決定が変わらないが、教師（`defense_cf_gen` の
# 解決後符号化）・実対局の読み出し（`SERVE_BATTLE_READOUT`）・木（`TREE_BOX_BATTLE`）と
# **3機構そろって**初めて機能する（gen12 で 8点 4.00→6.19）。gen11 以前の重みへ戻すときは
# 3つとも False に戻すこと（片方だけ ON は train/serve skew になる）。
SERVE_QUIESCE = True
QUIESCE_MAX_PLIES = 12    # 延長の上限手数（戦闘は数手で解決する。無限ループ防止の安全弁）

# --- 戦闘窓の読み出し（battle readout・v35・2026-08-05）------------------------
# **戦闘を1つの箱として畳む**（ユーザ整理 2026-08-05）: カウンター/ブロッカー窓では
# full-tree の root Q（＝箱の外の深い未来まで平均した値）でなく、**箱の出口**
# （`resolved_branch_values`＝解決後の盤面・手札・ライフ）の value を直接比べて選ぶ。
# メインフェーズ（バトルをするか・どこを殴るか）は従来どおり full-tree ＝変更しない。
#
# 根拠（v35 実測・m1@15 系）: 出口盤面の1手先評価は防御原則3類型を**すべて**正しく順序づける
# （m1@14 素通し1位 +0.076／m1@15 正解2000が1位 −0.053 > PASS −0.115／m2@58 全カウンター
# −0.901 に対し PASS −0.831 が1位）。にもかかわらず探索後 root Q では PASS が勝つ＝木の葉の
# 68% が「次の自ターン」の通常盤面で、そこは蒸留アンカーが旧レート（ライフ↔手札の交換比）を
# 保持したまま平均へ効くため。アンカーを層別に外すと今度は自ターン判断（m1@3）が崩れる
# ことを4設計で実測済み＝**読み出し側で箱を畳むのが筋**という結論。
#
# 意思決定者は葉評価と同じ value ネット自身（別系統の防御ロジックではない）。どの出口へ
# 至るかはエンジンの実計算＝算術的に確定する。False で従来（gen11 まで）と完全に同一挙動。
# **既定 ON（gen12・2026-08-05 採用）**: 解決後盤面で学習した value と**対で**効く
# （gen11 の出口評価は m1@15 で PASS が最上位のまま＝読み出しだけ変えても決定は変わらない）。
SERVE_BATTLE_READOUT = True

# --- 木の中の箱化（tree box・v35・2026-08-05）---------------------------------
# 実対局の窓を畳んでも（SERVE_BATTLE_READOUT）、**木の中の戦闘窓は通常ノードのまま**訪問を
# 配っていた＝同じ場面を木と実対局で違う規約で扱う唯一のずれ。これを解消し、木の戦闘窓ノードも
# 出口 value 最良の1手へ畳む（戦闘全体が1本のマクロ手になる）。
#
# 二人零和では相手は最善応手を返すのが正しく、PUCT の訪問混合は**収束前の副産物**であって
# 設計された保険ではない（ユーザ指摘 2026-08-05）＝畳む方がミニマックスに近い。幅も失われない
# （木には別の攻撃順・別盤面の戦闘が無数にあり、各々が独立した箱を持つ）。
#
# 期待する効果: (1) 規約の一致、(2) カウンターの組合せに費やしていた訪問がメイン判断へ回る、
# (3) **攻撃の帰結が具体的な出口として立ち上がる**＝「相手手札−1（カウンターを絞り出した）」か
# 「相手ライフ−1・手札+1（通した）」かの二択が見えるようになる。攻撃は必ず相手に損失を強いる
# ので「止められる＝無駄」ではない（ユーザ指摘 2026-08-05）。未解決の m2@66（人間は攻撃すべき
# と言うのに攻撃しない）が動くかが主要な観測点。
# **既定 ON（gen12・2026-08-05 採用）**: 実測では m2@66 は動かなかったが、**ドン付与の2点が
# 初めて動いた**（m2@44 0.00→0.62・m5@7 0.00→0.56＝どちらもノイズ幅 0.35 超）。ドン付与は
# 「その攻撃を通す/止められない形にする」投資なので、攻撃の帰結が具体的な出口として立ち
# 上がって初めて是非を判断できる、という機序。False で従来（gen11 まで）と完全に同一挙動。
TREE_BOX_BATTLE = True

# --- ターン静止（turn quiescence・v37・2026-08-06）----------------------------
# **ターンも箱として扱う**（ユーザ方針 2026-08-05「ターン終了時の状態評価までやりたい。
# ターンも箱とみなす事でゲーム全体としてのプランを立てられるように」）第1段:
# 探索の葉が**root 手番側の自ターン途中**に落ちたら、ターンが終わるまで進めてから評価する
# （メイン手＝policy 最良・戦闘窓＝出口 value の箱・`resolve_turn_inplace`）。
#
# 根拠（v36 実測・m2@66 系）: ターン途中の葉は「相手手札が減った瞬間」や「レスト露出」を
# 過大/過小評価して root Q を汚す——ナミ先攻撃の root Q −0.06 に対しターン末の実際は −0.31、
# 同一物質の出口でもロビンをレストさせた側だけ −0.082 低い（レスト入替で完全一致＝系統誤差）。
# 一方**ターン境界の盤面で比べれば正しく並ぶ**（4配置プラン×32世界: 記録対局のクマシー線が
# 最下位・最後の1ドンの配分だけで期待勝率22pt差を正しく検出）。ターン境界は自己対戦学習が
# 最も多く見た状態でもある（毎ターン必ず現れる＝value の較正が厚い）。
# 相手ターンの葉・防御窓は不変（相手の動きは決め打ちせず、分布（PIMC×def_temp 教師）と
# 毎手の再計画で扱う方針。予測を当てるのではなく頑健なプランを選ぶ）。
# False で従来（gen12）と完全に同一挙動。
SERVE_TURN_QUIESCE = False
TURN_QUIESCE_MAX_PLIES = 40   # ターン延長の上限手数（攻撃×戦闘窓を含む。無限ループ防止）

# --- プラン読み出し（plan readout・v37②・2026-08-06）--------------------------
# **ターンの箱の第2段**: 自ターンのメイン判断を「1世界の木」でなく**プラン×K世界の期待値**で
# 決める。①（葉のターン延長）は8点ゲート完全中立＋46%コストで採用見送り（箱化済みの木では
# 葉が実質ターン境界に近く、真の敵は**1世界決め打ちの seed 依存**だった＝m2@44/m5@7 の
# 0.6 前後は「正着が1位の世界」と「外す世界」の混合と実測）。②は提案（policy 温度サンプル）→
# 評価（K世界 CRN・自ターン末 value の平均）→ 実行（ターン内 sticky・次の手が非合法になったら
# 再計画）。4プラン×32世界の手動プローブが「最後の1ドンの配分だけの差＝期待勝率22pt」を安定
# 検出した原理の serve 化。プランはターンに1回だけ立てる＝コストはターン内の全 decide で償却。
# **判定 FAIL（2026-08-06・8点ゲート 7.06→6.12・m1@3 が 1.00→0.25 の bar 超え退行）**:
# ターン末 value がイワンコフ線を一貫して高く評価（−0.138 vs ウタ線 −0.178）＝**分散でなく
# バイアス**で、K世界平均では消えない（m1@3 は攻撃の無い序盤＝世界差でターン末盤面が変わらず
# 多世界化が無効な点でもあった）。gen11 で教えた m1@3 型の矯正は決定点近傍の盤面に住んでおり、
# ターン末の符号化までは転移しない。**箱の階層を1つ上げるたびに、その箱の出口盤面で value を
# 較正し直す必要がある**（戦闘箱に解決後盤面の教師が要った v35 と同型）＝ターン出口教師
# （プランCF: レフェリー世界 z をターン末符号化に付けた順位ペア）が前提条件。それまで既定 OFF。
# **v38 追試も FAIL（2026-08-06）**: ターン出口教師は狙った点を確かに直す（プランのみで学習した腕は
# m5@7 0.56→1.00・m2@66 0.88→1.00）が、**同じ value ヘッドに同居させると戦闘出口の較正が折れる**
# （m1@15 1.00→0.00）。混合コーパス・プラン行の複製均衡・α補間（0.3/0.5/0.7）を全て試して4点合計は
# 最良 3.06 < 本番 3.44。α で救えない理由は gen12 の m1@15 の正しいマージンが +0.062 しかないこと
# ＝共有重みに逆向きの勾配が掛かればどの混合比でも先に潰れる。真の勝率は盤面ごとに1つに定まるので
# 両者は論理的には矛盾せず、競合は「1組の重みで両方の較正を表す」工学的制約に由来する
# → v39 で**出力を分ける**（`ValueNet` のターン末専用ヘッド W2e/b2e＝胴体と既存ヘッドは凍結）。
# プラン読み出しの既定 ON はそのヘッドを持つネットでの再判定後に決める。
SERVE_PLAN_READOUT = False
PLAN_WORLDS = 6        # 期待値を取る決定化世界の数（プラン間で共有＝CRN）
PLAN_PROPOSALS = 6     # 提案ロールアウト本数（1本目は argmax・残りは温度サンプル・重複除去）
PLAN_TEMP = 1.0        # 提案サンプリングの温度（0=argmax）

# --- v4 学習（docs/cpu_v4_plan.md §4）---
# value 混合ラベル: y = α·z(勝敗±1) + (1−α)·q_root(探索後 root Q・終局減衰込み)。
# 勝敗単独（α=1）は v3 で忘却を実証済み・q_root が「何手で負けるか」の距離を持ち込む。
V4_LABEL_ALPHA = 0.5
# 残りターン数の補助損失（ValueNet の aux ヘッド・「2つの時計」をラベルで明示的に教える）。
V4_AUX_TURNS_WEIGHT = 0.25
# turns_left の正規化: min(turns_left, V4_TURNS_SCALE) / V4_TURNS_SCALE ∈ [0,1]。
V4_TURNS_SCALE = 15.0
